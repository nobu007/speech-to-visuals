# Task Execution with AI Hub

## Instruction Source
Custom instruction: kairo-loop.md

## Target Repository
- Name: speech-to-visuals
- Path: /home/jinno/speech-to-visuals

## Policy Version
unknown

## Instruction
あなたが作ったローカルの差分は必ず作業完了後にコミットしてください。

---

---
description: Kairoタスクを自動推定し、計画・依存関係分析・並列実装・品質確認・自己レビュー・コミットまで自律実行します。
allowed-tools: Read, Glob, Grep, Task, Write, Edit, TodoWrite, AskUserQuestion, TaskCreate, TaskList, TaskGet, TaskUpdate, Bash(git:*), Bash(npm:*), Bash(npx:*), Bash(pnpm:*), Bash(yarn:*), Bash(pytest:*), Bash(make:*), Bash(go:*), Bash(cargo:*), Bash(python:*), Bash(uv:*)
argument-hint: "[要件名(省略可)] [開始TASK-ID(省略可)] [終了TASK-ID(省略可)] [--hil] [--sequential] [--max-parallel=N] [--no-commit]"
---
<!-- spine-preamble:autoinjected -->
## 0. Spine-first source-of-truth

このリポジトリに `specs/_doc_spine.yml` が存在する場合、以下を**絶対優先**する：

1. `SYSTEM_CONSTITUTION.md` を読む（不可侵ルール）
2. `specs/_doc_spine.yml` で対象 feature_id を同定する
3. **正本は `specs/<feature_id>/`**。要件・設計・タスク・ノートを同じ feature ディレクトリに置く
4. acceptance criteria（design.md `## Acceptance criteria` の `- [ ]`）が完了条件
5. 担当 feature の `code_root` 外には触れない
6. legacy code の削除は `## Supersedes` 宣言＋全 criteria green まで保留
   （`spine --audit-supersedence` で確認）

spine が無い repo でも、新規 Kairo 成果物は `specs/<feature_id>/` に統一する。既存 `docs/**` は読み取り・移行元としてのみ扱う。

## 0.5 AI Hub main-loop context gate

Kairo loop は `make run` の Kairo 系列と、`make alternating-loop` の 23系/21系改善系列を接続する実装ドライバである。`.audit/` は重要な入力だが、唯一の正本ではない。step0 の前に、存在するものを以下の優先順で読む。

1. 最高法規と目的: `SYSTEM_CONSTITUTION.md` / `hub/constitution.md`, `PURPOSE.md`
2. 実行者向け文脈: `AGENTS.md`, `CLAUDE.md`, `HANDOFF.md`, `TASKS.md`, `README.md`
3. specs 正本: `specs/_doc_spine.yml`, `specs/<feature_id>/`, `specs/<feature_id>/design.md`, acceptance criteria, code_root
4. concept 制約: `.concept/invariants.yml`, `.concept/ontology.yml`, `.concept/mappings.yml`
5. 既存計画: `specs/**`, Claude Code TaskList（legacy `docs/spec/**`, `docs/design/**`, `docs/tasks/**` は読み取り・移行元のみ）
6. audit/feedback: `.audit/eval_log.txt`, `eval_log.txt`, `.audit/stagnation_analysis.yml`, `.audit/analysis/gap.yml`, `.audit/improvement_feedback.yml`, `.audit/execution/feedback_to_auditor.yml`, `.audit/purpose_driven_plan.yml`, `.audit/quality/**`, `.audit/constitution/**`, `.audit/meta_analysis.json`, `.audit/proposal/changes/**`
7. 実態: `git status --short`, `git log --oneline -10`, 直近差分、テスト・lint・coverage の現状

`make alternating-loop` は 23系で PURPOSE を未来志向に再構成し、21系を複数回実行して SYSTEM_CONSTITUTION/PURPOSE/eval_log/直近コミットに基づくコード改善を進める。Kairo loop はこの流れと矛盾せず、未達 PURPOSE・spine acceptance・concept 不変量・失敗テスト・audit CM/GAP を実装可能 TASK に落とし込む。docs-only/spec-only の増殖を実装進捗とみなさない。

North Star: AIによる全自動開発を、人間主導より圧倒的に効率よく成立させる。loop は観測→実装→検証→自己レビュー→コミットまでを閉じ、substantive verified commit を最優先成果にする。

# kairo-loop

指定または自動推定した Kairo TASK 範囲を、計画フェーズからコミットまで自律実行する。引数が不足していても、TaskList・タスク文書・git 状態から推定して停止しない。

## context

```yaml
requirement_name: {{requirement_name}}     # 引数・TaskList・specs/<feature_id>/tasks から自動推定
start_task_id: {{start_task_id}}           # 省略時は未完了かつ依存解決済みの最初の TASK
end_task_id: {{end_task_id}}               # 省略時は同一要件内の未完了 TASK の最後
hil: {{hil}}                               # --hil 指定時のみ true。既定 false
execution_mode: auto                       # auto / sequential。--sequential 指定時は sequential
max_parallel: 3                            # --max-parallel=N 指定時は N。上限 5
auto_debug: true                           # 全 TASK 後に /tsumiki:auto-debug を実行
commit_enabled: true                       # --no-commit 指定時のみ false
max_task_retry: 2                          # TASK 単位の自動戻り・再実行上限
max_review_fix_round: 2                    # 自己レビュー指摘の自動修正上限
state_file: tmp/kairo-loop/{{requirement_name}}-{{timestamp}}.md
main_loop_context: {{main_loop_context}}
```

モデル選択ルールは `skills/kairo-implement/SKILL.md` と同じにする。

- `thinkTaskName`: `--think-model` があればそれ、なければ `--model`、どちらもなければ `opus`
- `tddTaskName`: `--tdd-model` があればそれ、なければ `--model`、どちらもなければ `sonnet`
- `noteTaskName`: `--note-model` があればそれ、なければ `--model`、どちらもなければ `haiku`

## 基本方針

- 引数不足でユーザーに質問しない。合理的に推定して続行する。
- 不明点は「最も安全な自動推定」を採用する。危険・破壊的・コミット不能な状態だけ簡潔に報告して停止する。
- 長時間処理に備えて、TodoWrite と `state_file` に計画・依存関係・実行 wave・リトライ履歴・検証結果を逐次保存する。
- `~/tsumiki/skills/kairo-implement/SKILL.md`、`skills/kairo-implement/kairo-tdd-process.md`、`skills/kairo-implement/kairo-direct-process.md` の工程を、実装 TASK サブエージェントの標準手順として読み替える。
- サブエージェントには「担当 TASK、参照文書、推定 write set、禁止事項、成功マーカー」を完全に渡す。サブエージェントが追加情報を待たなくてよい粒度にする。
- メインエージェントは依存グラフ、並列制御、共有ファイル更新、全体検証、自己レビュー、コミットを担当する。
- main_loop_context を TodoWrite と `state_file` に保存し、各 TASK の read_set / write_set / test_scope / success marker に反映する。

## step0: 引数解析と自動推定

1. `$ARGUMENTS` から以下を抽出する。
   - `--hil`
   - `--sequential`
   - `--max-parallel=N`
   - `--no-commit`
   - `--model` / `--think-model` / `--tdd-model` / `--note-model`
   - `TASK-\d+` 形式の TASK-ID
   - 残りの文字列を要件名候補
2. main-loop context gate の入力を読み、`main_loop_context` に要約する。`.audit/` だけで判断しない。
3. `git status --short` を記録し、開始時点の変更を `baseline_git_status` として保存する。以後、ユーザー既存変更を戻さない。
4. 要件名がない場合、以下の優先順位で推定する。
   - TaskList の未完了 TASK の metadata.requirement_name
   - `specs/*/tasks/overview.md` のうち未完了チェックボックスを持つ最新更新ディレクトリ
   - `specs/*/tasks/overview.md` のうち未完了チェックボックスを持つ最新更新ファイル
   - `specs/*/note.md` と `specs/*/design.md` の共通名
5. 開始 TASK がない場合、以下の優先順位で推定する。
   - TaskList の `status=pending` かつ `blockedBy` が空の最小 TASK-ID
   - タスク文書で未完了、かつ明示依存が完了済みの最小 TASK-ID
   - 未完了 TASK の最小 TASK-ID
6. 終了 TASK がない場合、同一要件で未完了の最大 TASK-ID にする。
7. 範囲外に未完了の依存 TASK がある場合は、自動的に実行範囲へ追加する。同一要件外の依存だけは blocked として記録し、その依存を持つ TASK を今回の実行対象から外す。
8. 推定結果をユーザーに短く宣言し、step1 へ進む。

## step1: コンテキスト収集

存在するものを優先して読み込む。

- `specs/{feature_id}/note.md`
- `SYSTEM_CONSTITUTION.md` / `hub/constitution.md`
- `PURPOSE.md`
- `AGENTS.md` / `CLAUDE.md` / `HANDOFF.md` / `TASKS.md`
- `.concept/**`
- `.audit/**` のうち main-loop context gate で指定したファイル
- `specs/_doc_spine.yml` と対象 `specs/<feature_id>/design.md`
- `docs/rule/**`
- `docs/rule/kairo/**`
- `docs/tech-stack.md`
- `CLAUDE.md`
- `AGENTS.md`
- `README.md`
- `specs/{feature_id}/tasks/overview.md`
- `specs/{feature_id}/tasks/overview.md`
- `specs/{feature_id}/tasks/TASK-*.md`
- `specs/{feature_id}/tasks.md`
- TaskList / TaskGet で取得できる Claude Code タスク詳細

不足しているタスクノートがある場合は、Task ツールで以下を実行して生成してから読み込む。

```text
@Task tool (subagent_type: general-purpose, model: {{noteTaskName}})
/tsumiki:kairo-tasknote {{要件名}}
```

## step2: タスク計画フェーズ

実行対象 TASK を正規化し、`state_file` と TodoWrite に保存する。

各 TASK について以下を抽出・推定する。

- `task_id`
- `title`
- `task_file`
- `claude_task_id`
- `task_type`: `TDD` / `DIRECT`
- `status`
- `phase`
- `dependencies`
- `blockedBy`
- `read_set`: 参照予定ファイル
- `write_set`: 変更予定ファイル
- `shared_write_set`: `overview.md`、TaskList、ロックファイル、DB migration、環境設定など共有更新になり得るファイル
- `test_scope`: 実行すべきテスト
- `risk`: `low` / `medium` / `high`
- `parallelizable`: `true` / `false`

### 依存関係推定ルール

明示依存を最優先し、不足分を安全側で補完する。

- TaskList の `blockedBy`
- タスク文書内の依存関係、前提条件、関連 TASK
- overview の順序
- DB schema / migration / package manager / config / 認証基盤 / 共通型定義を変更する TASK は、それに依存する API・UI・テスト TASK より前に置く
- 同じ `write_set` を持つ TASK は並列化しない
- `write_set` が不明な TASK は高リスクとして直列実行にする
- `overview.md` と TaskList の完了更新はメインエージェントだけが行う共有作業として扱い、TASK サブエージェントには編集させない

### 実行 wave 作成

1. 依存関係グラフを DAG として構築する。
2. 循環依存がある場合は、タスク文書と実装順序から最小の循環解除順を推定し、循環箇所を `state_file` に記録する。
3. `blockedBy` が解消済みの TASK を ready set とする。
4. ready set の中から、以下をすべて満たす TASK だけ同一 wave で並列実行する。
   - 相互依存がない
   - `write_set` が重ならない
   - `shared_write_set` をサブエージェントが編集しない
   - DB migration、package manager、環境設定、生成コード、グローバル状態を共有しない
   - `risk != high`
5. 1 wave の最大並列数は `max_parallel`。既定 3、指定されても最大 5。
6. 条件を満たさない TASK は sequential wave にする。

## step3: TaskCreate / TodoWrite による進捗登録

実行対象 TASK ごとに TaskCreate または既存 TaskList を対応付ける。

```text
subject: "kairo-loop {{TASK-ID}}: {{タスク名}}"
description: "{{要件名}} の {{TASK-ID}} を kairo-implement 互換フローで実装する。依存: {{dependencies}}"
activeForm: "{{TASK-ID}} 実装中: {{タスク名}}"
```

依存 TASK が実行対象内にある場合は TaskUpdate の `blockedBy` に設定する。TodoWrite には以下を登録する。

- 計画作成
- wave N 実行
- TASK ごとの実装
- TASK ごとの品質確認
- 戻り処理
- 完了チェックボックス更新
- auto-debug
- 自己レビュー
- レビュー修正
- コミット

すべての todo には依存関係を明記する。

## step4: wave 実行

wave ごとに以下を実行する。並列可能な wave では、1つのメッセージで複数の Task tool call を送信する。

### 4.1 TASK 開始処理

- 対象 TASK の TaskUpdate を `in_progress` にする。
- `state_file` に `wave_started`、対象 TASK、開始時 git status を記録する。

### 4.2 実装サブエージェント起動

各 TASK に対して `general-purpose` サブエージェントを起動する。TDD/DIRECT の判定は `skills/kairo-implement/SKILL.md` と同じルールに従う。

```text
@Task tool (subagent_type: general-purpose, model: {{tddTaskName}})

あなたは kairo-loop の実装サブエージェントです。

担当:
- 要件名: {{要件名}}
- TASK-ID: {{TASK-ID}}
- task_type: {{TDD or DIRECT}}
- task_file: {{task_file}}
- claude_task_id: {{claude_task_id}}
- read_set: {{read_set}}
- write_set: {{write_set}}
- 依存関係: {{dependencies}}

参照手順:
- skills/kairo-implement/SKILL.md
- skills/kairo-implement/kairo-tdd-process.md
- skills/kairo-implement/kairo-direct-process.md

実行ルール:
- `skills/kairo-implement/SKILL.md` と各 process 定義を読み、担当 TASK に必要な範囲へ縮約した kairo-implement 互換フローとして実装する。
- TDD の場合は tasknote → requirements → testcases → red → green → refactor → verify-complete 相当の順で進める。
- DIRECT の場合は direct-setup → direct-verify 相当の順で進める。
- `/tsumiki:kairo-implement` をそのまま再帰起動しない。必要な調査・編集・検証は担当範囲内で直接行う。
- `overview.md`、TaskList の完了更新、git commit は行わない。これらはメインエージェントが wave 終了後に実行する。
- `write_set` 外のファイルを編集しない。必要になった場合は `KAIRO_TASK_NEEDS_EXPANSION` を返して理由と必要ファイルを報告する。
- 既存のユーザー変更を戻さない。
- 品質確認で問題があれば最大 {{max_task_retry}} 回まで戻り先を決めて再実行する。

戻り先決定:
- テストケース不足: red/testcases へ戻る
- 実装不足: green へ戻る
- リファクタリング起因の失敗: refactor へ戻る
- 検証設定・環境問題: env/build/timeout/flaky の分類を付けて報告する
- スコープ外失敗のみ: 成功扱いにせず `KAIRO_TASK_SCOPE_OUT_FAILURES` として報告する

完了時の出力:
- 成功: `KAIRO_TASK_SUCCESS`
- 失敗: `KAIRO_TASK_FAILED`
- write_set 拡張が必要: `KAIRO_TASK_NEEDS_EXPANSION`
- 変更ファイル一覧
- 実行したテスト・検証コマンド
- 残存リスク
- 完了チェックボックス更新可否
```

## step5: wave 結果統合と戻り処理

1. すべてのサブエージェントの結果を確認する。
2. `KAIRO_TASK_SUCCESS` の TASK は、以下を実行する。
   - 変更ファイルを確認する。
   - `write_set` 外の変更があれば、理由を確認して必要なら sequential fix wave に回す。
   - `specs/{feature_id}/tasks/overview.md` または対応タスク文書の完了チェックボックスをメインエージェントが更新する。
   - TaskUpdate で `completed` にする。
3. `KAIRO_TASK_NEEDS_EXPANSION` は、拡張 write_set が他 TASK と競合しないか再判定する。
   - 競合なし: 同じ TASK を sequential retry wave に入れる。
   - 競合あり: 競合 TASK の完了後に再実行する。
4. `KAIRO_TASK_FAILED` は、失敗分類に応じて戻り先 todo を追加する。
   - 追加 todo には依存関係を設定する。
   - 例: step-e に戻る場合は、step-e、step-f、step-g、品質確認を追加する。
   - retry 回数が `max_task_retry` 未満なら sequential retry wave に入れる。
   - retry 上限到達時は `blocked` として記録し、後続依存 TASK を実行対象から外す。
5. 同一 wave 内でファイル競合が見つかった場合、競合 TASK をすべて sequential integration wave に移す。
6. `state_file` と TodoWrite を更新して step6 へ進む。

## step6: wave 後の品質確認

各 wave 後に、推定した最小検証を実行する。

検証コマンドの自動検出順:

1. `specs/{feature_id}/note.md` または tasknote の `test_command`
2. `CLAUDE.md` / `AGENTS.md` / `README.md`
3. `package.json` の `test` / `lint` / `build`
4. `Makefile` の `test` / `lint` / `build`
5. `pytest.ini` / `pyproject.toml`
6. `go test ./...`
7. `cargo test`

失敗時の処理:

- ビルドエラー: `/tsumiki:build-fix` を Task サブエージェントで実行し、再検証する。
- タイムアウト: `/tsumiki:timeout-fix` を Task サブエージェントで実行し、再検証する。
- flaky: `/tsumiki:flaky-fix` を Task サブエージェントで実行し、再検証する。
- 環境問題: `/tsumiki:env-fix` を Task サブエージェントで実行し、再検証する。
- 実装起因: 影響 TASK を sequential retry wave に戻す。

## step7: 次 wave 判定

TaskList と依存グラフを更新し、未完了かつ依存解消済みの TASK があれば step4 に戻る。

未完了 TASK が以下だけになった場合は step8 へ進む。

- 範囲外依存で blocked
- retry 上限到達で blocked
- ユーザー既存変更との分離が不可能

## step8: 全体デバッグと最終検証

すべての実行可能 TASK が完了したら `/tsumiki:auto-debug` を Task サブエージェントで実行する。

```text
@Task tool (subagent_type: general-purpose, model: {{tddTaskName}})
/tsumiki:auto-debug
```

その後、検出済みの test / lint / build コマンドを再実行する。実行できないコマンドは、理由を `state_file` と最終報告に記録する。

## step9: 自己レビュー

自己レビュー専用サブエージェントを起動する。レビューでは編集しない。

```text
@Task tool (subagent_type: general-purpose, model: {{thinkTaskName}})

あなたは kairo-loop の自己レビュー担当です。
git diff と関連 TASK 文書を読み、以下をレビューしてください。

- 要件充足
- TASK 完了チェックの妥当性
- テスト不足
- 仕様漏れ
- セキュリティ
- パフォーマンス
- 並列実行による統合ミス
- write_set 外の不要変更
- 既存ユーザー変更の巻き戻しがないか
- コミットしてよい変更だけか

出力:
- `SELF_REVIEW_PASSED` または `SELF_REVIEW_ISSUES_FOUND`
- findings: severity / file / reason / fix proposal
- commit readiness: yes/no
```

`SELF_REVIEW_ISSUES_FOUND` の場合:

1. severity high / medium の指摘を修正 todo として登録する。
2. 修正範囲ごとに general-purpose サブエージェントを起動する。
3. 修正後に step8 の最終検証を再実行する。
4. 最大 `max_review_fix_round` 回まで繰り返す。
5. 上限後も high が残る場合はコミットせず、最終報告に残す。

## step10: コミット

`commit_enabled=true` かつ自己レビューが `commit readiness: yes` の場合のみ実行する。

1. `git status --short` と `git diff --name-only` を確認する。
2. `baseline_git_status` と比較し、今回の kairo-loop が変更したファイルだけをステージする。
3. 既存ユーザー変更と今回変更を安全に分離できない場合は、コミットせずに理由を報告する。
4. ステージ対象には以下を含める。
   - 実装・テスト・設定変更
   - タスク完了チェック更新
   - 必要なドキュメント更新
5. ステージ対象には以下を含めない。
   - `tmp/kairo-loop/**`
   - 既存の未追跡ファイル
   - ユーザーが開始前から変更していたファイルで今回変更と分離不能なもの
6. コミット前に `git diff --cached --check` を実行する。
7. コミットメッセージは以下を基本形にする。

```text
feat(kairo): complete {{要件名}} {{start_task_id}}-{{end_task_id}}
```

実装ではなく設定・文書のみの場合は `chore(kairo): ...` にする。

## step11: 完了報告

以下を簡潔に報告する。

- 推定した要件名と TASK 範囲
- 実行 wave 数、並列実行数、直列化した理由
- 完了 TASK / blocked TASK / retry TASK
- 実行した検証
- 自己レビュー結果
- コミット hash またはコミットしなかった理由
- 残存リスクと次に必要な作業

## rules

MUST:

- 引数不足でも自動推定して進める。
- `skills/kairo-implement/SKILL.md` の実装タイプ判定と TDD/DIRECT フローを尊重する。
- 実装 TASK は Task サブエージェントに委譲する。
- 依存関係と write_set が安全な TASK だけ並列実行する。
- 並列化に迷う場合は直列化する。
- wave ごとに `state_file` と TodoWrite を更新する。
- TASK 完了時は TaskUpdate を `completed` にし、完了チェックボックスを更新する。
- すべての TASK 完了後に `/tsumiki:auto-debug` を実行する。
- 自己レビューを通してからコミットする。
- 既存ユーザー変更を戻さない。

NEVER:

- 未完了依存のある TASK を強行実行しない。
- 同じファイルを変更する TASK を並列実行しない。
- サブエージェントに `overview.md`、TaskList、git commit を更新させない。
- retry 上限を超えて無限ループしない。
- 検証失敗や high severity の自己レビュー指摘が残ったままコミットしない。
- `git push` は実行しない。

---

## Execution Context
You are executing this task within AI Hub's observation and evolution system.

### Requirements
1. Execute the instruction exactly as specified
2. Work within the target repository
3. Make minimal, focused changes
4. Ensure all tests pass
5. Document your decisions

### Output Format
After completion, provide:
- Summary of changes made
- Decision trace
- Files modified
- Test results

### Stopping Conditions
- Stop if instruction is fully executed
- Stop if critical errors occur
- Ask for clarification if instruction is ambiguous

### MANDATORY: Commit Protocol
You MUST commit all changes before finishing. This is a hard requirement.
After completing work, execute:
```bash
git add -A
git commit -m "<type>(<scope>): <description>"
```
Types: feat, fix, docs, test, refactor, chore, audit, perf
NEVER leave uncommitted changes. The observation system counts commits to
measure productivity. Zero commits = the work is considered lost.
