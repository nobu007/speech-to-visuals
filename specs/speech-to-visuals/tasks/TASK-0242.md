# TASK-0242: tests ツリー non-null assertion ratchet 単調減少ラウンド 8・node≥7 全数 12 ファイルで tests/unit 残存最大層に初本格着手（guards 60 → 51・analysis 13 → 6・integration 71 → 50・unit 103 → 61・visualization 61 → 54）

**要件**: [requirements.md](../requirements.md) REQ-345 / **受け入れ基準**: [acceptance-criteria.md](../acceptance-criteria.md) TC-341
**種別**: DIRECT (実装 + guard)　**推定工数**: 3h　**信頼性**: 🔵 *A154 残課題（「tests 残 338 の継続縮小」）+ steering の ratchet 単調減少継続指令 + guard-first per-file census 実測*

## 背景

Phase 154（TASK-0241 / REQ-344）が降順上位 10 ファイル 90 node → 0（総 428 → 338）を完了。steering が単調減少の継続を指令。本ラウンドは機械閾値を **「node ≥ 7 全数」** に引き下げて選定した — 該当 12 ファイル（86 node・5 ディレクトリ横断）がそのまま降順上位と一致する:

- tests/guards/node-extent-scan-single-source.test.ts — **9 node**
- tests/analysis/llm-cache-stats-paths.test.ts — **7 node**
- tests/integration/export-job-lifecycle.test.ts — **7 node**
- tests/integration/export-security-e2e.test.ts — **7 node**
- tests/integration/secure-download-edge-cases.test.ts — **7 node**
- tests/unit/export/apng-encoder.test.ts — **7 node**
- tests/unit/monitoring/pipeline-metrics-collector.test.ts — **7 node**
- tests/unit/pipeline/pipeline-orchestrator-quality.test.ts — **7 node**
- tests/unit/quality/batch-operation-recovery.test.ts — **7 node**
- tests/unit/quality/error-recovery-health-tracker.test.ts — **7 node**
- tests/unit/quality/error-recovery-state-management.test.ts — **7 node**
- tests/visualization/strategies/dagre-layout-strategy.test.ts — **7 node**

最大残存でありながら steering 指令の integration/visualization 優先で手つかずだった **tests/unit（103）に初めて本格着手**（6 ファイル 42 node → 0・103 → 61）。steering の「tests/unit が 0 になった時点で 14 ディレクトリ個別 pin → tests 全体 exact-0 pin 集約」指令にはまだ届かない。

## 実施内容（2026-08-20 完了）

### 1. guards/node-extent-scan-single-source.test.ts 9 node → 0（fail-loud extents unwrap）

`foldNodeExtents(...)` は空入力のみ `null` を返すため、非空リストを渡す fold site 9 件を wrap:

```ts
function requireExtents(result: NodeExtents | null): NodeExtents {
  if (result === null) throw new Error('expected non-empty node extents');
  return result;
}
```

空入力 pin の `expect(foldNodeExtents([], [])).toBeNull()` は helper 適用対象外としてそのまま保存（検証対象は null 戻り自体）。

### 2. analysis/llm-cache-stats-paths.test.ts 7 node → 0（`requireDisk`・Phase 153 同型）

`readCacheFile()` が `| null` を返すため cachePath 付き throw に集約:

```ts
function requireDisk() {
  const disk = readCacheFile();
  if (disk === null) throw new Error(`readCacheFile() returned null for ${cachePath}`);
  return disk;
}
```

`const disk = requireDisk();` 3 site + `expect(requireDisk().entries)` 4 site。

### 3. integration 3 ファイル 21 node → 0（`requireDequeued` / `requireArtifact` / `requireDownloadUrl`）

- **export-job-lifecycle.test.ts**: `dequeue(): QueuedExportJob | undefined` を `requireDequeued(queue)` に・artifact 読み取りを `requireArtifact(store, artifactId)` に（type import `QueuedExportJob` / `StoredArtifact` 追加）。直前の `expect(dequeued).toBeDefined()` 対は throw に折りたたみ。
- **export-security-e2e.test.ts**: `generateDownloadUrl(): ArtifactDownloadUrl | undefined` を `requireDownloadUrl(dl)` に（undefined guard）。**perl 一括置換の事故 2 件**: rule が `const dl = …` 宣言を `const dl1 = …` に改名して次行の `dl.url` が ReferenceError（suite 実行で検出し復元）・helper のドキュメントコメント内 `dl!.url` まで `dl.url` に変換（Edit で復元）。
- **secure-download-edge-cases.test.ts**: 同型 `requireDownloadUrl` を file 内に再導入。line 190 の `const dl1 = store.generateDownloadUrl(stored1.artifactId);` は perl `\b` anchor が match せず個別 Edit で対処。

### 4. unit/export/apng-encoder.test.ts 7 node → 0（`requireChunk`）

`chunks.find(c => c.type === 'IHDR')!` 系 7 site を chunk type 名付き throw に:

```ts
function requireChunk(chunks: PngChunkInfo[], type: string): PngChunkInfo {
  const chunk = chunks.find((c) => c.type === type);
  if (chunk === undefined) throw new Error(`missing ${type} chunk`);
  return chunk;
}
```

直前の `expect(ihdr).toBeDefined()` 1 組を折りたたみ。

### 5. unit/monitoring/pipeline-metrics-collector.test.ts 7 node → 0（`requireStage`）

`snap.stages.find(...)` 4 site + `c.getSnapshot().stages.find(...)` 3 site を stage 名付き throw に:

```ts
function requireStage(snap: PipelineMetricsSnapshot, stage: string): StageDurationAggregate {
  const agg = snap.stages.find((a) => a.stage === stage);
  if (agg === undefined) throw new Error(`stage ${stage} missing from the snapshot`);
  return agg;
}
```

### 6. unit/pipeline/pipeline-orchestrator-quality.test.ts 7 node → 0（typed `QualityCall` + narrowing）

spy call 配列の要素型を `Parameters<QualityMonitor['recordMetrics']>` から導出し find site を wrap:

```ts
type QualityCall = Parameters<QualityMonitor['recordMetrics']>;
function requireQualityCall(call: QualityCall | undefined, stage: string): QualityCall {
  if (call === undefined) throw new Error(`quality monitor was not called for stage ${stage}`);
  return call;
}
```

find callback の引数注釈も `(call: QualityCall)` に短縮。`scores!` は `if (scores === undefined) throw` narrowing で保存。

### 7. unit/quality/batch-operation-recovery.test.ts 7 node → 0（`requireItemError`）

`ItemResult.error` は optional だが全 site が item の失敗を主張するため index 付き throw が verdict を保存:

```ts
function requireItemError(item: ItemResult<unknown>, index: number): ClassifiedError {
  if (item.error === undefined) throw new Error(`expected item ${index} to carry a classified error`);
  return item.error;
}
```

**型の落とし穴**: (a) 素の `ItemResult` は generic（TS2314）のため `ItemResult<unknown>` が必要・(b) `ClassifiedError` は本 module から re-export されないため `@/quality/error-classifier` から直接 import。line 445 の standalone `expect(result.items[0].error).toBeDefined()` は bang 無しの正当な verdict ため未触。

### 8. unit/quality/error-recovery-health-tracker.test.ts 7 node → 0（`requireStageScore` + generic `requireBreaker`）

`assessment.stages.find(...)` を `requireStageScore(assessment, stage)` に。`breakers.get('...')!` 3 site は **Map value の member shape が site 毎に異なる**ため generic 必須:

```ts
function requireBreaker<T>(breakers: Map<string, T>, stage: string): T {
  const breaker = breakers.get(stage);
  if (breaker === undefined) throw new Error(`circuit breaker for ${stage} was not initialized`);
  return breaker;
}
```

### 9. unit/quality/error-recovery-state-management.test.ts 7 node → 0（generic `requireBreaker`）

`recovery['circuitBreakers'].get('...')!` 7 site を `requireBreaker(recovery['circuitBreakers'], '...')` に（§8 と同型の generic を file 内に導入）。

### 10. visualization/strategies/dagre-layout-strategy.test.ts 7 node → 0（typed `findNode` + `?? Number.NaN` + superfluous `!` 除去）

Phase 152/154 と同型の `findNode`（`DiagramLayout` / `PositionedNode` 型付き・import 追加）で 3 find site を wrap。残る 4 node は 2 系統:

- `longNode.w!` → `longNode.w ?? Number.NaN` — `w` は optional ため undefined→failed-matcher verdict（NaN 比較 false）を保存
- `edge.points!` ×2 → **superfluous `!` 除去のみ** — `LayoutEdge.points` は non-optional のため純粋な checker 抑制（ラウンド 7 flowchart-strategy と同型の挙動保存）

### 11. ratchet pin 更新 + MW-021

census guard（tests/guards/non-null-assertion-census.test.ts）:

- `TESTS_DIR_PINS`: guards 60 → **51**・analysis 13 → **6**・integration 71 → **50**・unit 103 → **61**（初の本格着手）・visualization 61 → **54**
- `PINNED['tests (excl. __mocks__)']`: 338 → **252**
- baseline chain に `338 − 86 = 252`（内訳: guards −9・analysis −7・integration −21・unit −42・visualization −7）を追記・liveness note に `−110 / −90 / −86` の減少系列を追記
- ヘッダに Phase 155 bullet（12 ファイル・helper 群）と **MW-021** を追記

**MW-021**（ledger 台帳・監査 pin ≥20 → ≥21）: Phase 155 rewrite への `!` 1 node 再注入 — `expect(longNode.w ?? Number.NaN).toBeGreaterThanOrEqual(shortNode.w ?? Number.NaN);` → `expect(longNode.w!).toBeGreaterThanOrEqual(shortNode.w ?? Number.NaN);`（dagre-layout-strategy.test.ts）— で **tests 合計 ratchet（`Expected: <= 252 / Received: 253`）と tests/visualization dir ratchet（`Expected: <= 54 / Received: 55`）の 2 tests RED** を実測 → revert で GREEN・監査 pin ≥21 GREEN。

**補正**: なし — 編集 15 ファイル（test 12 + census guard + ledger guard + ledger md）に source-anchor guard の肯定 pin は存在しない。計数器（AST NonNullExpression + `!:` node）の意味変更なしのため pin 再ベースラインの新旧対応表も不要（減少のみ）。

## 検証

- census + ledger 監査 guard GREEN（**2 suites / 55 tests**・監査 pin ≥21）
- 置換対象 12 suite を含むパターン一致: **14 suites / 364 tests GREEN**（TC-341 再検証コマンド）
- `node_modules/.bin/tsc -p tsconfig.test.json --noEmit`: **0 新規 error**（census guard の既知 3 error のみ・TS2503 ts namespace ×2 + TS2339 exclamationToken）・`tsconfig.app.json` は 0 error
- フルスイート（全変更後・並走なし）: **742 suites / 742 passed・23,165 passed / 0 failed / 17 skipped（jest Time 137.507s）**。run 履歴は A155 の [EVIDENCE] 行に出典（+2 は MW-021 の ledger it.each 増のみ — R7 の MW-020 と同一パターン）
- MW-021 mutant RED 2 failed → revert → GREEN サイクル実測
- census 実測: guards = **51**・analysis = **6**・integration = **50**・unit = **61**・visualization = **54**・tests 合計 = **252**

## 完了条件

- [x] guards 60 → 51・analysis 13 → 6・integration 71 → 50・unit 103 → 61・visualization 61 → 54・tests 合計 pin 338 → 252（guard pin 更新 + GREEN）
- [x] 対象が guard-first survey の node≥7 全数（降順上位 12 ファイルと一致）で選択されていること
- [x] 置換 12 ファイルの `!` AST node = 0（挙動保存・verdict 保存・`?? Number.NaN` 保存・superfluous `!` 除去 2 site・generic helper 2 種）
- [x] 対象 suite GREEN・tsc 0 新規 error・full suite 0 failed
- [x] MW-021 台帳エントリ + 監査 pin ≥21
