# TASK-0273: measurement-fixture class の repo-wide census guard 新設 + 残存 9 site 一括撲滅（REQ-391・TC-375-01〜05・MW-055）

<!-- spine:anchor:begin -->
> **Spine anchor**: [speech-to-visuals アーキテクチャ設計](../architecture.md)
>
> - parent: `speech-to-visuals/architecture.md`
> - role: `detailed`
> - status: `canonical_child`
<!-- spine:anchor:end -->

**要件**: [requirements.md](../requirements.md) REQ-391 / **受け入れ基準**: [acceptance-criteria.md](../acceptance-criteria.md) TC-375-01〜05（本タスクで [x] 化）
**種別**: TDD (census RED → 撲滅 fix → mutation 検証)　**推定工数**: 3h　**信頼性**: 🔵 *steering が直接要求した audit-pass-first（「REQ-387/389/390 が同 class を 3 commit 別に直した滴り → 全候補 enum → 確定 → 一括 fix」）。census guard は実際に手動 enumeration を超える 31 key を発見し steering の懸念（滴りは同 class red の再発を許す）を実証的に裏付けた*

## 背景

REQ-383〜390 まで 8 REQ が同一 class — 測定を約束する field への凍結定数・未 populate contract（REQ-383 iteration-quality legs / REQ-384 compliance / REQ-385 empty-criteria / REQ-386 performance / REQ-387 memoryUsage / REQ-388 spine / REQ-389 LLM extraction scale / REQ-390 framework quality checks — を個別発見・個別修正してきた。steering は「1コミット1箇所の滴下は同class redの再発を許す」とし、spine anchor census（Phase 186）と同型の **全候補 enum → 確定 → 一括 fix** を要求。

手動 grep enumeration（52+ site）の後、census guard を先に書いて走らせたところ **31 key の見落とし**が発覚: 値が object literal の行末 `}` 直前にある site（`confidence: 0.92\n      }`）は comma 要求の旧 grep では不可視で、ezol `// Simulated` trio・layout-engine simple-mode `confidence: 1.0`・production-monitor thresholds・user-guided strategy table ほかが含まれた。census 自体が audit の権威を実証した形。

## 実装

1. **census guard 新設** — `tests/guards/measurement-fixture-census.test.ts`: `walkProductionSurface()`（repo src/ + @stv-core core-four）を `file::field` granularity で走査。discovery regex `([A-Za-z][A-Za-z0-9]*)\s*:\s*(-?\d+\.\d+|\d{2,})\s*(?=[,})]|//)`（trailing-comment 値込み）+ MEASUREMENT_TOKENS（accuracy/confidence/performance/health/current/score ほか 18）− CONFIG_TOKENS（threshold/min/max/target/weight ほか 11）+ EXEMPT_FIELDS（healthy/unhealthy = HTTP status map key）。7 test: completeness（未分類 site = 新規 fixture ship で RED）/ stale-row / reason 衛生（4 台帳全 entry）/ eradicated-reappear（21 key）/ DELETED_FILES（module 削除維持）/ **Math.random 測定値製造 ban** / discovery 非空。ALLOWED 38 key は design-time table・実測証拠への heuristic annotation・config vocabulary・disclosed fail-closed default・demo payload の 5 分類
2. **module 削除** — `src/monitoring/production-monitoring-excellence.ts`: 全 metric が凍結 literal か `Math.random()`（detectionAccuracy 97.8 / accuracy 94.5 predictive / random 5-20% optimizeMetric / 7 fabricated health indicator / 4 fabricated alert row）。唯一の live consumer は api shutdown の `destroy()` — import・shutdown 行・mock 2 件・defect-9 roster 行（ruledOut → DELETED コメント）を整理
3. **field 削除・neutral 化** — performance-dashboard `accuracyScore`（自己申告 fixture・測定 source なし）・continuous-learner `currentPerformance`（reader ゼロ）・main-pipeline/intelligent-cache cache metadata（`performanceScore: 0.8`/`complexity: 0.5` → neutral 0・duration は実測: transcription 自身の duration / decorator の実 elapsed）・orchestrator の tuner `quality: 0.8`（document 済み `?? 0.8` default へ委譲）
4. **決定的 placeholder 化** — whisper `PLACEHOLDER_SEGMENT_CONFIDENCE = 0.95` / streaming `PLACEHOLDER_CHUNK_CONFIDENCE = 0.75`（旧 range 下限に pin）
5. **ezol derive 化** — crossings stub `floor(len*0.1)` → round-43 single source `countEdgeCrossings` delegate（pin test Layer 3 に 4 site 目追加）・overlapArea `len*100` → 実 AABB 積和（getNodeWidth/getNodeHeight で寸法解決）・symmetryScore `return 0.75` → centroid-balance 実測導出・compactnessScore → canvasUtilization 同一値（定義的同一の明記）・readabilityScore → 実測 violation count の penalty model（`< 0.7` warning gate が定数では絶対に発火不能だった）・aestheticScore → `deriveAestheticScore` 重み合成 single source（optimizer 向け copy と duplicate-formula 解消）・dead tree quartet（frozen 300/600・[]・fabricated points・zero caller）削除
6. **live 化・統一評価** — gemini-analyzer `currentTimeoutMs` → `getAdaptiveTimeout()` public 化で live（ESM mock 2 file 追記）・layout-engine simple-mode `confidence: 1.0` → `_evaluateLayoutResult` 経由の `calculateLayoutConfidence`（complex path の DROPS fix e0f269af と同型の bypass 解消）・Iteration43Interface demo random 5 leg → 決定的 disclosed 値（test の random=0 mock と同一値で挙動不変）
7. **MW-055 mutation 検証（3 独立 RED 実測）** — (a) ezol `aestheticScore: 0.85` 再注入 → **2 failed**（eradicated-reappear `reappeared @ :1226` + completeness 二重捕捉）(b) 未 roster `healthScore: 0.99` 注入 → **1 failed**（completeness `@ :108`）(c) `95 + Math.random() * 3` 再注入 → **1 failed**（random ban `:188`）。各 revert で 7/7 GREEN・git diff clean

## 検証

- census guard 7/7 GREEN・広域 66 suites / 1346 tests GREEN（ezol・edge-crossing pin・layout-engine・gemini-analyzer・llm-service・simple-pipeline・Iteration43・graceful-shutdown・interval-leak・defect-9・performance-dashboard・continuous-learner・intelligent-cache・transcription・main-pipeline・orchestrator）
- tsc 両 config（app/test）exit 0
- 台帳 dogfood: MW-055 entry + appendix row・監査 pin **≥54 → ≥55**・ledger-shape template row 追加
