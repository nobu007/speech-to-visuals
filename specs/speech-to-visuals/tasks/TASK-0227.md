# TASK-0227: storage key read/write parity sweep + 常設 guard

<!-- spine:anchor:begin -->
> **Spine anchor**: [speech-to-visuals アーキテクチャ設計](../architecture.md)
>
> - parent: `speech-to-visuals/architecture.md`
> - role: `detailed`
> - status: `canonical_child`
<!-- spine:anchor:end -->

**要件**: [requirements.md](../requirements.md) REQ-329 / **受け入れ基準**: [acceptance-criteria.md](../acceptance-criteria.md) TC-325
**種別**: DIRECT (sweep + guard)　**推定工数**: 2h　**信頼性**: 🔵 *AI Hub steering META-intent + grep 実測*

## 背景

steering は「書き手が存在しない storage key を読む」欠陥クラスの単発スイープを指示した（固有名 `STORAGE_KEYS`・起点 commit `b86ddeb6`）。**固有名は本リポジトリに存在しない**（`STORAGE_KEYS` grep 0 hits・`b86ddeb6` は git 履歴に存在せず — cross-repo 汚染 6 件目・interview-record A141）。META-intent（reader/writer パリティの機械検証）のみ採用。

## sweep 実測（2026-08-19）

- 永続化 surface は `@stv/core/utils/safe-storage`（safeLoadFromStorage / safeSaveToStorage）+ CorruptionOverlay の動的 `localStorage.removeItem`（corruption event 文字列から key 抽出）のみ。
- 直叩き `getItem`/`setItem` は src/supabase 全域で 0 件。removeItem は CorruptionOverlay の 3 サイトのみ（いずれも動的 key）。
- key リテラル: `first-visit`（TutorialSystem load:47 + save:66）・`tutorial-progress`（load:54 + save:228）。**両 key とも load+save パリティあり・LIVE-dead な read は存在しない**。

## 実施内容（2026-08-19 完了）

`tests/guards/storage-key-parity.test.ts`（4 tests）新設 — 単発 sweep を常設不変条件に:
1. LOADED key は全て SAVED されている（dead-read 欠陥クラスの再侵入防止）
2. SAVED key は全て LOADED されている（dead-write）
3. リテラル key セットを `{first-visit, tutorial-progress}` に exact pin
4. 非 literal storage access は CorruptionOverlay のみ（`extractStorageKey` の存在 pin で免除の正当性も固定）

mutant（TutorialSystem の save key を rename）で 3 test RED を実測検証（MW-006）。

## 完了条件

- [x] sweep 結果の記録（LIVE-dead なし・cross-repo 汚染の実証）
- [x] parity guard が CI で常時検証（mutant RED 検証済み）
