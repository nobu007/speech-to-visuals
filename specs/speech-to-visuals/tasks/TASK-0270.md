# TASK-0270: spine anchor role 一括正規化 pass — role 行なし 93 file + anchor 欠落 62 file の一括解消 + census guard（REQ-388・TC-372-01〜04・MW-052）

<!-- spine:anchor:begin -->
> **Spine anchor**: [speech-to-visuals アーキテクチャ設計](../architecture.md)
>
> - parent: `speech-to-visuals/architecture.md`
> - role: `detailed`
> - status: `canonical_child`
<!-- spine:anchor:end -->

**要件**: [requirements.md](../requirements.md) REQ-388 / **受け入れ基準**: [acceptance-criteria.md](../acceptance-criteria.md) TC-372-01〜04（本タスクで [x] 化）
**種別**: DIRECT (契約 module 移植 + generator + 一括 pass + census guard + 台帳 dogfood)　**推定工数**: 2h　**信頼性**: 🔵 *make-run steering が直接指摘した gap（「repo内に依然90件超のrole行なしspecが残る。次回はspineManifestValidatorを緑に保ったまま、SPINE_ANCHOR_ROLESからroleを一括割当する有界な正規化passを1回実行し、2行ずつの滴下を繰り返さないこと」「末端掃込みコミットでanchorスキーマ修正を雑扱いしないこと」）。hub 側 spine.py の role 導出規則と実 241 block の既存 role 値を突合して導出誤差 0 を確認済み*

## 背景

spine anchor block（`<!-- spine:anchor:begin -->` … `end`）の schema は Phase 176〜185 で `> - role:` 行を要求するよう拡張されたが、その適用は各 phase の関係 file 2 件ずつの「滴下」だった。結果として Phase 186 時点の specs/ は:

- **93 file**（TASK-0109〜0222 群）: anchor block は有るが role 行なし → census 上 `ROLE_LINE_MISSING`
- **62 file**: TASK file なのに anchor block 自体がない（TASK-0223〜0269 の speech-to-visuals 47 + finite-safe-aggregation 11 + guard-harness-fold-census 4 — anchor 追加をやめていた時代の生成分）→ `TASK_ANCHOR_MISSING`
- 滴下の原因は構造的: role 行の追加が手書きで各タスクの付帯作業だったため、新規 TASK file が land しても schema 違反を検出する gate が存在しなかった

steering の指摘は「有界な正規化 pass を 1 回」+「spineManifestValidator 緑を維持」+「以後の滴下禁止」。なお指摘中の `SPINE_ANCHOR_ROLES` という名前の module は実在しない（phantom）— 実体は hub 側 `spine.py` の `_classify_filename` / `_role_for_spine_node` であり、これを in-repo に移植した `SPINE_ROLES` が対応物。

## 実装

1. **`tests/guards/spine-anchor-contract.ts`（新規・純関数契約 module）** — spine.py の導出規則の移植:
   - `classifySpineFilename(specsRel)`: filename 語彙 → `system` / `method` / `detailed` / `reference`（評価順も含め移植）。**rel は specs-relative を要求**（`specs/` prefix は fail-loud — repo-relative だと全 path に "spec" substring が載り全て `reference` に退化する罠）
   - `deriveSpineRole(specsRel, parentRel)`: top-level（parent が repo root 直下）は `feature_root` / `system_design_root`、それ以外は filename 分類
   - `parseAnchorBlocks` / `renderAnchorBlock` / `normalizeAnchorRole` / `deriveTaskAnchorDefaults` / `relativeLink` / `auditSpineAnchors`（違反 5 種: `ROLE_LINE_MISSING` / `ROLE_VALUE_MISMATCH` / `ROLE_UNKNOWN` / `PARENT_LINE_MISSING` / `TASK_ANCHOR_MISSING`）
   - `normalizeAnchorRole` は**有界**: (a) role 行なし block へ導出 role 1 行を parent 行直後に挿入 (b) `specs/<feature>/tasks/TASK-\d+.md` で anchor なし file へ正規形 block を H1（または frontmatter）直後に挿入（spine.py `_apply_anchor` と同じ位置）(c) **それ以外の specs file には触れない**（engine の outsider 判断を捏造しない境界）(d) 既存 role 値の不一致は書き換えず fail-loud（census が違反として差し戻す）
   - 親 topology は実データから導出: 全 child（sibling feature 含む）は **root feature の** `architecture.md` に anchor する（`deriveTaskAnchorDefaults` が parent anchor の root-level 判定から自動発見）
2. **`scripts/sync-spine-anchor-roles.ts`（新規 generator）** — `npm run specs:anchor:sync`（書き込み） / `specs:anchor:check`（census only・violations で exit 1）。check 出力は「末端掃込みコミット禁止・anchor 生成と同じタスク内で自己説明的 commit にせよ」と案内する
3. **一括正規化 pass（1 回）** — `specs:anchor:sync` 実行: 155 file 修正（role 行挿入 93 + block 挿入 62）。2 回目の実行は 0 file 書き込み（冪等）。**TASK-0270.md 自身の anchor block も本 generator が挿入**（dogfooding）
4. **`tests/guards/spine-anchor-role-census.test.ts`（新規 36 tests）** — 実 tree census（violations exact `[]`・`roleLines === anchorBlocks`）+ TASK file floor pin（≥277）+ 移植表の unit test + normalizeAnchorRole 全分岐 + 合成 fixture による違反検出 5 種（実 tree は正規化済みで違反ゼロのため、detector の腐敗は synthetic でしか捕まらない — MW-052 (c) の設計根拠）

## MW-052 mutation（3 独立 RED 実測）

- (a) `classifySpineFilename` default bucket `detailed`→`reference` → **6 failed / 30 passed**（classify 表 2 leg + 実 tree census role 一致 + normalizeAnchorRole 挿入 2 leg + audit 正規形 leg）
- (b) `normalizeAnchorRole` の role 行挿入位置を parent 行直後→block 末尾 → **1 failed / 35 passed**（挿入位置 exact pin）
- (c) `auditSpineAnchors` から `ROLE_LINE_MISSING` 検出を削除（detector blinding）→ **1 failed / 35 passed**（合成 fixture 検出 leg）
- 各 revert（Edit / sed 逆適用・grep で mutant 消滅確認 — `git checkout --` 不使用・Phase 178 GOTCHA）で 36/36 GREEN 復元

## 検証

- census guard 36/36 GREEN・`specs:anchor:check` GREEN（312 file + 本タスク file / 303+1 block / role 行 同数 / violations 0）
- **`spine:validate` GREEN 維持**（spineManifestValidator + specs mirror contract — steering の「緑に保ったまま」条件）
- `tsc -p tsconfig.app.json` 0 error・`tsc -p tsconfig.test.json` 0 error・`tsc -p tsconfig.node.json` 0 error
- 台帳 dogfood: MW-052 entry + appendix row・監査 pin **≥51 → ≥52**

**設計決定**: (1) 既存 role 値の機械的書き換えはしない — 移植導出規則と実 241 block の既存値の突合で不一致 0 を確認済みのため書き換え対象が存在せず、仮に今後不一致が出たら fail-loud で人手 curation（値の捏造書き換えは 正規化の有界性を壊す）。(2) role 語彙の追加（`task` 等）は hub 側 spine.py の変更が先 — in-repo 移植は語彙・評価順を spine.py と pin で固定し、hub 側変更時は census が差分を RED で知らせる。(3) `_doc_spine.yml` manifest は auto-generated / gitignore 済みのため本 pass の対象外（spine.py 再生成時に role 行付き anchor が正しく render されることを `spine:validate` が担保）。
