# TASK-0276: statement-level frozen literal（frozen return / 直値 initializer）census guard 新設（REQ-394・TC-378-01〜03・MW-058）

<!-- spine:anchor:begin -->
> **Spine anchor**: [speech-to-visuals アーキテクチャ設計](../architecture.md)
>
> - parent: `speech-to-visuals/architecture.md`
> - role: `detailed`
> - status: `canonical_child`
<!-- spine:anchor:end -->

**要件**: [requirements.md](../requirements.md) REQ-394 / **受け入れ基準**: [acceptance-criteria.md](../acceptance-criteria.md) TC-378-01〜03（本タスクで [x] 化）
**種別**: TDD (audit-pass-first → census → mutation 検証)　**推定工数**: 2h　**信頼性**: 🔵 *steering audit-pass-first の第 4 facet。REQ-391 値側（object-literal）・REQ-392 契約側・REQ-393 条件側に続き、3 census の regex 形のいずれにも合致しない文-level 形状を閉じて凍結測定 class の既知 facet を cover 完了*

## 背景

REQ-391/392/393 の 3 census は凍結測定 class を値（`field: 0.9` object literal）・契約（`field?: number` の producer 有無）・条件（ternary false-leg / `??`/`||` fallback）の facet 別に census 化したが、**文そのものが凍結値**の形状 — 測定名関数が丸ごと `return 0.9;` する・測定名変数に直値を bind する（`const healthScore = 0.95;`）— は 3 census のいずれの discovery にも入っていなかった。

audit pass（手動 enumeration・REQ-391〜393 と同一 `walkProductionSurface` 上で binding initializer 形と frozen-return 形を走査）で **16 site / 15 key** を enumerate し全 site を分類した結果、**捏造 site は 0** — 全て既存の正当分類（canonical estimator band 3・実測補正 heuristic base 8・deduction ladder / named fail-closed ceiling 3・defect-9 台帳裁決済み BY-DESIGN 1・REQ-391/393 の named disclosed placeholder 3）に滞留していた。@stv/core core-four は 0 site。

したがって本タスクは REQ-391〜393 と異なり**撲滅を同梱しない**: steering の「残件数 0 を確認してから個別修正」の確認側が完了しているため、census は confirmed-zero 状態を固定し、以後この形状の新規 site に意識的な分類（ALLOWED row + reason）を強制する。

## 実装

1. **census guard 新設** — `tests/guards/measurement-statement-literal-census.test.ts`: discovery は 2 形状を走査 (a) binding initializer `const/let/var <測定名> (: type)? = <literal>;`（REQ-391 literal 形 = bare decimal or ≥2 桁整数）(b) 測定名関数の frozen `return <literal>;` — return 行から 60 行以内の直近 enclosing declaration（`function NAME` / method `NAME(…) {` / arrow binding `const NAME = … => {`・control keyword 除外）へ attribute。identifier 判定は REQ-391 語彙（measurement token − config token）
2. **ALLOWED 18 key 完全分類** — canonical estimator band 3（`quality-estimators.ts::score` / `::scoreNodeDensity` / `::estimateRelationAccuracy`）/ measured-adjustment heuristic base 8（`INITIAL_LLM_CONFIDENCE`・language-detector / LayoutEvaluator / video-generator / enhanced-export-engine / adaptive-content-processor / smart-parameter-tuner ×2 の disclosed base ± 実測 delta）/ deduction ladder・fail-closed ceiling 3（pipeline monitor `::score`・`MEASURED_QUALITY_ABSENT_SCORE_CEILING`・quality monitor `::assessMemoryUsage`）/ defect-9 BY-DESIGN 1（`pipeline-health-score.ts::scoreCost` の no-baseline `return 100;` — 再分類は defect-9 roster の決定事項と reason 明記）/ named disclosed placeholder 3（`FINAL_NO_CONFIDENCE_STANDIN`・`PLACEHOLDER_CHUNK_CONFIDENCE` ×2）
3. **random-return ban** — 測定名関数内 `return …Math.random` を生産 surface 全体で RED（REQ-391 random ban の return-shape 対偶）
4. **合成 liveness test** — 実 tree が clean なため detector の歯は合成 fixture のみが保持（REQ-388 spine-census precedent: 証人が合成しかいない detector は静かに腐れない）: 捏造 binding / frozen return / random return の 3 形状 MUST・config-token（`maxConfidenceThreshold`）/ 非測定名（`retryCount` / `getPageTitle`）/ 一桁整数（`return 7;`）decoy MUST-NOT
5. **上限の doc 化** — census header に正直に記載: return 形は直近 enclosing declaration への attribution であり測定 token を持たない helper 経由の return は escape・binding 形は直値 initializer のみ（式は不可視）
6. **MW-058 mutation 検証（3 独立 RED 実測）** — (a) `const healthScore = 0.95;` 注入 → **1 failed**（completeness `::healthScore`）(b) `export function estimateOverallHealth(): number { return 0.9; }` 注入 → **1 failed**（completeness `estimateOverallHealth @ :346` — 実 file 上で enclosing-declaration attribution が作動）(c) random-return 注入 → **2 failed**（completeness + random-return ban）

## 検証

- census guard 6/6 GREEN・tsc 両 config（app/test）exit 0
- specs 整合: `specs:anchor:check` / `specs:mirror:check` / `spine:validate` 全 GREEN
- 台帳 dogfood: MW-058 entry + appendix row・監査 pin **≥57 → ≥58**・`tests/guards/mutation-witness-ledger.test.ts` PINNED_MIN_ENTRIES 更新

## 設計決定

1. **撲滅なしの confirmed-zero 固定** — audit で残件 0 を確認済みのため ERADICATED ledger を持たない。REQ-391〜393 との差分はここ（各々 9 / 4 / 10 site を同梱撲滅）。将来この形状で fabricated site が ship された場合、completeness test が未分類 key として即 RED にする
2. **defect-9 管轄の尊重** — `scoreCost` の no-baseline `return 100;` は defect-9 roster が BY-DESIGN 裁決し 2 regression suite が pin している。本 census は ALLOWED reason でその管轄を明記し、census が別 roster の裁決を覆さない（REQ-393 の config-knob 分類と同じルール）
3. **census beats manual の再実証** — census 初回 run は手動 enumeration を 3 key 上回って発見（`export const` named 定数 — 手 audit regex に `export` prefix がなかった）。REQ-391（31 key 超過発見）と同じく、audit-pass-first の enumeration は census 化で機械検証される必要があることの実例
