# TASK-0251: 義務 C 第1ゲート — tests/unit exact-0（残存全 21 ファイル 61 ノード一括撲滅・REQ-357・MW-033）

<!-- spine:anchor:begin -->
> **Spine anchor**: [speech-to-visuals アーキテクチャ設計](../architecture.md)
>
> - parent: `speech-to-visuals/architecture.md`
> - role: `detailed`
> - status: `canonical_child`
<!-- spine:anchor:end -->

**要件**: [requirements.md](../requirements.md) REQ-357 / **受け入れ基準**: [acceptance-criteria.md](../acceptance-criteria.md) TC-352（本タスクで追加）
**種別**: DIRECT (tests ツリー non-null assertion ratchet 単調減少ラウンド 9・義務 C 前半)　**推定工数**: 2h　**信頼性**: 🔵 *TASK-0250 §残存 obligation の指定どおり — guard-first survey で残存ノード全数を確定してから一括撲滅*

## 背景

TASK-0243 が定義した義務 C は「`tests/unit exact-0` + `tests total ≤ 100` の 2 gate 通過 + manual unskip 発火」。ラウンド 1〜8（Phase 148〜155）で tests/unit は 471 → 61 まで減ったが、残りは 21 ファイルに散在する 3〜6 ノードずつの細粒度になり、閾値ベースの選別（≥10 → ≥7）は枯渇済み。本タスクは残存 **全数** を一括で 0 にして第1ゲートを通す。

## survey（guard-first・AST census と同一ロジック）

2026-08-20 時点の tests/unit 残存: **21 ファイル / 61 ノード**（最多 = export-retry-lifecycle 6・generate-scenes 5・pipeline-health-score 5）。tests total は 252。

## 置換パターン（Phase 148/149 が確立した fail-loud idiom の踏襲）

| パターン | ファイル | ノード |
|---------|---------|-------|
| `requireDefined(value, label)` ファイル内 helper（`find()` / `dequeue()` / `getBaseline()` / optional field / null 戻り） | cors-config(2 は別パターン)…計 14 ファイル | 41 |
| `fireCapturedResolver(fn, label)` — Promise executor が同期的に捕捉する resolver 6 宣言 (`let resolveRender: () => void` → `(() => void) \| undefined`) | export-retry-lifecycle | 6 |
| `act()` コールバック内 definite-assignment (`resolveExecution!:` / `execPromise!`) → `\| undefined` 型 + 即 guard | use-framework-pipeline | 2 |
| env truthy-ternary の二重読み → const capture（`as string \| undefined` キャスト不要） | cors-config | 2 |
| `(scenes?.length ?? 0) === 0` short-circuit → const capture + `=== null \|\| === undefined` 明示 guard | nullable-access-null-guard | 3 |
| `expect(x!.length).toBe(1)` → `expect(x).toHaveLength(1)`（undefined で明確 RED） | async-resource-cleanup | 1 |

**GOTCHA（実測）**: nullable-access-null-guard の `FakeResult.scenes` は `Array | null | undefined` で、テストが `scenes: null` を直接渡す。`?.` は null も拾うため、`=== undefined` のみの等価置換は **原本より検出力が落ちる**（null で `Cannot read properties of null` の TypeError = 3 RED）。`=== null || === undefined` の両チェックで原本挙動（null → return 0）を保存して 31/31 GREEN。**`?.`/`??` を guard に置き換える際は null と undefined の両方を見る** — `x!` 撲滅で挙動を弱めてはならない。

## pin 変更

- `TESTS_DIR_PINS.unit`: 61 → **0**（transcription（Phase 154）に次ぐ 2 番目の dir exact-0。空洞化チェックは files-based なので test ファイルが存在する限り 0 pin は有効）
- `PINNED['tests (excl. __mocks__)']`: 252 → **191**
- 履歴 comment に Phase 165 / REQ-357 エントリ（round 9）と MW-033 mutation 記録を追記

## MW-033 ledger 登録

`specs/speech-to-visuals/mutation-witness-ledger.md` に MW-033 を追加（監査 pin **≥32 → ≥33**）:

| mutation | observed |
|----------|----------|
| tests/unit/monitoring/health-check-service.test.ts:761 `expect(cached.status).toBeDefined()` → `expect(cached!.status).toBeDefined()`（Phase-165 rewrite への単発 `!` 再注入） | `2 failed / 9 passed / 11 total` — tests/unit dir ratchet（`Received: 1`・expected ≤ 0）と tests-total ratchet（`Received: 192`・expected ≤ 191）の**同時 RED**。revert で census + health-check-service 系 3 suites / 76 tests GREEN 復元 |

## 完了条件

- [x] guard-first survey で tests/unit 残存 21 ファイル 61 ノードを確定（AST census 同一ロジック）
- [x] 全 61 ノードを fail-loud idiom で置換（`!`/`!:` ゼロ・挙動保存 — null チェック漏れ 3 RED を検出し修正）
- [x] census pin unit 61 → 0（2 番目の dir exact-0）・total 252 → 191・census 11/11 GREEN
- [x] 触 21 suite + 同名衝突 = 35 suites / 826 tests GREEN・guards 76 suites / 3226 GREEN・tsc 0 error
- [x] MW-033 mutation witness（unit 0→1 と total 191→192 の同時 RED 2 failed 実測 + revert GREEN 復元）+ 台帳登録 + 監査 pin ≥32 → ≥33
- [x] REQ-357 / TC-352 / TASK-0251 / overview Phase 165 を同一 commit に同梱
- [x] `npm run specs:mirror:check` — exit 0（REQ-357 は §Edgeケース 追加であり mirror 対象節 §非機能要件 の外・stamp 無影響を確認）

## 残存 obligation（次サイクル）

- **義務 C 第2ゲート**: tests total ≤ 100（現状 191。内訳 guards 51・visualization 54・integration 50・pipeline 11・quality 9・analysis 6・他 10 — 次は guards/visualization/integration の上位ファイル）
- **manual unskip 発火**: TASK-0243 TC-342 の end-of-ratchet gate（tests 全 dir exact-0 到達時）
- **adaptive-quality-gates の non-finite 対応**: TASK-0248 から引き続き（production wiring 無し・別 TASK のまま）
- **mirror 対象の拡張**: 真の mirror が新たに生まれたら marker 追加（TASK-0249 から引き継ぎ）

## 検証

- `NODE_OPTIONS='--experimental-vm-modules --max-old-space-size=4096' npx jest --config jest.config.cjs --testPathPatterns 'cors-config|async-resource-cleanup|generate-scenes|export-dlq-retry-metrics|export-metrics-collector|export-retry-lifecycle|use-framework-pipeline|health-check-service|http-metrics-collector|production-error-handler|nullable-access-null-guard|cost-efficiency-metrics|performance-baseline|pipeline-error-recovery-integration|pipeline-health-score|error-recovery-monitor|pipeline-run-recovery-tracker|quality-module-typed-errors|iteration-logger|report-corruption-frozen|report-corruption-toctou'` — 35 suites / 826 tests GREEN（nullable-access 31/31 含む）
- `npx jest --testPathPatterns 'guards/'` — 76 suites / 3226 tests GREEN
- `node_modules/.bin/tsc -p tsconfig.app.json --noEmit` — 0 error
- `npm run specs:mirror:check` — exit 0

## commit 形

- `refactor(tests): REQ-357 義務C第1ゲート tests/unit exact-0 — 残存21ファイル61ノード一括撲滅 (Phase 165 / TASK-0251 / MW-033・specs 同梱)` — 単一 commit（test 置換 + census pin + ledger + specs 一式）
