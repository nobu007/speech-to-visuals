# TASK-0263: 4-row mutant ledger template 形状契約 guard 新設（REQ-381・TC-365-01・MW-045）

**要件**: [requirements.md](../requirements.md) REQ-381 / **受け入れ基準**: [acceptance-criteria.md](../acceptance-criteria.md) TC-365-01（本タスクで [x] 化 — 723/723）
**種別**: DIRECT (guard 新設 + ledger dogfood + 監査 pin 更新)　**推定工数**: 2h　**信頼性**: 🟡 *REQ-381 は提案ベース要件（Phase 175 仕様化）。appendix section 自体は Phase 175 spec commit `f019d4dd` で台帳末尾に導入済みで、本 TASK は機械 audit（shape guard）の欠落を閉じる*

## 背景

Phase 175（REQ-381 仕様化）時点で appendix section（`## 4-row mutant ledger template（再利用可能付録）`・5 列 `ID / mutant / RED-count / RED-test-name / restoration`）は `specs/speech-to-visuals/mutation-witness-ledger.md` 末尾に導入済み（MW-001 + MW-038〜043 の demonstrator row 7 件・MW-044 は entry 末尾に inline row）。しかし TC-365-01 が要求する機械担保 `tests/guards/mutation-witness-ledger-shape.test.ts` が未実装で、同 criterion が**最後の未達 checkbox（722/723）**として残っていた。台帳の appendix が drift しても（見出し変更・行削除・列欠損・新規エントリの row 忘れ）何も RED にならない状態だった。

## 修正

### 新設 `tests/guards/mutation-witness-ledger-shape.test.ts`（19 tests）

REQ-381 / TC-365-01 の契約を 7 面で検証:

1. **付録が台帳末尾の正確な見出し** — `indexOf(APPENDIX_HEADING)` が file 内最後の `## ` 見出し行と一致
2. **5 列 header 行** — `| ID | mutant | RED-count | RED-test-name… | restoration |`
3. **demonstrator row floor ≥7** — appendix body から demonstrator 行が消えない anti-erasure pin
4. **`grep -cE '^\| MW-0'` count >= `LEDGER.length`** — criterion 記載コマンドの機械チェック（aggregate floor）
5. **it.each 件数同期** — row parser は grep 対象行（`^\| MW-0`）と 1:1 で drop なし。不正形状行は sweep から消えず it.each で RED
6. **MW-043+ エントリの row 必須** — `LEDGER` = 付録導入（Phase 175）後エントリの自動派生集合（`LEGACY_BASELINE = 42` 超）。narrative-only の新規エントリ追加は per-entry it.each が throw で検出（「自動 append」の機械強制）。**設計決定**: RED 数値は著者の mutation run 由来で generator が発明できる情報ではないため、検出強制のみ（自動生成はしない）
7. **phantom row 排除** — 全 row ID が実 `## MW-NNN` entry に対応

**設計決定**: legacy MW-001〜042 の正規化は段階的・可逆（free-form narrative 保持）のまま許容（MW-002〜037 は row 無しで GREEN）。requirements.md REQ-381 設計決定の「42 全件を template に書換え + PINNED ≥42」版は、台帳本文（Phase 177 で commit 済み）の「可逆・段階的正規化を許容」契約に置換（TC-365-01 の「リファクタ**可能**であること」文言に整合）。

### 台帳 dogfood: MW-045 entry + appendix row

- MW-045 entry（本 guard の teeth 記録）+ appendix row を同梱 — 契約の最初の dogfood
- `tests/guards/mutation-witness-ledger.test.ts` 監査 pin **PINNED_MIN_ENTRIES ≥44 → ≥45**（parity test leg 2 の `>= PINNED_REGISTRY + 1`（44）は 45 で自然通過・変更不要）

## テスト

- `tests/guards/mutation-witness-ledger-shape.test.ts` 新設 19 tests（内 it.each = LEDGER 3 + rows 9）
- `tests/guards/mutation-witness-ledger.test.ts` pin 45（MW-045 entry の必須 field・target 実在も監査対象に追加）
- 影響 6 suites / 141 tests GREEN（shape + audit + parity + census + pre-fold + zero-fallback）・`tsc -p tsconfig.app.json` 0 error・`tsc -p tsconfig.test.json` 0 error・`npm run specs:mirror:check` exit 0

## MW-045 mutation（3 独立 RED 実測）

- (a) appendix 見出しを `## mutant ledger template（付録）` に縮約 → **2 failed / 17 passed / 19 total**（末尾位置 leg + demonstrator floor leg）
- (b) MW-044 entry 末尾の template 行 1 行削除 → **1 failed / 17 passed / 18 total**（`MW-044 has no template row — append one before committing` throw。aggregate floor は 8 >= 3 で緩い契約どおり GREEN のまま — per-entry leg が精密検出面）
- (c) appendix MW-043 行の第 5 cell（restoration）除去 → **2 failed / 17 passed / 19 total**（per-entry + row-shape leg が `Expected length: 5 / Received length: 4`）
- 各 revert（perl 逆置換）で ledger 2 suites / 112 tests GREEN 復元

**GOTCHA**: mutant (c) の適用に `\s*$` anchor を使うと `\s*` が行末 `\n` を食って次行と merge する（`-p` は置換結果の行跨ぎを防がない）。行末 anchor は `$`（改行直前一致）までに留め、適用後は awk で cell 数を検査して merge 事故を検出。
