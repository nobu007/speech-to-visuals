# TASK-0163: Pipeline Full E2E Recovery Integration Test


<!-- spine:anchor:begin -->
> **Spine anchor**: [speech-to-visuals アーキテクチャ設計](../architecture.md)
>
> - parent: `speech-to-visuals/architecture.md`
> - status: `canonical_child`
<!-- spine:anchor:end -->

**タスクID**: TASK-0163
**タスクタイプ**: TDD
**推定工数**: 16時間
**フェーズ**: Phase 58 - リカバリ検証ループ・CI統合
**信頼性レベル**: 🔵 *AI Hub フィードバック「close the verification loop」・REQ-149 既存テストパターン参照*

## 関連文書

- **概要**: [📋 overview.md](overview.md)
- **要件定義**: [📋 requirements.md](../requirements.md) - REQ-149
- **設計文書**: [📐 architecture.md](../architecture.md)

## タスク概要

PipelineOrchestrator が PipelineErrorRecoveryOrchestrator に正しく移譲することを検証する、フル E2E リカバリ統合テストを追加する。各パイプラインステージ（transcription → analysis → layout → rendering）でのエラー注入→リカバリ→完了の経路を網羅する。Phase 57 で追加された PipelineErrorRecoveryOrchestrator の統合が実際にパイプライン全体で機能することを実証する。

## 依存タスク

- **前提タスク**: [TASK-0162: Pipeline Recovery CI Smoke Test](TASK-0162.md)
- **後続タスク**: [TASK-0165: Phase 57-58 Documentation Update](TASK-0165.md)

## 完了条件

- [x] 統合テストファイル `tests/integration/pipeline-orchestrator-recovery.test.ts` が存在する
- [x] 各ステージでのエラー注入→リカバリ成功のテスト（4ステージ分）
- [x] PipelineOrchestrator → PipelineErrorRecoveryOrchestrator 移譲の検証
- [x] 連鎖フェイルシナリオ（複数ステージ同時フェイル）のテスト
- [x] リカバリメトリクス（attempts, duration, fallback count）の検証
- [x] 既存テストへの回帰なし
- [x] npm test 全テスト通過 <!-- Verified 2026-05-24: 12/12 E2E tests pass -->

---

## 主要実装項目

### 1. Full E2E Recovery Integration Test 作成 🔵

**信頼性**: 🔵 *REQ-149・pipeline-recovery-e2e.test.ts パターン・PipelineOrchestrator 実装参照*

PipelineOrchestrator の stageExecution が PipelineErrorRecoveryOrchestrator.executeStage() に正しく移譲することを検証する統合テスト。

**テストファイル**: `tests/integration/pipeline-orchestrator-recovery.test.ts`

```typescript
// テスト構成イメージ
describe('PipelineOrchestrator Full E2E Recovery', () => {
  it('recovers from transcription stage failure', async () => {
    // Whisper モックがフェイル → リカバリ → 成功
  });
  it('recovers from analysis stage failure', async () => {
    // Gemini モックがフェイル → フォールバック → 成功
  });
  it('recovers from layout stage failure', async () => {
    // LayoutEngine モックがフェイル → リトライ → 成功
  });
  it('recovers from rendering stage failure', async () => {
    // VideoGenerator モックがフェイル → リカバリ → 成功
  });
});
```

### 2. 連鎖フェイルシナリオテスト 🟡

**信頼性**: 🟡 *PipelineErrorRecoveryOrchestrator の設計から妥当な推測*

複数ステージが連続してフェイルするシナリオ。オーケストレーターの shouldAbort() ロジックと degradation tracking を検証。

### 3. リカバリメトリクス検証 🔵

**信頼性**: 🔵 *pipeline-recovery-e2e.test.ts の metrics テスト参照*

- executeStage() の結果に attempts, duration, fallback path が正しく記録される
- finalizeRun() の recovery report に全ステージの結果が集約される
- Health assessment が実行後の状態を正しく反映

---

## 基本的なテスト要件

### テストケース1: Per-Stage Recovery 🔵

**信頼性**: 🔵 *REQ-149・PipelineOrchestrator stage execution ロジック参照*

- **Given**: PipelineOrchestrator がリカバリオーケストレーター付きで初期化
- **When**: 特定ステージでフェイルを注入
- **Then**: リカバリパスが実行され、パイプラインが完了

### テストケース2: Multi-Stage Cascade 🟡

**信頼性**: 🟡 *PipelineErrorRecoveryOrchestrator 設計から推測*

- **Given**: 複数ステージが連続フェイル
- **When**: パイプラインを実行
- **Then**: shouldAbort() が適切に閾値判定し、結果が degraded フラグ付きで返る

### テストケース3: Recovery Metrics Accuracy 🔵

**信頼性**: 🔵 *pipeline-recovery-e2e.test.ts metrics テスト参照*

- **Given**: リカバリ付きパイプライン実行後
- **When**: finalizeRun() の recovery report を取得
- **Then**: attempts, duration, fallback count が正確

---

## 実装手順

### TDDタスクの場合

1. `/tsumiki:tdd-requirements TASK-0163` - 詳細要件定義
2. `/tsumiki:tdd-testcases` - テストケース作成
3. `/tsumiki:tdd-red` - テスト実装（失敗）
4. `/tsumiki:tdd-green` - 最小実装
5. `/tsumiki:tdd-refactor` - リファクタリング
6. `/tsumiki:tdd-verify-complete` - 品質確認

---

## 注意事項

- モックは PipelineOrchestrator の実際の依存関係（Whisper, Gemini, LayoutEngine, VideoGenerator）を使用
- リカバリオーケストレーターの strategy chain と boundary fallback 両方をカバー
- テスト実行時間は30秒以内を目標

---

## 信頼性レベルサマリー

- **総項目数**: 6項目
- 🔵 **青信号**: 4項目 (67%)
- 🟡 **黄信号**: 2項目 (33%)
- 🔴 **赤信号**: 0項目 (0%)

**品質評価**: ✅ 高品質 - 既存テストパターンとオーケストレーター実装に基づく。連鎖フェイルシナリオは設計から推測。
