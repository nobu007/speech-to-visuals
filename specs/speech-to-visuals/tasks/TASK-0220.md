# TASK-0220: critical-path guard real fix + mutation witness pair (async-setState-after-unmount)


<!-- spine:anchor:begin -->
> **Spine anchor**: [speech-to-visuals アーキテクチャ設計](../architecture.md)
>
> - parent: `speech-to-visuals/architecture.md`
> - role: `detailed`
> - status: `canonical_child`
<!-- spine:anchor:end -->

**タスクID**: TASK-0220
**タスクタイプ**: TDD
**推定工数**: 6時間
**フェーズ**: Phase 132 - Steering feedback A1 grounding
**信頼性レベル**: 🔵 *直近 AI_HUB_MAKE_RUN_FEEDBACK「critical-path guard から real fix 1 ペア」*

## 関連文書

- **概要**: [📋 overview.md](overview.md)
- **要件定義**: [📋 requirements.md](../requirements.md) - REQ-300 拡張
- **設計文書**: [📐 architecture.md](../architecture.md)
- **直近関連タスク**: TASK-0219 (positive-case fixture)、Phase 130 (REQ-297 stale-closure GUARDED-STRUCTURAL)

## タスク概要

直近 AI_HUB_MAKE_RUN_FEEDBACK が「critical-path な guard の production code 側 violating instance を 1 件 real fix + test ペアで処理する」ことを推奨している。本タスクは：

- critical-path guard として **`async-setState-after-unmount`** を選択（直近 memory の "🔴 MISSED-SIBLING-SITE (CORE LESSON)" 系 / "post-await React-state stale-closure CLOSED-STRUCTURAL" 系の live 候補）
- src/ から 1 件の production violating instance を特定（unmount 後の setState 呼び出しが cleanup なしに残るサイト）
- real fix: TASK-0219 で抽出した `useEffectWithUnmountGuard` 経由に置換、または mountedRef パターンの導入
- mutation witness: fix を revert → 既存 TC (`async-state-stale-closure-guard.test.ts`) が RED になる → 再 fix で GREEN に戻る

end-to-end の verify により「guard machinery と guard 対象 code の健全性が同時に証明」される（steering feedback の中核命題）。

## 依存タスク

- **前提タスク**: TASK-0219（`useEffectWithUnmountGuard` hook 抽出 + positive-case fixture）
- **後続タスク**: なし（Phase 132 完了確認）

## 完了条件

- [ ] src/ から critical-path violating instance を 1 件特定（unmount 後に setState が reachable なサイト）
- [ ] 当該サイトを `useEffectWithUnmountGuard` 経由または mountedRef パターンで real fix
- [ ] mutation witness テスト追加: real fix を一時的に revert → TC RED → 再 fix で GREEN
- [ ] TypeScript エラー 0 件
- [ ] 既存テスト全通過（no regression、特に `async-state-stale-closure-guard.test.ts`）

---

## 実装詳細

### 1. violating instance 特定 🔵

**信頼性**: 🔵 *直近 memory の "post-await React-state stale-closure CLOSED-STRUCTURAL" 系*

**調査手順**:
- `src/components/` および `src/hooks/` 配下で `useEffect` 内に `setState` / `setX(` 呼び出しがあり、cleanup が `mountedRef.current = false` を含まないサイトを grep
- 特に async 関数内で await 後 setState するパターン（既知の CLOSED-STRUCTURAL 系 live 候補）
- 1 件選定（steering 推奨「1 ペア」に従う）

### 2. real fix (useEffectWithUnmountGuard 経由置換) 🔵

**信頼性**: 🔵 *TASK-0219 hook 抽出済み*

- 該当コンポーネント / hook を `useEffectWithUnmountGuard` 経由に置換
- または mountedRef.current パターンを手動で導入（TASK-0219 hook が不適合な場合）
- docstring で「unmount 後の setState は noop + dev warn」仕様を明記

### 3. mutation witness 🔵

**信頼性**: � *steering feedback A1 grounding*

**テストファイル**: `tests/guards/async-state-stale-closure-real-fix-witness.test.ts`（新規）

```typescript
describe('TASK-0220 real-fix witness', () => {
  it('RED when guard bypassed, GREEN when restored', async () => {
    // 1. 該当コンポーネントを mount → unmount → setState 呼出
    // 2. mountedRef.current = true 固定 (bypass)
    // 3. console.warn が出ない → RED
    // 4. bypass 解除 → GREEN
  });
});
```

---

## 単体テスト要件

### テストケース1: real fix RED→GREEN サイクル 🔵

**信頼性**: 🔵 *steering feedback A1 grounding*

**Given**: TASK-0220 で fix されたコンポーネント
**When**: mutation witness で fix を一時的に bypass
**Then**: RED（console.warn が出ない、または stale state がリーク）
**And**: bypass 解除で GREEN

### テストケース2: no regression �

**信頼性**: 🔵 *REQ-297 基盤*

**Given**: Phase 130 で GUARDED-STRUCTURAL 化された guard テスト
**When**: TASK-0220 の fix を適用
**Then**: 全テスト pass、RED site = 0

---

## 信頼性レベルサマリー

| カテゴリ | 🔵 青 | 🟡 黄 | 🔴 赤 | 合計 |
|---------|-------|-------|-------|------|
| 実装詳細 | 3 | 0 | 0 | 3 |
| 単体テスト | 2 | 0 | 0 | 2 |

**品質評価**: 高品質（steering feedback A1 中核命題の grounding）
