# TASK-0274: 未populate契約（optional metric の producer ゼロ）census guard 新設 + 4 dead leg 一括撲滅（REQ-392・TC-376-01〜04・MW-056）

<!-- spine:anchor:begin -->
> **Spine anchor**: [speech-to-visuals アーキテクチャ設計](../architecture.md)
>
> - parent: `speech-to-visuals/architecture.md`
> - role: `detailed`
> - status: `canonical_child`
<!-- spine:anchor:end -->

**要件**: [requirements.md](../requirements.md) REQ-392 / **受け入れ基準**: [acceptance-criteria.md](../acceptance-criteria.md) TC-376-01〜04（本タスクで [x] 化）
**種別**: TDD (census → 撲滅 → mutation 検証)　**推定工数**: 3h　**信頼性**: 🔵 *steering が直接要求した audit-pass-first の契約側（「optional metrics の未producer契約」）。REQ-389 の設計決定 (2) 撤回という設計判断を含むが、撤回の根拠（hatch は prose では producer 同時 ship を保証できず census なら機械的に保証する）は REQ-392 本文に明記*

## 背景

REQ-383〜391 まで 9 REQ が「凍結定数・未 populate contract が実測を偽装する」class を撲滅してきたが、値の凍結側（bare literal / `Math.random()`）は REQ-391 で census 化済みに対し、**契約側**（測定を約束する `field?: number` 宣言自体に producer がゼロ）は手動発見のまま残っていた。steering は「optional metrics の未producer契約」を明示的に enum 対象として指示。

手動 enumeration（repo src/ + @stv-core core-four・31 optional numeric measurement 宣言）で 4 dead leg を確定:

1. `ExtendedPipelineMetrics.entityExtractionF1Score` / `relationAccuracy` — "if ground truth is available" pair。producer ゼロ（test のみが明示 set）。REQ-389 は measured branch を canonical estimator に委譲したが「field は実 ground truth 専用に維持」(設計決定 2) としたため hatch は恒久 dormant のままだった
2. pipeline `QualityMetrics.cacheHitRate`（0-1）— 全 writer は LLMStats/RTPM 形状に feed し、pipeline monitor への recordMetrics caller は存在しない。cache-warming recommendation と improvement-detector 'Caching Efficiency' opportunity の 2 consumer は実 run で発火不能。live channel は llm-service 実測 cache stats → RTPM `s.llm.cacheHitRate`（HealthCheckService + adaptive gates が消費）で、本 leg は never-wired duplicate
3. `intelligent-cache.currentSize` — "alias for totalEntries" の decorative alias を `getStats` は一度も書かない。health snapshot の `stats.currentSize ?? stats.totalEntries` は左 operand が恒久 undefined

## 実装

1. **census guard 新設** — `tests/guards/optional-metric-producer-census.test.ts`: `walkProductionSurface()`（REQ-391 と同一）上で `field?: number` 宣言 × measurement token（REQ-391 語彙 + f1/completeness）− config token を discovery し、producer predicate（object-literal key / `.field =` 代入 / JSX prop・宣言行は除外）で LIVE 判定。ROSTER 32 row = LIVE 27（producer site 引用・@stv-core core-four 3 key 含む）+ INPUT-CONTRACT 1（`PipelineProgress.initialQualityScore` — SSR/test 用 presentational prop・producer 出現で LIVE 昇格義務）+ ERADICATED 4。8 test: authority / completeness / stale-row / **LIVE-keeps-producer**（測定契約の dark 化 RED）/ INPUT-CONTRACT / eradicated-reappear / reason 衛生 / negative anchors（`src/quality/quality-monitor.ts` に `entityExtractionF1Score|hasEntity|hasRelation|relationAccuracy`・pipeline 両 file に `cacheHitRate`・intelligent-cache に `currentSize` の code 行 ban）。NAME-level 粒度の上限（writer が feed する interface は見えない）は header に正直 doc 化し interface-level dead leg は ERADICATED ledger + anchors で保持
2. **dead leg 撲滅** — (a) 2 field + `assessLLMExtractionQuality` measured branch を削除し single-path canonical-estimator 委譲へ（REQ-389 設計決定 (2) を撤回 — 再導入は census が producer 同梱を強制）。`clamp01` import 解除に伴い clamp01-single-source は同 file を SITES から BANNED_INLINE_ONLY へ移行（死んだ import 強制を回避しつつ formula 再-inline ban は維持）(b) `cacheHitRate` field + 2 consumer を削除（polarity registry / dual-scale guard の当該 leg も除去）(c) `currentSize` alias を削除し health snapshot は `stats.totalEntries` 直接参照
3. **test 整理** — 旧 branch を exercise する test（src/pipeline/__tests__ と tests/pipeline / tests/unit/pipeline の同居 twin 群）を撲滅 pin に更新（cache-warming rec / Caching Efficiency opportunity の不在断言・fixture 除去）
4. **Phase 189 の滴りを修復** — measurement-fixture-census が non-null assertion 2 件（`discovered.get(k)!`）を ship し tests-tree exact-0 ratchet（`tests/guards/non-null-assertion-census.test.ts`）を **RED のまま commit していた**ことを本 run の broad batch が発見。`(discovered.get(k) ?? [])` で null-safe 化（本 census file の同 shape 2 site も同様）。滴りが guard 自体に残るという steering 懸念の実例
5. **MW-056 mutation 検証（3 独立 RED 実測）** — (a) `entityExtractionF1Score?: number;` 再注入 → **1 failed**（eradicated-reappear + completeness 二重捕捉）(b) cache-warming branch 再注入 → **1 failed**（negative anchors `quality-monitor.ts:478`）(c) SimplePipeline `layoutConfidence: lr.confidence` producer 削除 → **1 failed**（LIVE-keeps-producer `Received: ["layoutConfidence"]`）。各 revert で 8/8 GREEN

## 検証

- census guard 8/8 GREEN・広域 153 suites / 5290 tests GREEN（tests-tree exact-0 ratchet 修復込み・src/quality|src/pipeline/__tests__ quality trio|tests/unit/quality|tests/unit/pipeline quality trio|tests/unit/monitoring|tests/guards）
- tsc 両 config（app/test）exit 0
- 台帳 dogfood: MW-056 entry + appendix row・監査 pin **≥55 → ≥56**・TC-373-04 に REQ-392 移行注記
