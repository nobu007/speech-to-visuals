# TASK-0233: src/analysis non-null assertion 撲滅 + census ratchet 縮小

**要件**: [requirements.md](../requirements.md) REQ-335 / **受け入れ基準**: [acceptance-criteria.md](../acceptance-criteria.md) TC-331
**種別**: DIRECT (実装 + guard)　**推定工数**: 4h　**信頼性**: 🔵 *AI Hub steering（REQ-328『TASK-0226 以降に ratchet TASK を追加』の継続第6弾）+ census 実測*

## 背景

Phase 144（REQ-333）で src/export 10→0・Phase 145（REQ-334）で src/monitoring 7→0 にした継続。Phase 145 終了時点（2026-08-20 実測）の残 src 分布: **src/analysis 6**（最大バケット・llm-service 1 行・scene-segmenter 5 行）・framework 5・api 4・test 4・components 3・quality 3・remotion 2・main.tsx 1・pages 1・workers 1 = 計 30。解析層は LLM クライアントの optional 初期化とセグメント集約ループという内部境界から `!` を排除し strict mode の実検証範囲を拡大する。

## 実施内容（2026-08-20 完了）

**src/analysis 6 件 → 0 件**（2 ファイル・挙動保存置換 4 パターン）:

1. **fail-loud captured guard**（1 サイト）: llm-service `executeRequest` の `this.genAI!.getGenerativeModel(...)` → `const genAI = this.genAI; if (genAI === undefined) throw new LLMResponseError('LLM service not enabled (API key missing or disabled)')`。`genAI?` は ctor が API key 存在時のみ代入する optional。**`execute()` は全経路で `isEnabled()` = `Boolean(this.genAI)` のゲートを通ってからこの private メソッドを呼ぶため undefined 分岐は公開 API 経由で到達不能**（テストも `new LLMService('test-key')` + `@google/generative-ai` mock だけで到達 — A144 教訓の mock 到達可能性 grep を実施・該当なし）。到達不能でも旧 `!` は `undefined.getGenerativeModel` の bare TypeError になるため、ゲート自身のメッセージで fail-loud に正典化（Phase 144 `requireSceneId` と同型）
2. **narrowing `else if` + const capture**（3 サイト・scene-segmenter `currentSegment!.endMs/texts.push/keyphrases.add`）: 拡張分岐は `shouldStartNew = !currentSegment || hasKeywordTopicShift || isOverMax` が false の場合のみ実行されるため **null の currentSegment は `!currentSegment` 経由で shouldStartNew を true にする — else 到達時点で非 null が自明**。boolean 中介変数が TS の narrowing を隠すため `else if (currentSegment)` で明示化し、forEach closure 内で `let` の narrowing が失効する問題は `const segment = currentSegment` 捕捉で構造解（Phase 142 const capture と同型）
3. **captured `get()` compare**（1 サイト・`cosineSimilarity` の `if (b.has(key)) { dot += val * b.get(key)!; }`）→ `const bVal = b.get(key); if (bVal !== undefined) { dot += val * bVal; }`。当該 private メソッドの呼び出し元は両方 `buildTopicVector` 出力で値は常に number（`(vec.get(kw) || 0) + 1`）— undefined 値を格納しない Map では `has(key)` ⟺ `get(key) !== undefined` が厳密に成立するため完全等価
4. **`pop()` + unreachable-undefined `break`**（1 サイト・`mergeShortSegments` の `const prev = result.pop()!`）→ `const prev = result.pop(); if (prev === undefined) break;`。while 条件が `result.length > 0` を要求するため pop() が undefined を返す経路は存在しない — assertion を loop contract の明示形に置換（Phase 141 `queue.shift()`+break と同型）

**補正**: なし — source-anchor guard（scene-segmenter sanitizeFinite ×3 系・SENTENCE_BOUNDARY_REGEX・llm-service retry defaults）は全て編集箇所と無関係で陳腐化なし（事前 grep で確認）。

**guard 更新** `tests/guards/non-null-assertion-census.test.ts`:

- `src/analysis (production): 0` exact pin を追加
- src ratchet **30 → 24**（30 − 6）に縮小
- mutant（scene-segmenter.ts:939 `const prev = result.pop();` → `const prev = result.pop()!;`）で **analysis exact pin + src ratchet の 2 tests RED** を実測 → **MW-011** として台帳化・ledger 監査 pin ≥10 → ≥11

## 検証

- 置換後: analysis+guards pattern = **135 suites / 7057 tests GREEN**（52.9s）
- `node_modules/.bin/tsc -p tsconfig.app.json --noEmit` exit=0
- census 実測: src/analysis = **0**・src = **24**
- census guard 9 tests GREEN・MW-011 mutant RED 検証（2 failed: analysis exact pin + `Expected: <= 24 / Received: 25` → revert 後 9 passed）
- ledger 監査 guard: pin ≥11 で GREEN（census 9 + ledger 24 = 33 tests）
- フルスイート: A146 の [EVIDENCE] 行（interview-record.md）に出典

## 完了条件

- [x] src/analysis 本体の postfix `!` = 0（census exact pin）
- [x] src ratchet 30 → 24 縮小
- [x] 置換後 analysis 系 suite GREEN・tsc exit=0
- [x] MW-011 台帳エントリ + 監査 pin 更新
