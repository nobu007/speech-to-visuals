# TASK-0229: src/pipeline non-null assertion 撲滅 + census ratchet 縮小

<!-- spine:anchor:begin -->
> **Spine anchor**: [speech-to-visuals アーキテクチャ設計](../architecture.md)
>
> - parent: `speech-to-visuals/architecture.md`
> - role: `detailed`
> - status: `canonical_child`
<!-- spine:anchor:end -->

**要件**: [requirements.md](../requirements.md) REQ-331 / **受け入れ基準**: [acceptance-criteria.md](../acceptance-criteria.md) TC-327
**種別**: DIRECT (実装 + guard)　**推定工数**: 4h　**信頼性**: 🔵 *AI Hub steering（REQ-328『TASK-0226 以降に ratchet TASK を追加』の継続）+ grep 実測*

## 背景

Phase 141（REQ-328）で src/visualization の `!` を 67→0 にし全ツリー census ratchet（src ≤93 / tests ≤960）を新設した。steering bullet 1 は「src と主要テストパスの残 `!` を census し」と続きを要求する。2026-08-20 実測の残分布: **src/pipeline 29**（最大・オーケストレーション核心）・src/transcription 17・src/export 10・他 38。

## 実施内容（2026-08-20 完了）

**src/pipeline 29 件 → 0 件**（9 ファイル・挙動保存置換 6 パターン）:

1. **const capture（closure 内 narrowing 変効の回避）**: pipeline-orchestrator.ts — stage4 で `scenes = stage4.result as SceneGraph[]` 直後に `const preparedScenes = scenes;` を捕捉し、`timeStage`/`executeStageWithGates` の closure と return（`scenes: scenes!`・`scenes!.reduce`）4 サイトを置換。単一代入のため値は同一
2. **fail-loud accessor**: framework-integrated-pipeline.ts — `requireIterationManager()` を新設（seed 済みなので throw は到達不能・`execute()` の init guard は温存）。`this.iterationManager!` 7 サイト + executeWithImprovement catch 内 1 サイト
3. **get-or-create Map 分岐**: main-pipeline.ts trackStagePerformance — `has → set([]) → get()!.push` を `get → push / set([duration])` に（同じ配列参照への push）
4. **`Number()` NaN 保存算術**: video-generator.ts（`endTime! - startTime!` → `Number(endTime) - Number(startTime)`・`startTime! * 1000` → `Number(startTime) * 1000`）・pipeline-orchestrator.ts:740（`startTime! + …`）。`Number(undefined)` = NaN で旧 `!` 算術と全状態で同一
5. **検証器境界の正規化**: video-generator.ts — `id: scene.id ?? ''`（`''` は falsy のまま validateRemotionData `!scene.id` が同一 ERROR）・`from/to` の `?? ''`・`label! || ''` → `label || ''`（label と対称）・`transcript: scene.content ?? ''`・`generateSceneTitle` の `(scene.content ?? '').substring`（旧は undefined で TypeError crash）
6. **guard 前置き narrowing**: quality-estimators.ts 3 関数（`const scenes = result.scenes; if (!success || !scenes || scenes.length === 0)` ≡ 旧 `(scenes?.length ?? 0) === 0`）・simple-pipeline.ts（`success === true`・`getLatestMetrics()` 直前の `recordMetrics()` 無条件 push を出典に named throw で narrow ×2）・bottleneck-detector.ts（`worstBottleneck` 三項 narrowing = `hasBottleneck` と同値）

**guard 更新** `tests/guards/non-null-assertion-census.test.ts`:

- `src/pipeline (production): 0` exact pin を追加
- src ratchet **93 → 64**（93 − 29）に縮小
- mutant（quality-estimators.ts:57 `scenes.length` → `(scenes as unknown as { length: number })!.length`）で **pipeline exact pin + src ratchet の 2 tests RED** を実測 → **MW-007** として台帳化・ledger 監査 pin ≥6 → ≥7

## 検証

- 置換前 baseline: `src/pipeline|tests/guards/non-null` = 38 suites / 657 tests GREEN
- 置換後: `pipeline|tests/guards|acceptance|nullable-access` = **201 suites / 5479 tests GREEN**（70.4s）
- `node_modules/.bin/tsc -p tsconfig.app.json --noEmit` exit=0
- census 実測: src/pipeline = **0**・src = **64**
- census guard 5 tests GREEN・MW-007 mutant RED 検証（revert 後 GREEN）
- ledger 監査 guard: pin ≥7 で GREEN
- full-suite（commit 482650f4・clean run）: `[EVIDENCE] started=2026-08-20T00:46:13+09:00 ended=2026-08-20T00:48:54+09:00 exit=0 elapsed_s=160.13 cmd=npm test commit=482650f4 branch=ai/instruction-speech-to-visuals-20260819-152424-574374` → **742 suites / 23,131 passed / 0 failed / 17 skipped**

## 完了条件

- [x] src/pipeline 本体の postfix `!` = 0（census exact pin）
- [x] src ratchet 93 → 64 縮小
- [x] 置換後 pipeline + guards + acceptance suite 全 GREEN・tsc exit=0
- [x] MW-007 台帳エントリ + 監査 pin 更新
