# TASK-0162: Pipeline Recovery CI Smoke Test


<!-- spine:anchor:begin -->
> **Spine anchor**: [speech-to-visuals アーキテクチャ設計](../architecture.md)
>
> - parent: `speech-to-visuals/architecture.md`
> - role: `detailed`
> - status: `canonical_child`
<!-- spine:anchor:end -->

**タスクID**: TASK-0162
**タスクタイプ**: TDD
**推定工数**: 8時間
**フェーズ**: Phase 58 - リカバリ検証ループ・CI統合
**信頼性レベル**: 🔵 *AI Hub フィードバック「wire the recovery orchestrator into end-to-end pipeline tests or CI to close the verification loop」に基づく*

## 関連文書

- **概要**: [📋 overview.md](overview.md)
- **要件定義**: [📋 requirements.md](../requirements.md) - REQ-149
- **設計文書**: [📐 architecture.md](../architecture.md)

## タスク概要

PipelineErrorRecoveryOrchestrator の CI 煙テストを追加し、リカバリ検証ループを閉じる。CI パイプライン (.github/workflows/ci.yml) の test ジョブに、リカバリオーケストレーターが基本的なフェイル→リカバリシナリオを処理できることを検証するスモークテストを追加する。AI Hub フィードバックで要請された「CI でリカバリが機能することの確認」を満たす。

## 依存タスク

- **前提タスク**: [TASK-0161: LLMキャッシュデバウンステスト追加](TASK-0161.md)
- **後続タスク**: [TASK-0163: Pipeline Full E2E Recovery Integration Test](TASK-0163.md), [TASK-0165: Phase 57-58 Documentation Update](TASK-0165.md)

## 完了条件

- [x] 煙テストファイル `tests/ci/recovery-smoke.test.ts` が存在する
- [x] PipelineErrorRecoveryOrchestrator の基本的なフェイル→リカバリシナリオを検証
- [x] 実行時間3秒以内で完了（CI の高速フィードバック要件）
- [x] CI 設定ファイル (.github/workflows/ci.yml) の test ジョブで実行される
- [x] 既存テストへの回帰なし
- [x] npm test 全テスト通過 <!-- Verified 2026-05-24: 3/3 smoke tests pass, TypeScript 0 errors, ESLint 0 errors -->

---

## 主要実装項目

### 1. CI Recovery Smoke Test 作成 🔵

**信頼性**: 🔵 *AI Hub フィードバック・既存 pipeline-recovery-e2e.test.ts パターン参照*

PipelineErrorRecoveryOrchestrator を直接インスタンス化し、最小限のフェイル→リカバリシナリオを実行する高速テスト。

**テストファイル**: `tests/ci/recovery-smoke.test.ts`

```typescript
// テスト構成イメージ
describe('CI Recovery Smoke Test', () => {
  it('recovers from a single stage failure', async () => {
    // 1. オーケストレーター初期化
    // 2. 意図的なフェイルを注入
    // 3. リカバリ成功を確認
    // 4. 実行時間3秒以内
  });
});
```

### 2. CI 設定ファイル更新 🔵

**信頼性**: 🔵 *.github/workflows/ci.yml 既存構成参照*

CI の test ジョブに煙テストパスを追加。既存の `npm test -- --coverage` で自動的に含まれるよう、testMatch パターンに `tests/ci/` を含めるか、明示的にテストパスを追加。

**実装ファイル**: `.github/workflows/ci.yml`

### 3. 実行時間最適化 🔵

**信頼性**: 🔵 *CI 高速フィードバック要件*

テストの実行時間を3秒以内に抑えるため、モックを活用して重い依存関係を排除。

---

## 基本的なテスト要件

### テストケース1: Basic Recovery Verification 🔵

**信頼性**: 🔵 *REQ-149・pipeline-recovery-e2e.test.ts パターン*

- **Given**: PipelineErrorRecoveryOrchestrator が初期化済み
- **When**: フェイルするステージを実行
- **Then**: リカバリパスが実行され、最終的に成功結果が返る

### テストケース2: Health Assessment After Recovery 🔵

**信頼性**: 🔵 *pipeline-recovery-e2e.test.ts の health assessment テスト参照*

- **Given**: リカバリが実行された後
- **When**: getHealthAssessment() を呼び出し
- **Then**: degradation=nominal であることを確認

### テストケース3: Execution Time Constraint 🔵

**信頼性**: 🔵 *CI 高速フィードバック要件*

- **Given**: 煙テスト全体
- **When**: テストスイートを実行
- **Then**: 3秒以内に完了

---

## 実装手順

### TDDタスクの場合

1. `/tsumiki:tdd-requirements TASK-0162` - 詳細要件定義
2. `/tsumiki:tdd-testcases` - テストケース作成
3. `/tsumiki:tdd-red` - テスト実装（失敗）
4. `/tsumiki:tdd-green` - 最小実装
5. `/tsumiki:tdd-refactor` - リファクタリング
6. `/tsumiki:tdd-verify-complete` - 品質確認

---

## 注意事項

- CI 環境での安定性を最優先。タイマー・非同期処理は jest.useFakeTimers() で制御
- テストファイルは tests/ci/ ディレクトリに配置（CI 専用テストとして明確化）
- 外部サービス（Supabase, Gemini API 等）への依存なし

---

## 信頼性レベルサマリー

- **総項目数**: 6項目
- 🔵 **青信号**: 6項目 (100%)
- 🟡 **黄信号**: 0項目 (0%)
- 🔴 **赤信号**: 0項目 (0%)

**品質評価**: ✅ 高品質 - AI Hub フィードバックと既存テストパターンに基づく
