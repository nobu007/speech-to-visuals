# TASK-0223: エビデンス runner（[EVIDENCE] 行）の実装と TC-323 pin

**タスクID**: TASK-0223
**タスクタイプ**: TDD
**推定工数**: 4時間
**フェーズ**: Phase 140 - エビデンス出典・収束駆動タスク生成
**信頼性レベル**: 🔵 *AI Hub steering（性能主張は実行出力の出典引用必須）・REQ-326・TC-323*

## 関連文書

- **概要**: [📋 overview.md](overview.md)
- **要件定義**: [📋 requirements.md](../requirements.md) - REQ-326
- **受け入れ基準**: [✅ acceptance-criteria.md](../acceptance-criteria.md) - TC-323-01〜03

## タスク概要

数量・性能主張（実行時間・件数・エラー数等）を commit message の文章ではなく実行出力に出典させるため、任意コマンドを wrap して grep 可能な 1 行のタイムスタンプ付き計測行（`[EVIDENCE]`）を発行する正典ツールを実装する。REQ-326 の計測基盤。

## 依存タスク

- **前提タスク**: なし
- **後続タスク**: [TASK-0224](TASK-0224.md)（strict lock-in の baseline 計測に本ツールを使用）

## 完了条件

- [x] `scripts/collect-evidence.ts` が `[EVIDENCE] started=<local-ISO> ended=<local-ISO> exit=<int> elapsed_s=<小数2桁> [label=] cmd=<shell引用> commit= branch=` 行を発行する
- [x] 子プロセス exit code が行と process exit の両方に伝搬する。spawn 失敗は exit=127（黙示成功しない）
- [x] `npm run evidence -- [--label=x] <command>` で実行可能
- [x] TC-323-01〜03（14 tests）GREEN・mutation RED 2 種検証済み

---

## 主要実装項目

### 1. 純関数ヘルパー 🔵

**信頼性**: 🔵 *TC-323 が形状を pin*

`isoLocalWithOffset`（numeric offset 付き local-ISO）・`shellQuote`/`shellJoin`（shell-copyable 引用・空引数は実在スロットとして `''`）・`formatEvidenceLine`（固定キー順）・`parseEvidenceArgv`（`--label=` と `--` セパレータ・未知オプションと空コマンドは usage エラー）。

**実装ファイル**: `scripts/collect-evidence.ts`

### 2. runner 本体 🔵

**信頼性**: 🔵 *E2E テストが node 子プロセスで実証*

`runEvidenceSync` は stdio inherit/pipe 両対応。elapsed は子プロセスのみを囲む計測。commit/branch は `git rev-parse` から取得（失敗時 `unknown`）。

**実装ファイル**: `scripts/collect-evidence.ts`・`package.json` scripts.evidence

---

## 基本的なテスト要件

### TC-323-01〜03: 行形状・失敗経路・計測範囲 🔵

**信頼性**: 🔵 *実装と同一ラウンドで RED→GREEN（mutation 2 種 RED 検証）*

**テストファイル**: `tests/scripts/collect-evidence.test.ts`（14 tests）

- 行形状: `EVIDENCE_LINE_RE`・キー順・label 無し時のスロット省略・引用が必要な cmd
- 失敗経路: spawn 失敗 exit=127・未知オプション/空コマンド exit 2・子 exit 3 の伝搬
- 計測範囲: 120ms sleep ≥ 0.12s・local-ISO round-trip

---

## 信頼性レベルサマリー

- **総項目数**: 5項目
- 🔵 **青信号**: 5項目 (100%)
- 🟡 **黄信号**: 0項目
- 🔴 **赤信号**: 0項目

**品質評価**: 高品質（steering 出典明示・実装とテスト同一コミット co-locate）
