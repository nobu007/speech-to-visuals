# TASK-0238: tests ツリー non-null assertion ratchet 単調減少ラウンド 4（tests/unit 169 → 103）

<!-- spine:anchor:begin -->
> **Spine anchor**: [speech-to-visuals アーキテクチャ設計](../architecture.md)
>
> - parent: `speech-to-visuals/architecture.md`
> - role: `detailed`
> - status: `canonical_child`
<!-- spine:anchor:end -->

**要件**: [requirements.md](../requirements.md) REQ-341 / **受け入れ基準**: [acceptance-criteria.md](../acceptance-criteria.md) TC-337
**種別**: DIRECT (実装 + guard)　**推定工数**: 3h　**信頼性**: 🔵 *A150 残課題（「tests/unit 残 169 の継続縮小・census 次点: pipeline-quality-monitor 13・real-time-performance-monitor 11・pipeline-orchestrated-recovery-integration 10」）+ REQ-337 の単調減少維持指令 + guard-first per-file census 実測*

## 背景

Phase 150（TASK-0237 / REQ-340）が tests/unit ratchet の減少ラウンド 3（274 → 169）を完了した。本タスクはラウンド 4。対象は Phase 150 と同じ **guard-first survey**（Phase 147 の AST census checker と同一ロジックの per-file 集計・降順ソート・手動列挙なし）上位から機械的に取得した **7 ファイル 66 node**:

- tests/unit/pipeline/pipeline-quality-monitor.test.ts — **13 node**（unit 残 169 の最大ファイル）
- tests/unit/monitoring/real-time-performance-monitor.test.ts — **11 node**
- tests/unit/pipeline/pipeline-orchestrated-recovery-integration.test.ts — **10 node**
- tests/unit/pipeline/bottleneck-detector.test.ts — **8 node**
- tests/unit/pipeline/pipeline-run-recovery-integration.test.ts — **8 node**
- tests/unit/quality/enhanced-error-recovery-extended.test.ts — **8 node**
- tests/unit/quality/recovery-strategy-chain.test.ts — **8 node**

7 ファイルとも同一根本クラス: optional 戻り値（`getLatestMetrics(): QualityMetrics | null`・`getStats(): ChainStats | null`・`worstBottleneck: BottleneckInfo | null`）・optional フィールド（`result.metrics?.recoveryReport`）・`.find()` キャプチャ（violations / trends / cascade chains / progress messages）に対する checker 抑制（TASK-0226 判定の典型）。

## 実施内容（2026-08-20 完了）

### 1. pipeline-quality-monitor.test.ts 13 node → 0（fail-loud `requireDefined`）

`monitor.getLatestMetrics()`（`QualityMetrics | null`）の `latest!.field` 8 node と `report.violations.find(…)` の `violation!.field` 5 node を、label 付き generic helper に置換。直前の `expect(latest).not.toBeNull()` 1 件と `expect(…).toBeDefined()` 3 件は helper の throw が同保証を担うため折りたたみ（Phase 149/150 と同一判断）。

**挙動保存の根拠**: 旧コードの不在時挙動は `undefined.field` TypeError = RED。helper は不在時に label を含む Error を throw するため **同一 verdict（RED）でメッセージが診断可能**。`memViolation` は直後の `!` がないため `toBeDefined()` のまま残存。

### 2. real-time-performance-monitor.test.ts 11 node → 0（fail-loud `requireTrend`）

`monitor.analyzeTrends().find((t: TrendAnalysis) => t.metric === …)` の `trend!.trend` / `trend!.prediction.*` / `trend!.confidence` 11 node を専用 helper に置換:

```ts
function requireTrend(trends: TrendAnalysis[], metric: string): TrendAnalysis {
  const trend = trends.find((t: TrendAnalysis) => t.metric === metric);
  if (trend === undefined) {
    throw new Error(`trend for "${metric}" was not analyzed`);
  }
  return trend;
}
```

直前の `expect(trend).toBeDefined()` 6 件は折りたたみ。`it.each(ANALYZED_METRICS)` の極性契約テスト 2 件（7ae31177 回帰ネット）も同一 helper で置換し verdict 保存。

### 3. pipeline-orchestrated-recovery-integration.test.ts 10 node → 0（fail-loud `requireDefined`）

`result.metrics?.recoveryReport` の `report!.field` 7 node と progress message の `.find(…)` の `x!.field` 3 node を label 付き helper で捕捉。直前の `expect(report).toBeDefined()` 2 件・`expect(…).toBeDefined()` 2 件は折りたたみ。

### 4. bottleneck-detector.test.ts 8 node → 0（fail-loud `requireWorstBottleneck`）

`report.worstBottleneck`（`BottleneckInfo | null`）の `report.worstBottleneck!.field` 8 node を:

```ts
function requireWorstBottleneck(report: BottleneckReport): BottleneckInfo {
  const worst = report.worstBottleneck;
  if (worst === null) {
    throw new Error('worstBottleneck was null (no bottleneck detected)');
  }
  return worst;
}
```

**null-guard 根拠**: `detectBottlenecks` は `bottlenecks.length === 0` のとき `null` を返す（src/pipeline/bottleneck-detector.ts:74）ため、helper は undefined でなく **null** を guard する（Phase 149 `requireJobStatus` と同一）。直前の `expect(report.worstBottleneck).not.toBeNull()` 1 件は折りたたみ。

### 5. pipeline-run-recovery-integration.test.ts 8 node → 0（fail-loud `requireRecoveryReport`・cast 除去込み）

`result.metrics!.recoveryReport as RunRecoveryReport` 7 node と `expect(result.metrics!.recoveryReport)` 1 node を:

```ts
function requireRecoveryReport(result: PipelineResult): RunRecoveryReport {
  const report = result.metrics?.recoveryReport;
  if (report === undefined) {
    throw new Error('recovery report was not produced in pipeline metrics');
  }
  return report;
}
```

**cast 除去の根拠**: 当該 field は `ExtendedPipelineMetrics` で既に `recoveryReport?: RunRecoveryReport` 型（src/pipeline/types.ts:108）。旧 `as RunRecoveryReport` は checker 抑制と併置された冗長 cast であり、`metrics?.` optional-chain + narrowing で正型が得られるため不要。

### 6. enhanced-error-recovery-extended.test.ts 8 node → 0（fail-loud `requireDefined`）

`chains.find(c => c.triggerStage === 'transcription')` の `transChain!.affectedStages` 2 node・`analytics.trends.find(t => t.stage === …)` の `xTrend!.field` 5 node・`pattern!.frequency` 1 node を label 付き helper で捕捉（直前の `toBeDefined()` 6 件は折りたたみ）。

### 7. recovery-strategy-chain.test.ts 8 node → 0（fail-loud `requireStats`）

`chain.getStats(name)`（`ChainStats | null`）の `stats!.field` 8 node を:

```ts
function requireStats(stats: ChainStats | null, chainName: string): ChainStats {
  if (stats === null) {
    throw new Error(`stats for chain "${chainName}" were not collected`);
  }
  return stats;
}
```

**null-guard 根拠**: `getStats` は未登録 chain に `null` を返す（src/quality/recovery-strategy-chain.ts:377）。直前の `expect(stats).not.toBeNull()` 1 件は折りたたみ。

### 8. ratchet pin 更新 + MW-017

census guard（tests/guards/non-null-assertion-census.test.ts）:

- `TESTS_DIR_PINS.unit`: 169 → **103**
- `PINNED['tests (excl. __mocks__)']`: 794 → **728**
- ヘッダに Phase 151 bullet（7 ファイル・helper 5 種）と **MW-017** を追記

**MW-017**（ledger 台帳・監査 pin ≥16 → ≥17）: Phase 151 rewrite への `!` 1 node 再注入 — `expect(latest.processingTime).toBe(5000);` → `expect(latest!.processingTime).toBe(5000);`（pipeline-quality-monitor.test.ts）— で **tests 合計 ratchet（`Expected: <= 728 / Received: 729`）と tests/unit dir ratchet（`Expected: <= 103 / Received: 104`）の 2 tests RED** を実測（`Tests: 2 failed, 9 passed, 11 total`）→ revert で census GREEN・ledger 監査 pin ≥17 GREEN。

**補正**: なし — 編集 10 ファイル（test 7 + guard 3）に source-anchor guard の肯定 pin は存在しない。

## 検証

- census guard **11 tests GREEN**（pin 更新後）・ledger 監査 guard **36 tests GREEN**（pin ≥17）
- 置換対象: **7 suites / 193 tests GREEN**（44+59+14+12+6+36+22）・TC-337 再検証コマンド（census guard 含む）**8 suites / 204 tests GREEN**
- `node_modules/.bin/tsc -p tsconfig.app.json --noEmit` exit=0
- guards 全 suite: **75 suites / 3171 tests GREEN**
- フルスイート（全変更後・初回 run・並走なし）: **742 suites / 742 passed・23,157 passed / 0 failed / 17 skipped（jest Time 136.735s）** — Phase 150 完了時 23,155 から +2（ledger 監査 pin ≥17 化の it.each 増分）。run 履歴は A151 の [EVIDENCE] 行に出典
- MW-017 mutant RED 2 failed → revert → GREEN サイクル実測
- census 実測: tests/unit = **103**・tests 合計 = **728**（14 ディレクトリ）

## 完了条件

- [x] tests/unit pin 169 → 103・tests 合計 pin 794 → 728（guard pin 更新 + GREEN）
- [x] 対象が guard-first survey（census 降順上位 7 ファイル）で機械的に選定されていること
- [x] 置換 7 ファイルの `!` AST node = 0（挙動保存・verdict 保存）
- [x] 対象 7 suite GREEN・tsc tsconfig.app.json exit=0
- [x] MW-017 台帳エントリ + 監査 pin ≥17
