# TASK-0232: src/monitoring non-null assertion 撲滅 + census ratchet 縮小

<!-- spine:anchor:begin -->
> **Spine anchor**: [speech-to-visuals アーキテクチャ設計](../architecture.md)
>
> - parent: `speech-to-visuals/architecture.md`
> - role: `detailed`
> - status: `canonical_child`
<!-- spine:anchor:end -->

**要件**: [requirements.md](../requirements.md) REQ-334 / **受け入れ基準**: [acceptance-criteria.md](../acceptance-criteria.md) TC-330
**種別**: DIRECT (実装 + guard)　**推定工数**: 4h　**信頼性**: 🔵 *AI Hub steering（REQ-328『TASK-0226 以降に ratchet TASK を追加』の継続第5弾）+ census 実測*

## 背景

Phase 143（REQ-332）で src/transcription 17→0・Phase 144（REQ-333）で src/export 10→0 にした継続。Phase 144 終了時点（2026-08-20 実測）の残 src 分布: **src/monitoring 7**（最大バケット・health-check-service 2 行・performance-dashboard 2 行・real-time-performance-monitor 1 行・production-error-handler 1 行・http-metrics-collector 1 行）・analysis 6・framework 5・api 4・test 4・components 3・quality 3・remotion 2・main.tsx 1・pages 1・workers 1 = 計 37。

## 実施内容（2026-08-20 完了）

**src/monitoring 7 件 → 0 件**（5 ファイル・挙動保存置換 3 パターン）:

1. **`?? Number.NaN` NaN 保存**（4 サイト）: health-check-service `bytesToMb(memoryUsage.rss!)`/`bytesToMb(memoryUsage.external!)` と performance-dashboard `memoryUsage.external!`/`memoryUsage.rss!`。`MemoryMetrics`（@stv/core/utils/memory-usage）は rss/external が **optional**（browser 経路の `getMemoryUsage` は両フィールドを省略・Node 経路は常に存在）。旧 `!` は不在時 `undefined` を `bytesToMb` へ直送し `undefined / (1024 * 1024)` は既に NaN → `?? Number.NaN` は同一 outcome を型で語る形に正典化。dashboard 側は sum/avg 集計で NaN 吸収が旧来同一。**`?? 0` は「0 MiB の健康に見える偽計測」を捏造するため非等価**（Phase 143 `?? Number.NaN` しきい値と同一判断）
2. **captured get-or-create**（2 サイト）: real-time-performance-monitor `if (!has) set([]); const history = get()!` 三段と production-error-handler `onError` の同三段 → `let x = map.get(k); if (x === undefined) { x = []; map.set(k, x); }`。不在分岐が格納する配列と同一 instance を get() から捕捉するため事後 presence assertion が不要（Phase 141/142 get-or-create と同型）。onError の unsubscribe 側は既に `get()`−falsy guard の安全形で対称化
3. **provably-dead definite-assignment 除去**: http-metrics-collector `routes!:` → `routes:`（constructor が `new CappedMap(...)` を無条件代入するため strictPropertyInitialization 解析が証明・Phase 144 `byCompoundKey` と同型）

**補正**: source-anchor guard `tests/guards/bytes-to-mb-canon.test.ts` の rss 肯定 pin が旧 `rss!?` 許容形で陳腐化 → `?? Number.NaN` 要求形に更新（Phase 144 site-780 と同一手順・source 変更と同一コミット）。

**guard 更新** `tests/guards/non-null-assertion-census.test.ts`:

- `src/monitoring (production): 0` exact pin を追加
- src ratchet **37 → 30**（37 − 7）に縮小
- mutant（real-time-performance-monitor.ts:209 `let history = this.metrics.get(metric);` → `let history = this.metrics.get(metric)!;`）で **monitoring exact pin + src ratchet の 2 tests RED** を実測 → **MW-010** として台帳化・ledger 監査 pin ≥9 → ≥10

## 検証

- 置換後: monitoring+guards pattern = **45 suites / 1068 tests GREEN**（40.0s）
- `node_modules/.bin/tsc -p tsconfig.app.json --noEmit` exit=0
- census 実測: src/monitoring = **0**・src = **30**
- census guard 8 tests GREEN・MW-010 mutant RED 検証（2 failed: monitoring exact pin + `Expected: <= 30 / Received: 31` → revert 後 8 passed）
- ledger 監査 guard: pin ≥10 で GREEN（35 tests = census 8 + ledger 21 + bytes-to-mb-canon 6）
- フルスイート: A145 の [EVIDENCE] 行（interview-record.md）に出典

## 完了条件

- [x] src/monitoring 本体の postfix `!` = 0（census exact pin）
- [x] src ratchet 37 → 30 縮小
- [x] 置換後 monitoring 系 suite GREEN・tsc exit=0
- [x] MW-010 台帳エントリ + 監査 pin 更新
