# TASK-0231: src/export non-null assertion 撲滅 + census ratchet 縮小

**要件**: [requirements.md](../requirements.md) REQ-333 / **受け入れ基準**: [acceptance-criteria.md](../acceptance-criteria.md) TC-329
**種別**: DIRECT (実装 + guard)　**推定工数**: 4h　**信頼性**: 🔵 *AI Hub steering（REQ-328『TASK-0226 以降に ratchet TASK を追加』の継続第3弾）+ census 実測*

## 背景

Phase 142（REQ-331）で src/pipeline 29→0・Phase 143（REQ-332）で src/transcription 17→0 にした継続。Phase 143 終了時点（2026-08-20 実測）の残 src 分布: **src/export 10**（最大バケット・multi-format-exporter 6 行・security-metrics-collector 1 行・production-exporter 1 行・export-job-queue 1 行・enhanced-export-engine 1 行）・monitoring 7・analysis 6・framework 5・api 4・test 4・components 3・quality 3・remotion 2・main.tsx 1・pages 1・workers 1 = 計 47。

## 実施内容（2026-08-20 完了）

**src/export 10 件 → 0 件**（5 ファイル・挙動保存置換 5 パターン）:

1. **fail-loud accessor `requireSceneId`**（5 サイト）: `sanitizeFilename(scene.id!)` ×4（svg/png/pdf/json filename）+ `escapeXml(scene.id!)`（SVG `<title>`）→ `this.requireSceneId(scene, format)`。`SceneGraph.id` は wire 型では optional だが正典 producer `buildSceneGraph` は常に `scene-${index}` を書く。id 無し scene の旧ランタイムは `sanitizeFilename`/`escapeXml` 内の `undefined.replace` TypeError → `export()` の catch で `{success:false}`。accessor の ExportError も同一 catch 経路で `{success:false}`（メッセージが診断可能に）。**`?? ''` fallback は非等価**（`unnamed.svg` で export が成功してしまう）
2. **`!== undefined` guard**: `hasNonWinAnsiChar` の `ch.codePointAt(0)! > 0xff` → `const cp = ch.codePointAt(0); if (cp !== undefined && cp > 0xff)`。`for…of` は code point 単位で index 0 は常に in-range だが、guard 形は旧 `undefined > 0xff` = false pass-through を保存
3. **provably-dead definite-assignment 除去**: `byCompoundKey!:` → `byCompoundKey:`（constructor が無条件代入するため strictPropertyInitialization 解析が証明・`!` は checker 黙らせのみ）
4. **`Number()` NaN 保存算術**（2 サイト）: production-exporter `job.endTime! - job.startTime!` → `Number(job.endTime) - Number(job.startTime)`（`undefined - x` ≡ `Number(undefined) - x` = NaN を safeMean が除外・Phase 142 video-generator と同型）・export-job-queue `job.startedAt! - job.enqueuedAt` → `Number(job.startedAt) - job.enqueuedAt`（0 捏造なし）
5. **truth-telling pass-through 署名**: enhanced-export-engine `writeOutputFile`/`getFileSize` の引数と戻り値を `string | undefined` にし `job.outputPath!` → `job.outputPath`。**まず fail-loud accessor（`requireOutputPath`）を実装したが REQ-228 の zero/negative timeout テストが `prepareExport` を丸ごと stub して `outputPath` 未設定のまま実 `finalizeExport` に到達するため 2 件 RED で REFUTED**（`result.success` true pin）。`?? ''` や経路再生成も値が変わるため非等価 — 署名が「この経路は undefined を素通しする」実際の耐性を語る形で解決（ExportResult.outputPath は optional で全下流が undefined 受容）

**補正**: source-anchor guard `tests/export/production-exporter-safe-aggregation-migration.test.ts` の site-780 肯定 pin が旧 `endTime! - startTime!` 形を要求し陳腐化 → post-Phase-144 形（`Number(job.endTime) - Number(job.startTime)`）に更新（ヘッダに Phase 144 注記）。

**guard 更新** `tests/guards/non-null-assertion-census.test.ts`:

- `src/export (production): 0` exact pin を追加
- src ratchet **47 → 37**（47 − 10）に縮小
- mutant（export-job-queue.ts:220 `Number(job.startedAt) - job.enqueuedAt` → `(job as { startedAt: number }).startedAt! - job.enqueuedAt`）で **export exact pin + src ratchet の 2 tests RED** を実測 → **MW-009** として台帳化・ledger 監査 pin ≥8 → ≥9

## 検証

- 置換前 baseline: `--testPathPatterns export` = 73 suites / 4144 tests GREEN（64.4s）
- fail-loud accessor 試行（enhanced-export-engine）: REQ-228 zero/negative timeout 2 tests + source-anchor 1 test RED → **REFUTED を記録**し pass-through 署名で再置換
- 置換後: 同 pattern = **73 suites / 4144 tests GREEN**（同一・71.1s）
- `node_modules/.bin/tsc -p tsconfig.app.json --noEmit` exit=0
- census 実測: src/export = **0**・src = **37**
- census guard 7 tests GREEN・MW-009 mutant RED 検証（2 failed: export exact pin + `Expected: <= 37 / Received: 38` → revert 後 7 passed）
- ledger 監査 guard: pin ≥9 で GREEN（27 tests = census 7 + ledger 20）

## 完了条件

- [x] src/export 本体の postfix `!` = 0（census exact pin）
- [x] src ratchet 47 → 37 縮小
- [x] 置換後 export 系 suite 前後同一 GREEN・tsc exit=0
- [x] MW-009 台帳エントリ + 監査 pin 更新
