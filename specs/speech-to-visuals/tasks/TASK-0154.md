# TASK-0154: API Auth Middleware Including Comprehensive Tests


<!-- spine:anchor:begin -->
> **Spine anchor**: [speech-to-visuals アーキテクチャ設計](../architecture.md)
>
> - parent: `speech-to-visuals/architecture.md`
> - status: `canonical_child`
<!-- spine:anchor:end -->

**Status**: ✅ 完了
**Phase**: 40
**Priority**: 高
**見積**: 2h

## 要約

API 認証ミドルウェア (`src/api/middleware/auth.ts`) の包括的ユニットテストを追加する。JWT 認証は本番環境のセキュリティ基盤であり、テストが存在しない唯一のセキュリティクリティカルミドルウェアであった。

## 背景

Phase 39 完了後、`src/api/middleware/auth.ts` (56行) にテストファイルが 0 件であることを発見。同じディレクトリの `error-handler.ts` には 20 テストが存在するが、JWT 認証ミドルウェアのテストカバレッジが空白状態だった。

## 作業ゴール

- auth.ts の全コードパス（正常系・異常系・境界値）をテストする 11 のテストケースを追加

## 受け入れ条件

- [x] 欠損 Authorization ヘッダーで 401 UNAUTHORIZED を返すことを確認
- [x] Bearer 以外のスキームで 401 を返すことを確認
- [x] 空 Bearer トークンで 401 を返すことを確認
- [x] 有効な JWT トークンで next() 呼出・req.user 設定を確認
- [x] role 欠損時のデフォルト "authenticated" 設定を確認
- [x] email 欠損時の空文字設定を確認
- [x] jwt.verify 例外時の TOKEN_ERROR レスポンスを確認
- [x] sub 欠損トークンの INVALID_TOKEN レスポンスを確認
- [x] JWT_SECRET/SUPABASE_JWT_SECRET 双方未設定時の 401 を確認
- [x] SUPABASE_JWT_SECRET フォールバック動作を確認
- [x] req.user の型プロパティ (id/email/role) を確認
- [x] 既存テストが通る（pipeline-auth の 5 件の既存失敗は prerequired）

## 変更対象

- `src/api/middleware/__tests__/auth.test.ts` — 新規 11 テストケース

## 検証コマンド

```bash
npx jest --config jest.config.cjs --testPathPatterns="src/api/middleware/__tests__/auth" --no-coverage
```

結果: 11 passed, 11 total
