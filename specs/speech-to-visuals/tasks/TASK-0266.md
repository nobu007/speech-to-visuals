# TASK-0266: compliance 評価の捏造 leg + vacuous every() pass 撲滅 — commitPhase 除去・testingPhase/integration fail-closed 化（REQ-384・TC-368-01〜03・MW-048）

**要件**: [requirements.md](../requirements.md) REQ-384 / **受け入れ基準**: [acceptance-criteria.md](../acceptance-criteria.md) TC-368-01〜03（本タスクで [x] 化）
**種別**: DIRECT (実挙動修正 + behavioral pin + ledger dogfood + 監査 pin 更新)　**推定工数**: 2h　**信頼性**: 🔵 *実装・comment・sibling leg 全を live 読みで確認済みの実欠陥（根拠ゼロ always-pass leg による失敗 mask・空配列 vacuous every による無証拠 pass）。修正方針は REQ-383「未計測は値を主張しない」+ keyset-derived denominator + sibling leg の missing-stage 0.0 規則という既存 repo 契約に準拠*

## 背景

Phase 181 (TASK-0265) が `evaluateIterationQuality` から `documentation: 1.0 // Assuming proper documentation`（捏造 always-pass leg）を除去した際、同一ファイルの Custom Instructions Compliance 評価に同型 sibling が残存していた（**missed-sibling-site class** — 本 repo の CORE recurring bug class）:

| site | 旧実装 | 実欠陥 |
|---|---|---|
| `evaluateRecursiveDevelopmentCompliance` commitPhase | `commitPhase: 1.0 // Assuming proper commit strategy` | 根拠ゼロ always-pass が (sum + 1) / 5 で実 4 leg の重みを希釈。実 legs 合計 3.5 の run が 90.0% と報告され < 0.9 の strengthening concern が mask される（REQ-383 documentation leg と同型の失敗希釈） |
| `evaluateRecursiveDevelopmentCompliance` testingPhase | `result.stages.every(s => s.success) ? 1.0 : 0.5` | `stages: []` で `every` が vacuously true → stage 記録ゼロの run が testing 証拠 1.0 を主張（履歴あり run では 100.0% + concern なし） |
| `assessPhaseSuccessCriteria` integration | `result.stages.every(s => s.success) ? 1.0 : 0.0` | 同 vacuous pass。sibling leg（transcription/analysis/visualization）は missing stage を 0.0 扱いなのに integration だけ無証拠 1.0（一貫性欠如） |

## 修正（src/quality/quality-monitor.ts）

1. **commitPhase leg 除去**（代替 estimator なし・keyset-derived denominator が実 4 leg に縮小）
2. **testingPhase を `stages.length > 0` gate の fail-closed 化**（`stages: []` → 0.0）
3. **integration を同 gate で fail-closed 化**（`stages: []` → 0.0）

## テスト（src/quality/__tests__/quality-monitor.test.ts に 5 tests 追加）

1. 初回成功 run → `Recursive Development Compliance: 75.0%`（(1+1+1+0)/4・旧 commitPhase 希釈 80.0%）+ strengthening concern
2. 2 回目成功 run → 100.0% + concern なし（happy path 不変）
3. 履歴あり + `stages: []` → 75.0% + concern（旧: vacuous pass で 100.0%）
4. `stages: []` → `Phase Success Criteria: 20.0%`（1/5・旧 vacuous integration で 40.0%）
5. 全 named stage 成功 run → `Phase Success Criteria: 100.0%`（happy path 不変）

## MW-048 mutation（3 独立 RED 実測）

- (a) compliance object に `commitPhase: 1.0` を再注入 → **2 failed / 39 passed**（`Expected: "75.0%" / Received: "80.0%"` ×2）
- (b) testingPhase を vacuous `every` 読みに revert → **1 failed / 40 passed**（`Expected: "75.0%" / Received: "100.0%"`）
- (c) integration を vacuous `every` 読みに revert → **1 failed / 40 passed**（`Expected: "20.0%" / Received: "40.0%"`）
- 各 revert（perl 逆置換 — `git checkout --` は未 commit の正規編集ごと消す Phase 178 GOTCHA）で 41/41 GREEN 復元

## 検証

- `src/quality/__tests__/quality-monitor.test` 41/41 GREEN・`quality-monitor` match 12 suites / 321 tests GREEN・広域 `src/quality/|tests/unit/quality/|tests/guards/mutation-witness` 49 suites / 1349 tests GREEN・`main-pipeline` 7 suites / 93 tests GREEN（production caller の回帰）
- `tsc -p tsconfig.app.json` 0 error・`tsc -p tsconfig.test.json` 0 error・`npm run specs:mirror:check` exit 0
- 台帳 dogfood: MW-048 entry + appendix row・監査 pin **≥47 → ≥48**

**設計決定**: `commitPhase` に代替 estimator は作らない — commit 戦略を測定する生 signal が `PipelineResult` に存在せず、捏造 leg 除去のみで mask 実害は解消（Phase 171/REQ-383「fail-closed が計装を促す」方針の踏襲。estimator を作ると commit 戦略の測定定義が新規に必要になり scope が膨らむ）。
