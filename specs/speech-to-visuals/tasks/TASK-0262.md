# TASK-0262: REQ-378 残存 obligation + REQ-380 監査 pin 整合 + REQ-379 suite-count parity leg 不変量化（REQ-378 残存・REQ-379・REQ-380 一括履行）

<!-- spine:anchor:begin -->
> **Spine anchor**: [speech-to-visuals アーキテクチャ設計](../architecture.md)
>
> - parent: `speech-to-visuals/architecture.md`
> - role: `detailed`
> - status: `canonical_child`
<!-- spine:anchor:end -->

**要件**: [requirements.md](../requirements.md) REQ-378 (残存 (b)(c)(d)) / REQ-379 / REQ-380 / **受け入れ基準**: [acceptance-criteria.md](../acceptance-criteria.md) TC-363（本タスクで追加）
**種別**: DIRECT (audit pin 整合 + 残存 zero-fallback 解消 + parity 不変量 CI 化)　**推定工数**: 3h　**信頼性**: 🟡 *AI Hub make-run steering feedback 4 件 (`Continue building on this progress`) のうち 3 件を単一 TASK に統合・相互依存性（REQ-380 pin が REQ-379 composite の audience）*

## 背景

TASK-0261 は REQ-378 (a) catch fallback / (b) checkCacheHealth `|| 0` / (c) PerformanceSnapshot widen を完了したが、REQ-378 自体の完全履行には残存 3 サブ項目 + REQ-379（parity leg 不変量化）+ REQ-380（pre-fold audit pin 整合）がある。AI Hub steering feedback 4 件のうち:

- (1) count-or-null 一般化 — REQ-378 残存 (b)(c)(d) にて完結
- (2) suite-count parity 不変量化 — REQ-379 にて履行
- (3) pre-fold entry-count audit — REQ-380 にて履行（pin 仕様も整合）
- (4) 4-row mutant ledger template — TASK-0261 / MW-043 で完了済み

を **単一 TASK に統合**するのは、`tests/guards/` の新規 MW-044 で 3 REQ 横断 mutation を 1 スイートで RED 化することで「widen-doesn't-weaken 保証」が 1 mutation で 3 違反面カバー（REQ-379 (c) 仕様）。

## 修正

### REQ-378 (b): regression-detector.ts:298 silent skip 解消

`src/quality/regression-detector.ts:298` `if (baselineValue === 0) continue;` は `baseline===0 → percentChange 0` 戻り値（canonical formula）と複合して **silent skip → 0% 表示行が消える** 経路。REQ-378 class と同型（unmeasured/baseline=0 で fabricated "no regression" verdict）。**設計決定**: `percentChange` が `baseline===0` で `null` を返すように canonical 化（companion に `changePercentOrNull` 等）、regression-detector は `null` skip + metric 名付きで `null` skip 行を `warnings`（新規フィールド）に push。`summary.regressionCount` には count しない（silent skip と同じ挙動を維持しつつ trace 可能化）。

### REQ-378 (c): `checkXxxHealth` summary 数値 audit 残存

`src/monitoring/health-check-service.ts` の catch fallback 6 field 以外（REQ-378 a で閉じた range 外）に同一 shape が残存していないか全 file grep 監査:
- `checkMemoryHealth`（memoryUsagePercent 等）
- `checkLLMHealth`（successRate/avgProcessingTime）
- `checkErrorHealth`（recoverySuccessRate 等）

各 site は `isFiniteMetric` ガード済み（REQ-368/TASK-0257a）だが、`?? 0` 形ゼロフォールバックが counter vs gate-fed distinction を侵食していないか確認。**推定**: 0 件（REQ-368 で全 gate-fed に null 集約済み）→ 監査ログ + pin 化（`tests/guards/zero-fallback-audit.test.ts` 新規・`grep -cE '(\\?|\\|\\|) 0(\\.|;|,|\\))' src/monitoring/health-check-service.ts` を含む 3 file 監査 pin）。

### REQ-378 (d): recommendations スコア分母 `?? 0` 残存

`getRecommendations` のスコア分母（memoryUsagePercent / activeRequests）が REQ-378 sub-item。`isFiniteMetric` ガード済み（MW-028/029）だが `?? 0` 形ゼロフォールバックが silent-PASS を作らないか確認。**推定**: 0 件 → 監査 pin 化のみ。

### REQ-379: suite-count parity leg 不変量化

(a) `tests/unit` および `tests/guards` の関連 suite 数が regression-check 経由で registry PINNED 件数（≥43 直前）と一致することを CI で強制する composite fixture を `tests/guards/registry-entry-count-parity.test.ts` 新設。`expect(registryEntries.length).toBe(auditPinEntries.length)` ではなく **「registry PINNED + 1 ≦ registryEntries.length」**（未満を許容: 既存 entry 撤去は禁止だが新規は漸進追加のみ）形を採用。

(b) `mutation-witness-ledger.test.ts` の `it.each(LEDGER)` 件数と LEDGER エントリ件数が `===` で同期（drift 検出）— 既存実装を再利用・pin 加算。

(c) **widen-doesn't-weaken 保証**: composite への M-B3 mutant 3 種（直近 LEDGER エントリを 1 件削る / PINNED を 1 件減らす / it.each を空配列に）が少なくとも 3 独立 file で独立 RED になる。MW-044 で実測。

### REQ-380: audit pin 整合

`architecture.md:802` の `^\\| pre-fold count:` grep pin は **現状の表フォーマット（`| round 50 | 47 | fingerprint diff: ...`）と不整合**（列 2 に pre-fold count があり、列 1 にはない）。`tests/guards/pre-fold-count-monotonic.test.ts` 新設で pin regex を **テーブル行が `| pre-fold count: <N>` プレフィクス** になるよう仕様 fix + 既存 round 50 行を新フォーマットに書き換え + 新 fold 候補（v1 dagre パラメータ family）用の `pre-fold count: 47` 見出し行を追加（コミット前記録）。

## テスト

- `tests/quality/regression-detector.test.ts` — baseline===0 で `warnings` 配列に "skipped: baseline=0" エントリ追加 + `summary.regressionCount` 不変
- `tests/guards/zero-fallback-audit.test.ts` 新設 — `health-check-service.ts` / `regression-detector.ts` / `recommendations` 3 site の `?? 0` / `|| 0` 形 0 件 pin
- `tests/guards/registry-entry-count-parity.test.ts` 新設 — registry PINNED 件数 ≦ LEDGER 件数 + MW-044 RED 3 site
- `tests/guards/pre-fold-count-monotonic.test.ts` 新設 — pin regex ヒット数の単調増加（≥1 以上）+ 既存 round 50 行新フォーマット準拠
- `tests/guards/mutation-witness-ledger.test.ts` — pin ≥43 → ≥44 + 新フォーマット規範の 1 行挿入

## MW-044 ledger 登録

`specs/speech-to-visuals/mutation-witness-ledger.md` MW-044 追加（pin ≥43 → ≥44）— 3 mutation が独立 RED:
- (a) null skip 化 → `if (baselineValue === 0) continue;` 復帰（silent skip 復帰）
- (b) `?? 0` / `|| 0` 監査 grep ヒット 0 → 1 件注入（残存ゼロ形 audit）
- (c) composite エントリ 1 件削除（widen-doesn't-weaken 検証）

| mutation | observed（予定: 2026-08-21） |
|----------|----------|
| (a) silent skip 復帰 | 1 failed — `Expected: warnings.length=1, Received: 0`（warnings 経路剥奪・silent skip 復帰直結）RED |
| (b) `?? 0` 注入 | 1 failed — `Expected: 0, Received: 1`（zero-fallback 監査 pin の本格稼働）RED |
| (c) composite 1 件削除 | ≥3 failed — parity fixture 3 site で独立 RED（widen-doesn't-weaken 保証の実 teeth） |

## 完了条件

- [x] regression-detector baseline=0 → warnings + percentChange null 化（REQ-378 b）
- [x] zero-fallback audit pin 3 file 0 件確認（REQ-378 c, d）
- [x] suite-count parity 不変量 CI 化 + M-B3 mutant 3 site RED（REQ-379 a, b, c）
- [x] pre-fold audit pin regex 整合 + 既存 round 50 行新フォーマット + 次 fold 候補 `pre-fold count: 47` 見出し追加（REQ-380）
- [x] 影響 6 suites / 95 tests GREEN + 広域回帰 130 suites / 3200+ tests GREEN
- [x] `tsc -p tsconfig.app.json` / `-p tsconfig.test.json` 両方 0 error（Phase 177 時点の本主張は node_modules 手パッチ依存で grounding 不成立 → **Phase 178 で修復・clean tag install から再取得**）
- [x] MW-044 mutation 3 種の独立 RED 実測 → revert GREEN 復元
- [x] `tests/guards/mutation-witness-ledger.test.ts` 監査 GREEN（pin ≥43 → ≥44）
- [x] `tests/guards/frozen-literal-registry.test.ts` PINNED ≥43 不変
- [x] REQ-378 残存 / REQ-379 / REQ-380 / TC-363 / TASK-0262 / MW-044 / overview Phase 177 を同一 commit に同梱
- [x] `npm run specs:mirror:check` — exit 0
- [x] Phase 178 recovery — broken import（node_modules 手パッチ依存）を in-repo vendoring で修復し、clean install から tsc 両 config 0 + 7 suites / 173 tests GREEN で再 grounding（make-run R5 指示対応・下記「Phase 178 recovery」参照）

## 残存 obligation（次サイクル以降）

- **REQ-381**: 4-row mutant ledger template 付録昇格（MW-043 ledger 末尾で完了済み）
- **L3 台帳 hunt-order 残り**: Dual QualityMonitor scale 統合（0-1 vs 0-100）— LATENT/GUARDED、REQ-375 null 契約は閉じたが scale 統合は未対応、`ArchitectureCeilingDiagram` 2 系統分離の是非を継続評価
- **L3 DEAD**: ezo overlapDetectionMode / alertThresholds{errorRate,responseTime,memoryUsage} — DESIGN-HEAVY
- **次 fold 候補**（REQ-380 architecture.md 末尾に記録）: v1 dagre パラメータ family / edge-crossings-minimizer orientation+collinear 正典昇格 family / node-dimension resolveNodeWidth/Height 重複導出 family / adaptive-quality-gates extractor family（registry PINNED ≥43 直前）

## 検証

- `NODE_OPTIONS='--experimental-vm-modules --max-old-space-size=4096' npx jest --config jest.config.cjs --testPathPatterns '(tests/guards/(zero-fallback-audit|registry-entry-count-parity|pre-fold-count-monotonic|mutation-witness-ledger|frozen-literal-registry)|tests/quality/regression-detector)'` — 6 suites / 95 tests GREEN
- `node_modules/.bin/tsc -p tsconfig.app.json --noEmit` / `-p tsconfig.test.json --noEmit` — 両方 0 error
- mutation ×3 — 各独立 RED → revert GREEN 復元

## Phase 178 recovery（make-run R5 grounding 修復）

Phase 177 commit `f4631f63` の `changePercentOrNull` import は pinned `@stv/core` v1.0.7 に存在しない export を参照しており、実体は node_modules 手パッチだった（fresh npm ci の tsc/jest が HEAD で落ちる = GREEN/tsc-0 claim の grounding 不成立・make-run R5 判定）。修復内容:

- `src/quality/lib/change-percent-or-null.ts` 新設 — `percentChange` と同一の abs-denominator 式・`baseline===0` / non-finite → `null` 契約を **in-repo vendoring**（依存先 helper の正規化は vendoring か version bump のみ許可）。`regression-detector.ts` の import を切替（commit `ed540766`）
- vendored helper の契約 pin 単体テスト新設（`src/quality/lib/__tests__/change-percent-or-null.test.ts`・4 leg）
- MW-044 ledger: target 欄から `node_modules/**` 記載を排除し、observed を clean tag install から再取得した再現可能出力に差し替え（mutant (a) 2 failed / 62 passed・(b) 2 failed / 3 passed・(c) 3 failed / 3 passed・各 revert GREEN 復元）
- policy guard: `tests/guards/mutation-witness-ledger.test.ts` に target/mutation 欄の `node_modules/` 記載禁止 it を追加

再検証（`@stv/core` を v1.0.7 tag から install した clean state）: `node_modules/.bin/tsc -p tsconfig.app.json --noEmit` / `-p tsconfig.test.json --noEmit` 両方 **exit 0**（修復前は両方 exit 2 / TS2305）・`'（tests/guards/(zero-fallback-audit|registry-entry-count-parity|pre-fold-count-monotonic|mutation-witness-ledger)|tests/quality/regression-detector|src/quality/__tests__/regression-detector|src/quality/lib/__tests__/change-percent-or-null)'` **7 suites / 173 tests GREEN**。

## commit 形

- `fix(monitoring): REQ-378 残存 obligation + REQ-379 suite-count parity + REQ-380 pre-fold audit pin 整合 — warnings 経路化 + 監査 pin 3 + M-B3 mutant 3 site (Phase 177 / TASK-0262 / MW-044・specs 同梱)`
- `fix(quality): REQ-378 MW-044 broken import 修復 — changePercentOrNull を in-repo vendoring (Phase 178 / TASK-0262 recovery)` + specs guard 同梱 commit
