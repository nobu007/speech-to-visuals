# TASK-0241: tests ツリー non-null assertion ratchet 単調減少ラウンド 7・transcription 初の dir exact-0 と空洞化チェック簡素化（guards 72 → 60・integration 107 → 71・visualization 78 → 61・pipeline 20 → 11・quality 17 → 9・transcription 8 → 0）

<!-- spine:anchor:begin -->
> **Spine anchor**: [speech-to-visuals アーキテクチャ設計](../architecture.md)
>
> - parent: `speech-to-visuals/architecture.md`
> - role: `detailed`
> - status: `canonical_child`
<!-- spine:anchor:end -->

**要件**: [requirements.md](../requirements.md) REQ-344 / **受け入れ基準**: [acceptance-criteria.md](../acceptance-criteria.md) TC-340
**種別**: DIRECT (実装 + guard)　**推定工数**: 3h　**信頼性**: 🔵 *A153 残課題（「tests 残 428 の継続縮小」）+ steering の ratchet 単調減少継続指令 + guard-first per-file census 実測*

## 背景

Phase 153（TASK-0240 / REQ-343）が 4 ディレクトリ横断 8 ファイル 110 node → 0（総 538 → 428）を完了。steering が単調減少の継続を指令。前ラウンドの機械閾値 **「node ≥ 10 全数」は残存 0 で枯渇**したため、本ラウンドの対象選定を **guard-first survey の降順上位 10 ファイル（6 ディレクトリ横断・計 90 node）** に戻した:

- tests/guards/edge-anchor-geometry-single-source.test.ts — **12 node**
- tests/integration/api.test.ts — **9 node**
- tests/integration/export-error-recovery-integration.test.ts — **9 node**
- tests/integration/export-retry-dlq-metrics-integration.test.ts — **9 node**
- tests/integration/pipeline-recovery-e2e.test.ts — **9 node**
- tests/visualization/layout-quality-composite.test.ts — **9 node**
- tests/quality/regression-detector.test.ts — **8 node**
- tests/transcription/browser-transcriber.test.ts — **8 node**
- tests/visualization/strategies/flowchart-strategy.test.ts — **8 node**
- tests/pipeline/retry-observability-surface.test.ts — **9 node**

tests/transcription が 8 → 0 となり **tests 内初のディレクトリ exact-0 pin** が発火。steering が予見した「pin が減り空洞化チェック（TC-333-02 c）と未 pin dir throw という失敗形そのものが不要になり guard が単純化される」方向の第一歩として、空洞化チェックを **hits 有無ベースから files 有無ベース（`testsDirsByFiles`）** に簡素化した（未 pin dir throw は残存 — 新ディレクトリ新設時の pin 忘れを捉えるため）。

## 実施内容（2026-08-20 完了）

### 1. guards/edge-anchor-geometry-single-source.test.ts 12 node → 0（fail-loud map lookup）

`byId.get(edge.from)!` / `byId.get(edge.to)!`（pair 引数の両端点）と `got.get(...)!`（points 参照 4 形態 — fallback it.each・v1/v2 assertPair・ezo centerAnchor block）を id/key 付き throw に集約:

```ts
function requireNode(byId: Map<string, PositionedNode>, id: string): PositionedNode {
  const node = byId.get(id);
  if (node === undefined) throw new Error(`node ${id} is missing from the emitted layout`);
  return node;
}
function requirePoints(got: Map<string, LayoutEdge['points']>, key: string): LayoutEdge['points'] {
  const points = got.get(key);
  if (points === undefined) throw new Error(`edge ${key} emitted no points`);
  return points;
}
```

`PositionedNode` / `LayoutEdge` は当該 suite が既に import 済みのため追加 import 不要。

### 2. integration/api.test.ts 9 node → 0（fail-loud `requireJobStatus`）

`getJobStatus(jobId): BatchJobStatus | null`（src/api/routes/batch.ts:115）の `status!` 5 block を `requireJobStatus(manager, jobId)` に。**null-guard 根拠**: 戻りが `| null` のため undefined でなく null を guard（Phase 151/152 と同一）。直前の `expect(status).not.toBeNull()` 対は helper の throw が同保証を担うため折りたたみ。

### 3. integration/export-error-recovery-integration.test.ts 9 node → 0（`requireDequeued` + definite-assignment holder）

`dequeue(): QueuedExportJob | undefined`（src/export/export-job-queue.ts:178）の 8 site を `requireDequeued(queue)` に。残る `resolveRender!()` は definite-assignment holder で保存:

```ts
let resolveRender: (() => void) | undefined;
const renderPromise = new Promise<void>((resolve) => { resolveRender = resolve; });
const finishRender = resolveRender;
if (finishRender === undefined) throw new Error('render promise executor did not run');
// …finishRender() で解決
```

executor は同期的に走るため throw は到達不能・到達時は fail-loud（旧 `resolveRender!()` の TypeError と同一 RED verdict）。

### 4. integration/export-retry-dlq-metrics-integration.test.ts 9 node → 0（`requireDequeued` / `requireReplayed`・loop 内 null guard 保存）

`replayDeadLetterJob(jobId): QueuedExportJob | undefined`（src/export/export-job-queue.ts:473）を `requireReplayed(queue, jobId)` に。**DLQ エラーメッセージ検証は verdict 保存に注意**: 失敗を繰り返すごとに job が queue に戻るため `dequeued` は再代入ループ構造で最終的に undefined になるのが検証対象 — helper で潰すと最終 `expect(dequeued).toBeUndefined()` が検証不能になるため、**loop 内 null guard**（`if (dequeued === undefined) throw new Error(...)`）+ ループ末尾の `expect(dequeued).toBeDefined()` / 最終 `toBeUndefined()` をそのまま保存。

### 5. integration/pipeline-recovery-e2e.test.ts 9 node → 0（`requireMetrics` / `requireRecoveryReport` / `requireStageTimings`・cast 除去）

Phase 153 と同型の helper 群を `PipelineResult` import 付きで導入（`type PipelineMetrics = NonNullable<PipelineResult['metrics']>`）。`requireRecoveryReport` の narrowing で旧 `as RunRecoveryReport` cast を解消（field は ExtendedPipelineMetrics で既に `RunRecoveryReport` 型）。既存の `!` 無し site（`result.metrics.recoveryReport as RunRecoveryReport`）は対象外として未触。

### 6. pipeline/retry-observability-surface.test.ts 9 node → 0（`requireMetrics` / `requireStageTimings`）

§5 と同型。局所構築 literal（`metrics` を直接構築する test）は file 自身の idiom である `result.metrics?.totalRetryAttempts` optional chaining で保存（helper 適用対象外 — その test の検証対象は optional 伝播自体）。

### 7. quality/regression-detector.test.ts 8 node → 0（`requireLoadedBaseline` / `requireByMetric`）

`loadBaseline(): BaselineData | null`（src/quality/regression-detector.ts:136）を `requireLoadedBaseline(loaded)` に:

```ts
type LoadedBaseline = Awaited<ReturnType<RegressionDetectorType['loadBaseline']>>;
function requireLoadedBaseline(loaded: LoadedBaseline): NonNullable<LoadedBaseline> {
  if (loaded === null) throw new Error('loadBaseline() returned null for a file that was just written');
  return loaded;
}
```

**`NonNullable` 戻りが必須**: 素の `Awaited<ReturnType<…>>` は `BaselineData | null` のままなので戻り型も nullable になり呼び出し側で TS18047（初回実装で発生・修正）。`loaded!.sampleSize` 系 4 site（147-148 / 315-316 相当）と `r!.severity` / `imp!.changePercent` / it.each `regression!.changePercent` は generic `requireByMetric<T extends { metric: string }>(entries, metric)` に集約。

### 8. transcription/browser-transcriber.test.ts 8 node → 0（null-guard 委譲 `fireHandler`）

mock recognition instance の `onresult` / `onerror` / `onend` handler（`((ev: T) => void) | null`）の起動 8 site:

```ts
function fireHandler<T>(handler: ((ev: T) => void) | null, ev: T): void {
  if (handler === null) throw new Error('recognition handler was not assigned by the transcriber');
  handler(ev);
}
```

未割り当て = transcriber が handler を登録しなかった実装破綻のため fail-loud が正しい verdict（旧 `onresult!(event)` の TypeError と同一 RED）。

### 9. visualization/layout-quality-composite.test.ts 9 node → 0（`requireStage3Gate` / `requireCriterionResult`）

`createDefaultQualityGates().find(g => g.stage === 3)` の `stage3!` と `evaluation.results.find(r => r.criterionName === …)` の `criterion!` / `composite!` / `overlap!` を、`StageEvaluationResult` / `StageCriterionResult` を `@/quality/quality-gate` から type import した helper に。perl 一括置換の 3 site 取りこぼしは個別 Edit で補完。

### 10. visualization/strategies/flowchart-strategy.test.ts 8 node → 0（typed `findNode` + superfluous `!` 除去）

Phase 152 flow-strategy と同型の `findNode`（`PositionedNode` 型付き）で 3 find site を置換。残る 5 node は **`!` が最初から不要**な抑制だった: `strategy.validateInputs!(…)` ×4 と `strategy.getStrategyDefaults!()` — 変数の静的型は具象 class `FlowchartLayoutStrategy`（member は non-optional・optional は `ILayoutStrategy` interface 側のみ）のため `!` 除去が挙動保存（checker 抑制の解消のみ）。

### 11. ratchet pin 更新 + 空洞化チェック簡素化 + MW-020

census guard（tests/guards/non-null-assertion-census.test.ts）:

- `TESTS_DIR_PINS`: guards 72 → **60**・integration 107 → **71**・visualization 78 → **61**・pipeline 20 → **11**・quality 17 → **9**・**transcription 8 → 0（tests 内初の dir exact-0）**（unit 103 は不変）
- `PINNED['tests (excl. __mocks__)']`: 428 → **338**
- 空洞化チェック: hits 有無（= pin > 0 の dir に残存 `!` があること）から **files 有無（`testsDirsByFiles` = census が走査した実ファイルの第一階層 dir 集合に pin key が含まれること）** に簡素化。exact-0 dir では hits が常に空になるため旧実装は運用不能だった。`countAssertions` を `{count, hits, files}` 戻りに拡張。未 pin dir throw は残存。
- ヘッダに Phase 154 bullet（10 ファイル・helper 群）と **MW-020** を追記

**MW-020**（ledger 台帳・監査 pin ≥19 → ≥20）: Phase 154 rewrite への `!` 1 node 再注入 — `fireHandler(mockRecognitionInstance.onerror, { error: 'network', message: 'Network error' });` → `mockRecognitionInstance.onerror!({ error: 'network', message: 'Network error' });`（browser-transcriber.test.ts）— で **tests 合計 ratchet（`Expected: <= 338 / Received: 339`）と tests/transcription exact-0 dir ratchet（`Expected: <= 0 / Received: 1`）の 2 tests RED** を実測 → revert で census GREEN・監査 pin ≥20 GREEN。同一 run 環境で空洞化チェックの新失敗形も RED 検証（bogus pin `'nonexistent-dir': 0` 追加 → `Expected: true / Received: false` → 除去）。

**補正**: なし — 編集 13 ファイル（test 10 + census guard + ledger guard + ledger md）に source-anchor guard の肯定 pin は存在しない。計数器（AST NonNullExpression + `!:` node）の意味変更なしのため pin 再ベースラインの新旧対応表も不要（減少のみ）。

## 検証

- census + ledger 監査 guard GREEN（監査 pin ≥20・census 11 tests）
- 置換対象 10 suite を含むパターン一致: **17 suites / 777 tests GREEN**（TC-340 再検証コマンド）
- `node_modules/.bin/tsc -p tsconfig.test.json --noEmit`: **0 新規 error**（census guard の既知 3 error のみ・`git stash` で HEAD 同一（行シフト 452→514 のみ）を確認済み）
- フルスイート（全変更後・並走なし）: **742 suites / 742 passed・23,163 passed / 0 failed / 17 skipped（jest Time 98.067s）**。run 履歴は A154 の [EVIDENCE] 行に出典
- MW-020 mutant RED 2 failed → revert → GREEN サイクル実測
- census 実測: guards = **60**・integration = **71**・visualization = **61**・pipeline = **11**・quality = **9**・transcription = **0**・tests 合計 = **338**

## 完了条件

- [x] guards 72 → 60・integration 107 → 71・visualization 78 → 61・pipeline 20 → 11・quality 17 → 9・transcription 8 → 0・tests 合計 pin 428 → 338（guard pin 更新 + GREEN）
- [x] 対象が guard-first survey の降順上位 10 ファイル（node ≥ 10 枯渇後の選定切替）で選択されていること
- [x] 置換 10 ファイルの `!` AST node = 0（挙動保存・verdict 保存・DLQ 再代入ループの undefined verdict 保存・superfluous `!` 除去 5 site・cast 除去）
- [x] transcription exact-0 に伴う空洞化チェックの files ベース簡素化（bogus pin RED 検証込み）
- [x] 対象 suite GREEN・tsc 0 新規 error・full suite 0 failed
- [x] MW-020 台帳エントリ + 監査 pin ≥20
