# TASK-0226: src/visualization non-null assertion 撲滅 + 全ツリー census ratchet

<!-- spine:anchor:begin -->
> **Spine anchor**: [speech-to-visuals アーキテクチャ設計](../architecture.md)
>
> - parent: `speech-to-visuals/architecture.md`
> - role: `detailed`
> - status: `canonical_child`
<!-- spine:anchor:end -->

**要件**: [requirements.md](../requirements.md) REQ-328 / **受け入れ基準**: [acceptance-criteria.md](../acceptance-criteria.md) TC-324
**種別**: DIRECT (実装 + guard)　**推定工数**: 6h　**信頼性**: 🔵 *AI Hub steering（前 iteration VALUABLE 判定への follow-up・2026-08-19）+ grep 実測*

## 背景

TASK-0224（strict lock-in）完了後、steering は「02fa054a で optional 寸法フィールドに付与した non-null assertion（`!`）は checker を黙らせるだけで安全性を証明しない」と指摘した。実測: src/visualization 本体（`__tests__` 除く）に 67 件の postfix `!`（20 ファイル）、src 全体で 170 件。

## 実施内容（2026-08-19 完了）

1. **census ratchet guard 新設** `tests/guards/non-null-assertion-census.test.ts`:
   - src/visualization 本体 = **0（exact pin）**、src 本体（`__tests__`/`__mocks__` 除外）= **≤93**、tests ツリー（`__mocks__` 除外）= **≤960**。
   - マッチング規則: `[\w)\]]!` で `!=`/`!==` を除外し、直後が code 継続文字（空白・句読点・EOL）に限る（文字列内の `'…!'` を除外）。コメント専用行はskip。
   - mutant 注入（advanced-layouts.ts 末尾に `!` 付き行追加）で 2 test RED を実測検証（MW-005）。
2. **src/visualization 67 件 → 0 件**（挙動変更ゼロ・全置換は以下の型別パターン）:
   - **no-op `!` 除去（型上不要）**: LayoutStrategy.ts（`EdgeDatum.from` は必須のため `edge.from ?? edge.source` は string）・layout-engine.ts（`layoutEvaluator` を `?` なしフィールド化 = constructor で無条件代入済み、evaluator メソッドは non-nullable 返却）・OverlapResolver.ts（`clearTimeout(undefined)` は lib 署名適合）
   - **narrowing**: node-dimensions.ts（`v !== undefined && Number.isFinite(v)`）・OverlapResolver initializeEdges（`edge.source !== undefined &&`）・edge-crossing-minimizer.ts（`fromId === undefined` continue ガード ×3 箇所）
   - **型付き helper**: `src/visualization/edge-endpoints.ts` `lookupEndpoint()` — `nodeMap.get(edge.source!)` 10 サイト（edge-repointing・layout/edge-crossings・SimulatedAnnealing・ProgressiveForce）を `lookupEndpoint(nodeMap, edge.source)` に。undefined key → undefined は `Map.get(undefined)` と同一結果
   - **fail-loud accessor**: ezo `requireMinimumSpacing()`/`requireOptimization()`（15 サイト）・cle `requirePerformanceTargets()`（5 サイト）— Partial spread による explicit-undefined override を名前付き Error に
   - **get-or-create 変数キャプチャ**: spatial-hash insert・ezo collision grid・tree-strategy levelGroups・LayoutOptimizer levels・timeline adjacency
   - **queue.shift() ガード**: 7 サイト（advanced-layouts・cle・flow・tree・conceptmap・timeline・mindmap）— `if (item === undefined) break;`
   - **find/get フォールバックの明示 throw**: TreeLayoutStrategy cycle guard・conceptmap/mindmap radial positions（見つからない場合の TypeError を名前付き Error に）
   - **defaultTheme フィールド化**: advanced-layouts getTheme（dark theme 単一リテラルを inline initializer に）
3. **guard pin 更新 2 件**（旧 `!` 形状を pin していた source anchor）:
   - edge-repointing-single-source.test.ts:275（`nodeMap.get(edge.source!)` → `lookupEndpoint(nodeMap, edge.source)`）
   - overlap-pair-scan-single-source.test.ts:329（`this.config.minimumSpacing!?\.nodeToNode` → `this.requireMinimumSpacing()\.nodeToNode`）

## 検証

- `tsc -p tsconfig.app.json --noEmit` exit=0
- tests/(visualization|guards)/ = **128 suites / 4319 tests GREEN**・src/visualization/__tests__ = **34 suites / 658 tests GREEN**（書き換え前後で全 GREEN・挙動変更ゼロの実証）
- census guard 自体: 4 tests GREEN・mutant RED 検証済み（MW-005）
- full-suite 実行: Phase 141 完了時の [EVIDENCE] 行を interview-record A141 に記載

## 完了条件

- [x] src/visualization 本体の postfix `!` = 0（census exact pin）
- [x] src/tests ratchet guard が CI で増加を防止
- [x] 置換後も 全 visualization + guard suite GREEN
