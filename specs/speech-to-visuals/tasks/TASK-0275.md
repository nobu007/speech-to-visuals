# TASK-0275: score-ladder（凍結小数 leg）census guard 新設 + 10 site 一括撲滅（REQ-393・TC-377-01〜04・MW-057）

<!-- spine:anchor:begin -->
> **Spine anchor**: [speech-to-visuals アーキテクチャ設計](../architecture.md)
>
> - parent: `speech-to-visuals/architecture.md`
> - role: `detailed`
> - status: `canonical_child`
<!-- spine:anchor:end -->

**要件**: [requirements.md](../requirements.md) REQ-393 / **受け入れ基準**: [acceptance-criteria.md](../acceptance-criteria.md) TC-377-01〜04（本タスクで [x] 化）
**種別**: TDD (audit → 撲滅 → census → mutation 検証)　**推定工数**: 3h　**信頼性**: 🔵 *steering が直接要求した audit-pass-first の第 3 facet（「数値literal比較でスコアを返すsite」）。REQ-391 値側・REQ-392 契約側に続く比較/fallback 側で、3 census で凍結測定 class の全既知 facet を cover*

## 背景

REQ-372〜392 が「凍結定数・未 populate 契約」class を撲滅してきたが、REQ-391 は object-literal の bare 値、REQ-392 は optional field の producer 有無を census 化したのに対し、**条件で選択される score literal**（`cond ? 0.9 : 0.3`・`x ?? 0.9`・count ladder）は手動発見のまま残っていた。steering は「数値literal比較でスコアを返すsite」を明示的に enum 対象として指示。

audit pass（`/tmp/ladder-audit.mjs`・行-level discovery を REQ-391/392 と同一 `walkProductionSurface` 上で実施）で pre-fix 29 site を enumerate し各 site を分類（fabricated / knob / disclosed-binary / measured-band / neutral-prior）→ fabricated 10 site を確定。@stv/core core-four は 0 site。

最も重要な発見: **SimplePipeline continuousLearner の transcription leg `success && segments>0 ? 0.9 : 0.3`**。learner 自身の `triggerCustomInstructionsImprovement` は `qualityScore < 0.85` で発火するため、成功 run が常に 0.9 を publish する限り quality-degradation 検出は恒久に沈黙する — REQ-383〜392 が潰してきた permanently-green gate class の pipeline 内残留。

## 実装

1. **canonical estimator 新設** — `src/pipeline/quality-estimators.ts` に `meanSegmentConfidence`（transcriber が publish する per-segment confidence の平均・未計測 segment は 0 を寄与して平均を引き下げる fail-closed・空配列 0）。SINGLE SOURCE 化で「how confident was this transcription」の第二 frozen scale 発生を構造的に防止
2. **SimplePipeline learner 6 leg 撲滅** — transcription/segmentation leg → `meanSegmentConfidence`（segmentation は ContentSegment.confidence = segmenter 自身の max-of-merged 導出）・enhanced bridge `aestheticScore ?? 0.8` → `?? 0`・layout leg `min(0.95, 0.8+conf*0.15)` → 測定 `sanitizeFinite(lr.confidence)` 直接（fail 0）・diagram_pipeline leg → 実測 scene yield（`successRate` にも同一値）・video leg → disclosed binary `? 1 : 0`（quality-monitor successScore idiom）
3. **transcription 3 file + video-generator 撲滅** — caption `confidence: segment.confidence ?? 0.9`（transcriber/whisper）→ `?? null`（`Caption.confidence: number | null` の null が型自身の未計測値）・Web Speech final `?? 0.9` → named `FINAL_NO_CONFIDENCE_STANDIN = 0.5`（interim chunk と同一の disclosed neutral 慣行）・streaming final-chunk `: 0.8` → named `PLACEHOLDER_CHUNK_CONFIDENCE`・`video-generator.ts` `scene.confidence ?? 0.8` → `?? 0`（in-file comment 自身が 0.8 mask は validateRemotionData の low-confidence warning を silencing すると論じていた — comment の論証完成）
4. **sibling single-sourcing** — PipelineOrchestrator transcription case の inline reduce（`?? 0` とはいえ独自 scale）を `meanSegmentConfidence` 委譲へ
5. **census guard 新設** — `tests/guards/score-ladder-census.test.ts`: discovery は ternary false-leg bare decimal / `??`・`||` fallback bare decimal × measurement token（REQ-391 語彙 − config token）を行 level で走査し `file::identifier` key 化。ALLOWED 17 key（neutral prior 2 / config knob 6 / disclosed binary gate・measured band 5 / config-side `??` 2 + UI form fallback 2）+ ERADICATED 11 key。7 test: authority / completeness / stale-row / reason 衛生 / eradicated-reappear / **negative anchors**（複数行に割れた segmentation/layout ladder など行-level discovery の上限を literal shape ban で補完）/ **estimator-integrity**（`meanSegmentConfidence` の measured-only 本体 `: 0;` anchor + 3 consumer delegation pin）
6. **learner leg 実測伝播 test** — `tests/unit/pipeline/simple-pipeline.test.ts` に REQ-393 describe 5 test（real `continuousLearner` singleton を spyOn し `learnFromProcessingResult` の第 5 引数 quality を pin）: 0.6/0.8→0.7・`[0.9, undefined]`→0.45（fail 値）・segmenter 0.75 pass-through・scene yield 2/2→1・layout 0.8 in→0.8 out（旧 rescale なら ~0.92）。fixture は `segmentConfidences`/`contentConfidence` を拡張
7. **MW-057 mutation 検証（3 独立 RED 実測）** — (a) `? 0.9 : 0.3` band 再注入 → **4 failed**（completeness + eradicated-reappear + negative anchors + estimator-integrity）(b) caption `?? 0.9` 再注入 → **3 failed**（completeness + eradicated-reappear + negative anchors・`transcriber.ts::confidence @ :288`）(c) estimator fail 値 `? c : 0`→`? c : 0.85` → guard **1 failed** + unit test **1 failed**（`Expected: 0.45, Received: 0.875`）。**副次発見**: mutation (c) が guard の source-anchor `: 0` は `: 0.85` の prefix に一致して素通りする抜け穴を発見 → anchor を `: 0;` に厳格化し guard 単独でも検出

## 検証

- census guard 7/7 GREEN・simple-pipeline suite 34/34 GREEN（新規 5 test 込み）
- full suite（761 suites）GREEN・tsc 両 config（app/test）exit 0
- specs 整合: `specs:anchor:check` / `specs:mirror:check` / `spine:validate` 全 GREEN
- 台帳 dogfood: MW-057 entry + appendix row・監査 pin **≥56 → ≥57**・`tests/guards/mutation-witness-ledger.test.ts` PINNED_MIN_ENTRIES 更新

## 副次発見・修復（stale test 4 件）

- REQ-393 挙動 pin 取り替え 3 件: `src/transcription/__tests__/transcriber.test.ts`（caption undefined confidence 0.9→null）・`streaming-transcriber.test.ts`（final-chunk fallback 0.8→PLACEHOLDER_CHUNK_CONFIDENCE 0.75）・`tests/integration/video-generator-duration-unit.test.ts`（missing confidence 0.8→0 — 当該 test block の comment 自身が 0.8-mask を批判していた）
- **pre-existing RED 1 件**（本 run の full-suite 検証が発見・stash で HEAD 起因を確認）: `src/__tests__/genuine-guard-verification.test.ts` は Phase 183 (REQ-385) の empty-criteria fail-closed 化で陳腐化した twin 取り残し（旧 vacuous `passed=true` / `allMet=true` を pin）で **HEAD 時点でも 2 failed** — REQ-385 契約（loud fail row・passed=false）へ更新。subset run は 3 phase 連続でこれを見逃しており full-suite run の価値の再実証（Phase 181 と同型）
