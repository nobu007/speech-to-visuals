# TASK-0309: 第九回 discovery sweep — 同一 batch guard へ 28 kind 追加 + string-char-split unify 1 site + 1 class pin 前棄却（母集団不一致 3 例目） + MW-082 mutation 検証（REQ-418・batch 形式）

<!-- spine:anchor:begin -->
> **Spine anchor**: [dead-idiom batch census 要件定義](../requirements.md) REQ-418
>
> - parent: `dead-idiom-batch-census/requirements.md` REQ-418
> - role: `detailed`
> - status: `canonical_child`
<!-- spine:anchor:end -->

**要件**: [requirements.md](../requirements.md) REQ-418-001〜004
**種別**: TDD（sweep #9 測定 → 28 kind 追加 → unify 1 site → 棄却 1 記録 → MW-082）　**推定工数**: 2h　**信頼性**: 🔵 *実測 29 candidate class（331 file・canonical `walkProductionSurface` 直接使用で計測・1 class は pin 前棄却）+ unify 1 site は classifyChar range table 実値との交差確認で ratio 完全等価を保証 + ALLOWED 1 site は実コード読み + MW-082 4 mutation とも offender list で kind 帰着を確認（(c) は mutation が detector optional-chain 抜けを発見 → 修正して RED 化）*

## 背景

steering 契約（REQ-410 確立）の 9 回目の適用。新 spec dir・新 family・新
phase 群を開かず、**同一 guard の kind 追加のみ**で 28 class の ratchet を
得る。90 kind 状態（REQ-417 sweep #8 merge 済み）から開始。

sweep #9 は 29 candidate を計測: 26 class exact-0 pin、1 class は ALLOWED
1 site 同梱（dead-ua-platform）、1 class（string-char-split）は unify 1 site
同梱（`split('')` → `[...text]`・ratio 等価）、1 class（react-default-props）
は pin 前棄却。

## 実装

1. **`tests/guards/dead-idiom-batch-census.test.ts` に 28 kind 追加**
   （`IDIOM_KINDS` 90→118 entry・kind pin ratchet 更新）:
   outer-html-assignment / srcdoc-assignment / legacy-dispatch-event /
   ie-attach-event / ie-current-style / window-execscript /
   window-navigate / element-set-capture / document-create-stylesheet /
   array-join-repeat / react-string-ref / react-create-class /
   react-is-mounted / legacy-context-api / on-prefixed-event-name /
   object-ctor / document-domain-access / with-statement /
   window-showmodaldialog / document-selection-ie / ie-do-scroll /
   event-cancel-bubble / event-src-element / document-layers /
   console-assert-sink / then-two-arg-rejection / dead-ua-platform /
   string-char-split — いずれも行 detector 1 entry（steering 契約
   どおり class 追加 = 1 entry）
2. **liveness fixture (ac1)〜(ac28)**: 各 kind の検出・非検出境界
   （`Array(width + 1).join(pad)` は bare-array-ctor との二重検出を
   変数長引数で分離・`window.showModalDialog(url)` は lookbehind を
   member 許容形にして検証・`navigator.language` は dead-ua-platform
   への帰着を負例で分離）
3. **既存 fixture 負例の明け渡し（kind 分離契約 4 例目）**: (ab3)
   `navigator.platform` 負例 → `navigator.language` 形に差し替え
   （既存 kind の detect regex は 1 行も不変）
4. **src unify 1 site**: `src/analysis/language-detector.ts:537`
   `const chars = text.split('');` → `const chars = [...text];`
   （classifyChar range は surrogate block と不交差 → ratio 完全等価・
   同 file の `for (const char of text)` 綴りと整合）を ERADICATED 13 key
   に固定 + ALLOWED 1 key 追加（production-error-handler:273
   REPORT-ONLY）+ census-pin header 22→23 key / 12→13 key
5. **MW-082 mutation 検証**: 4 独立 mutation（language-detector 末尾へ
   `text.split('')` 再出・`el.outerHTML = markup`・
   `globalThis.document?.createEvent("HTMLEvents")`・
   `addEventListener('onclick', ...)` 注入）で completeness RED を実測し
   mutation-witness ledger に追記
6. **spec 同梱更新**: requirements.md に REQ-418 節・本 TASK・
   overview Phase 226

## 受け入れ基準

- [x] kind 118 entry ratchet（90→118・追加分 28 kind の順序固定）
- [x] 26 kind とも surface 実測 0 site（exact-0 pin）
- [x] ALLOWED 23 key / ERADICATED 13 key の three-way 句一致
- [x] fixture (ac1)〜(ac28) で検出・非検出の両面検証
- [x] unify 1 site の ratio 等価性（range 交差ゼロ実値確認）+
      language-detector test 68/68 GREEN
- [x] 棄却 1 class の根拠が requirements に記録済み
- [x] MW-082 の 4 mutation で RED（(a) は 3 独立面で RED・(c) は detector
  修正後に RED）・revert で 8/8 GREEN 復元

## 成果物

- `src/analysis/language-detector.ts`（unify 1 site）
- `tests/guards/dead-idiom-batch-census.test.ts`（28 kind + fixture (ac) + 負例明け渡し 1 件 + ALLOWED 1 key + ERADICATED 1 key）
- `specs/dead-idiom-batch-census/requirements.md`（REQ-418 節）
- `specs/dead-idiom-batch-census/tasks/TASK-0309.md`（本ファイル）
- `specs/dead-idiom-batch-census/tasks/overview.md`（Phase 226 節）
- `specs/speech-to-visuals/mutation-witness-ledger.md`（MW-082 行）
- `tests/guards/mutation-witness-ledger.test.ts`（PINNED_MIN_ENTRIES 75→76）
