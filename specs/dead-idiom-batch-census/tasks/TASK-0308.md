# TASK-0308: 第八回 discovery sweep — 同一 batch guard へ 12 kind 追加 + 4 class pin 前棄却（母集団不一致 2 例目・挙動一致型 2 例目） + MW-081 mutation 検証（REQ-417・batch 形式）

<!-- spine:anchor:begin -->
> **Spine anchor**: [dead-idiom batch census 要件定義](../requirements.md) REQ-417
>
> - parent: `dead-idiom-batch-census/requirements.md` REQ-417
> - role: `detailed`
> - status: `canonical_child`
<!-- spine:anchor:end -->

**要件**: [requirements.md](../requirements.md) REQ-417-001〜004
**種別**: TDD（sweep #8 測定 → 12 kind 追加 → 棄却 4 記録 → MW-081）　**推定工数**: 2h　**信頼性**: 🔵 *実測 16 candidate class（331 file・canonical `walkProductionSurface` 直接使用で計測・4 class は pin 前棄却）+ ALLOWED 同梱 4 site とも実コード読み（whisper probe は README 文書化・logger 3 行は :20 既存 row と同型）+ src の `.catch` 7 site 全数読みで複数行 noop 0 確認 + MW-081 4 mutation とも offender list で kind 帰着を確認*

## 背景

steering 契約（REQ-410 確立）の 8 回目の適用。新 spec dir・新 family・新
phase 群を開かず、**同一 guard の kind 追加のみ**で 12 class の ratchet を
得る。78 kind 状態（REQ-416 sweep #7 merge 済み）から開始。

sweep #8 は 16 candidate を計測: 10 class exact-0 pin、2 class は ALLOWED
4 site 同梱（swallowed-rejection 1・console-nondebug-sink 3）、4 class
（dom0-handler-assign / throw-bare-error / void-zero-undefined / bare
postMessage）は pin 前棄却。

## 実装

1. **`tests/guards/dead-idiom-batch-census.test.ts` に 12 kind 追加**
   （`IDIOM_KINDS` 78→90 entry・kind pin ratchet 更新）:
   date-string-parse / bare-decodeuri / dead-ua-field /
   react-legacy-root-api / react-unsafe-lifecycle /
   dangerously-set-innerhtml / bare-array-ctor / postmessage-wildcard /
   script-element-creation / instanceof-object / swallowed-rejection /
   console-nondebug-sink — いずれも行 detector 1 entry（steering 契約
   どおり class 追加 = 1 entry）
2. **liveness fixture (ab1)〜(ab12)**: 各 kind の検出・非検出境界
   （`navigator.userAgent` は useragent-sniffing・`new Array(12)` は
   sparse-array-ctor・`console.log` は console-debug-log への帰着を
   負例で分離・`componentWillUnmount` は unsafe lifecycle の検出域外）
3. **既存 fixture 負例の明け渡し（kind 分離契約 3 例目）**: (y12)
   console info/warn/error 負例 → logger facade 形、(z14)
   dangerouslySetInnerHTML 負例 → `{text}` children 形に差し替え
   （既存 kind の detect regex は 1 行も不変）
4. **ALLOWED 4 key 追加**（whisper-transcriber:121
   PROBE-DELIBERATE・logger.ts:25/30/35 LOGGER-IMPL）+
   census-pin header 18→22 key
5. **MW-081 mutation 検証**: 4 独立 mutation（untrusted-json-core 末尾へ
   `Date.parse('2026-01-01')`・`Array(16)` call 形・`console.error()`・
   `postMessage(payload, '*')` 注入）で completeness RED を実測し
   mutation-witness ledger に追記
6. **spec 同梱更新**: requirements.md に REQ-417 節・本 TASK・
   overview Phase 225

## 受け入れ基準

- [x] kind 90 entry ratchet（78→90・追加分 12 kind の順序固定）
- [x] 10 kind とも surface 実測 0 site（exact-0 pin）
- [x] ALLOWED 22 key / ERADICATED 12 key の three-way 句一致
- [x] fixture (ab1)〜(ab12) で検出・非検出の両面検証
- [x] 棄却 4 class の根拠が requirements に記録済み
- [x] MW-081 の 4 mutation で RED（各 1 failed・kind 帰着確認）・
  revert で 8/8 GREEN 復元・src 変更ゼロ

## 成果物

- `tests/guards/dead-idiom-batch-census.test.ts`（12 kind + fixture (ab) + 負例明け渡し 2 件 + ALLOWED 4 key）
- `specs/dead-idiom-batch-census/requirements.md`（REQ-417 節）
- `specs/dead-idiom-batch-census/tasks/TASK-0308.md`（本ファイル）
- `specs/dead-idiom-batch-census/tasks/overview.md`（Phase 225 節）
- `specs/speech-to-visuals/mutation-witness-ledger.md`（MW-081 行）
- `tests/guards/mutation-witness-ledger.test.ts`（PINNED_MIN_ENTRIES 74→75）
