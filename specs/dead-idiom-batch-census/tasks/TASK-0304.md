# TASK-0304: 第四回 discovery sweep — 同一 batch guard へ 7 kind 追加 + var ambient 2 site ALLOWED + 3 class pin 前棄却 + MW-077 mutation 検証（REQ-413・batch 形式）

<!-- spine:anchor:begin -->
> **Spine anchor**: [dead-idiom batch census 要件定義](../requirements.md) REQ-413
>
> - parent: `dead-idiom-batch-census/requirements.md` REQ-413
> - role: `detailed`
> - status: `canonical_child`
<!-- spine:anchor:end -->

**要件**: [requirements.md](../requirements.md) REQ-413-001〜004
**種別**: TDD（sweep #4 測定 → 7 kind 追加 → var 2 site ALLOWED → MW-077）　**推定工数**: 2.5h　**信頼性**: 🔵 *実測 10 candidate class（331 file・src と core-four の両方で計測・3 class は pin 前棄却）+ `declare global` block 開始行を実コード読みで確認*

## 背景

steering 契約（REQ-410 確立）の 4 回目の適用。新 spec dir・新 family・
新 phase 群を開かず、**同一 guard の kind 追加のみ**で 7 class の ratchet
を得る。sweep #4 は 10 candidate を計測: 6 class exact-0、
var-declaration **2 site（同一 file の `declare global {}` 内 ambient 宣言）**
ALLOWED、3 class（`.map(async`・`new Array(n)`・`Math.max(...xs)` 系）は
検出不可能型/投資不釣合型の根拠で pin 前棄却。

src 変更は**ゼロ**（ALLOWED 判断のみ・REQ-411 と同じ純 ratchet 形状）。

## 実装

1. **`tests/guards/dead-idiom-batch-census.test.ts` に 7 kind 追加**
   （`IDIOM_KINDS` 28→35 entry・kind pin ratchet 更新）:
   primitive-wrapper-ctor / arguments-index-access / regexp-literal-ctor
   （完全 literal 単一引数のみ）/ split-join-replaceall / label-statement /
   bare-encodeuri（lookbehind 付き）/ var-declaration（行頭 `var `）
2. **ALLOWED 2 key 追加**（browser-transcriber :497/:502 の AMBIENT-SYNTAX
   理由文）+ authority floor に var-declaration >= 2 + negative anchor
   （`declare global {` block 内 `var SpeechRecognition: {` 共存 pin）
3. **liveness fixture (x1)〜(x7)**: 7 kind の検出・非検出境界
   （`Number(x)` 非 boxing 除外・rest-param 除外・regex literal と動的
   `new RegExp(src, 'i')` 除外・`replaceAll`/split 単独除外・plain loop
   と object key 除外・`encodeURIComponent` 除外・`let`/`const` と
   comment 行除外）
4. **guard header に第四回 sweep 表 + 棄却 3 class 根拠 + REQ-413 kind の
   行粒度 ceiling を追記**（split-join nested-paren・regexp escaped-quote）
5. **spec 同梱更新**（requirements.md REQ-413-001〜004 + sweep #4 kind 表・
   note.md sweep #4 節・interview-record.md A-413-1〜2・tasks/overview.md
   Phase 220 行・mutation-witness ledger MW-077）
6. **MW-077 mutation 検証（REQ-413-004）**: 4 独立 mutation の RED 実測 +
   ledger section 追記

## 検証

- dead-idiom-batch-census 全 test GREEN（8 test・kind 35 entry ratchet・
  fixture (a)〜(x7)・ALLOWED 11 key / ERADICATED 7 key）
- census-artifact-three-way GREEN（ALLOWED 11 key 句の一致）
- mutation-witness ledger GREEN（MW-077 section・PINNED_MIN_ENTRIES 71）
- src 変更ゼロ・tsc 両 config exit 0

## 設計決定

1. **`var` の AMBIENT-SYNTAX ALLOWED は文脈を実証** — detector は行頭
   `var ` しか見られないため、:497/:502 が `declare global {`（:449）block
   内にあることを read で確認し、negative anchor で block 共存を pin。
   runtime `var` は unrostered RED で teeth は生存（MW-077 (a) で実証）。
2. **`.map(async` の検出不可能型棄却** — 可否は消費側で決まり行 detector
   に不可視。REQ-411-003（tsc 重複型）・REQ-412-005（挙動等価型）に続き
   棄却理由型の 3 番目「検出不可能型」+ 4 番目「投資不釣合型」を記録。
3. **`Math.max(...xs)` は棚上げでなく再計測契約化** — 25 site 全部が入力
   有界であることを read で確認した上で、per-site prose の投資を回避。
   無限界入力 site 出現時の再計測を guard header に明示的に定義。
