# TASK-0307: 第七回 discovery sweep — 同一 batch guard へ 7 kind 追加 + 3 class pin 前棄却（投資不釣合型 4/5 例目・検出不可能型 2 例目） + MW-080 mutation 検証（REQ-416・batch 形式）

<!-- spine:anchor:begin -->
> **Spine anchor**: [dead-idiom batch census 要件定義](../requirements.md) REQ-416
>
> - parent: `dead-idiom-batch-census/requirements.md` REQ-416
> - role: `detailed`
> - status: `canonical_child`
<!-- spine:anchor:end -->

**要件**: [requirements.md](../requirements.md) REQ-416-001〜004
**種別**: TDD（sweep #7 測定 → 7 kind 追加 → 棄却 3 記録 → MW-080）　**推定工数**: 2h　**信頼性**: 🔵 *実測 10 candidate class（331 file・repo src/ の .ts+.tsx と core-four の両方で計測・detector 最終形 regex で再計測・3 class は pin 前棄却）+ 棄却 3 class とも実測 site 全数（10+27+13）を読んでの根拠 + MW-080 4 mutation とも offender list で kind 帰着を確認*

## 背景

steering 契約（REQ-410 確立）の 7 回目の適用。新 spec dir・新 family・新
phase 群を開かず、**同一 guard の kind 追加のみ**で 7 class の ratchet を
得る。前段として main 未到達だった REQ-414/415（sweep #5/#6・branch
`ai/instruction-speech-to-visuals-20260825-122921-645868` に滞留）を
cherry-pick 移植（0c8fa4a0・b5d49c47 — session 229 の aa166388 と同一の
回復パターン）した 71 kind 状態から開始。

sweep #7 は 10 candidate を計測: 7 class exact-0 pin、`.length = 0`（10
site）と `.splice(…)`（27 site）は投資不釣合型（4/5 例目）、`return` in
`finally` は検出不可能型（2 例目）+ site 母集団 ≠ incident 母集団の初例
として pin 前棄却。

## 実装

1. **`tests/guards/dead-idiom-batch-census.test.ts` に 7 kind 追加**
   （`IDIOM_KINDS` 71→78 entry・kind pin ratchet 更新）:
   async-promise-executor / array-delete-hole /
   instanceof-primitive-wrapper / atob-btoa / inner-html-op-assign /
   insert-adjacent-html / sparse-array-ctor — いずれも行 detector 1 entry
   （steering 契約どおり class 追加 = 1 entry）
2. **liveness fixture (aa1)〜(aa7)**: 各 kind の検出・非検出境界
   （`instanceof Array` は既存 kind 検出域であることを負例で分離・
   同一行の atob+btoa は行単位 1 site・`new Array(1,2,3)` dense 形と
   `Array.from({length})` は除外）
3. **guard header に第七回 sweep 測定表 + 棄却根拠を追記**
   （再計測条件: 変数長 `new Array(n)` site 出現時・receiver 判別要の
   `.length = 0`/`.splice` の incident 出現時）
4. **MW-080 mutation 検証**: 4 独立 mutation（untrusted-json-core 末尾へ
   `new Promise(async …)`・`new Array(16)`・`instanceof Number`・`atob()`
   注入）で completeness RED を実測し mutation-witness ledger に追記
5. **spec 同梱更新**: requirements.md に REQ-416 節・本 TASK・
   overview Phase 224

## 受け入れ基準

- [x] kind 78 entry ratchet（71→78・追加分 7 kind の順序固定）
- [x] 7 kind とも surface 実測 0 site（exact-0 pin・ALLOWED/ERADICATED
  roster は 18/12 不変・three-way 句一致）
- [x] fixture (aa1)〜(aa7) で検出・非検出の両面検証
- [x] 棄却 3 class の根拠が guard header + requirements に記録済み
- [x] MW-080 の 4 mutation で RED（各 1 failed・kind 帰着確認）・
  revert で 8/8 GREEN 復元・src 変更ゼロ

## 成果物

- `tests/guards/dead-idiom-batch-census.test.ts`（7 kind + fixture (aa) + header 測定表）
- `specs/dead-idiom-batch-census/requirements.md`（REQ-416 節）
- `specs/dead-idiom-batch-census/tasks/TASK-0307.md`（本ファイル）
- `specs/dead-idiom-batch-census/tasks/overview.md`（Phase 224 節）
- `specs/speech-to-visuals/mutation-witness-ledger.md`（MW-080 行）
- `tests/guards/mutation-witness-ledger.test.ts`（PINNED_MIN_ENTRIES 73→74）
