# TASK-0310: 第十回 discovery sweep — 同一 batch guard へ 7 kind 追加 + 1 class pin 前棄却（記録済み判断衝突型の初例） + MW-083 mutation 検証（REQ-419・batch 形式）

<!-- spine:anchor:begin -->
> **Spine anchor**: [dead-idiom batch census 要件定義](../requirements.md) REQ-419
>
> - parent: `dead-idiom-batch-census/requirements.md` REQ-419
> - role: `detailed`
> - status: `canonical_child`
<!-- spine:anchor:end -->

**要件**: [requirements.md](../requirements.md) REQ-419-001〜003
**種別**: TDD（sweep #10 測定 → 7 kind 追加 → 棄却 1 記録 → MW-083）　**推定工数**: 1.5h　**信頼性**: 🔵 *実測 8 candidate class（331 file・canonical `walkProductionSurface` 直接使用で計測・REQ-418 merge 後 tree が対象）+ 棄却 1 class は REQ-418 fixture (ac26) の意図的負例 pin との突き合わせで判断 + MW-083 4 mutation とも offender list で kind 帰着を確認*

## 背景

steering 契約（REQ-410 確立）の 10 回目の適用。新 spec dir・新 family・新
phase 群を開かず、**同一 guard の kind 追加のみ**で 7 class の ratchet を
得る。118 kind 状態（REQ-418 sweep #9 merge 済み）から開始。

sweep #10 は 8 candidate を計測: 7 class exact-0 pin、1 class
（legacy-get-elements）は pin 前棄却 — REQ-418 の fixture (ac26) が
`getElementsByClassName` を「live HTMLCollection は現行 API」と**意図的な
負例**として pin 済みのため、kind 追加は記録済み判断と直接矛盾する
（記録済み判断衝突型の初例）。同一計測ラウンドの 5 class
（`.outerHTML` 読み取り形 / `initEvent` 単独 / `.cancelBubble` 読み取り形 /
`.split('')` / `.then(null, …)`）は REQ-418 kind が同一 shape を既に管轄
（重複）のため候補から除外。

## 実装

1. **`tests/guards/dead-idiom-batch-census.test.ts` に 7 kind 追加**
   （`IDIOM_KINDS` 118→125 entry・kind pin ratchet 更新）:
   filter-index-zero / inline-handler-attr / instanceof-function /
   object-setprototypeof / exec-command-legacy / setimmediate-call /
   process-nexttick — いずれも行 detector 1 entry（steering 契約
   どおり class 追加 = 1 entry）
2. **liveness fixture (ad1)〜(ad7)**: 各 kind の検出・非検出境界
   （`xs.filter(…)[i]` 変数 index は検出域外・`handlers.setImmediate =`
   member 形は検出域外・`instanceof MyClass` と `execScript(` は既存
   kind（instanceof-primitive-wrapper / window-execscript）の shape と
   分離 — 負例は既存 kind の正検出形を含まない）
3. **rosters 不変**: 7 kind とも exact-0 のため ALLOWED 23 key /
   ERADICATED 13 key・census-pin header も不変
4. **MW-083 mutation 検証**: 4 独立 mutation（untrusted-json-core 末尾へ
   `xs.filter(isNum)[0]`・`document.execCommand('copy')`・
   `setImmediate(callback)`・`process.nextTick(callback)` 注入）で
   completeness RED を実測し mutation-witness ledger に追記
5. **spec 同梱更新**: requirements.md に REQ-419 節・本 TASK・
   overview Phase 227

## 受け入れ基準

- [x] kind 125 entry ratchet（118→125・追加分 7 kind の順序固定）
- [x] 7 kind とも surface 実測 0 site（exact-0 pin）
- [x] ALLOWED 23 key / ERADICATED 13 key の three-way 句一致（不変）
- [x] fixture (ad1)〜(ad7) で検出・非検出の両面検証
- [x] 棄却 1 class（legacy-get-elements・記録済み判断衝突型）の根拠が
  requirements に記録済み
- [x] MW-083 の 4 mutation で RED（各 1 failed・kind 帰着確認）・
  revert で 8/8 GREEN 復元・`git status --short src/` 0 file
  （src 変更ゼロ sweep #10）

## 成果物

- `tests/guards/dead-idiom-batch-census.test.ts`（7 kind + fixture (ad)）
- `specs/dead-idiom-batch-census/requirements.md`（REQ-419 節）
- `specs/dead-idiom-batch-census/tasks/TASK-0310.md`（本ファイル）
- `specs/dead-idiom-batch-census/tasks/overview.md`（Phase 227 節）
- `specs/speech-to-visuals/mutation-witness-ledger.md`（MW-083 行）
- `tests/guards/mutation-witness-ledger.test.ts`（PINNED_MIN_ENTRIES 76→77）
