# TASK-0310: 第十回 discovery sweep — 同一 batch guard へ 34 kind 追加 + ALLOWED 4 key + 2 class pin 前棄却（挙動一致型 3 例目・機能本質型 1 例目） + 計測・fixture 発見の detector 初稿 bug 3 件修正 + MW-083 mutation 検証（REQ-419・batch 形式）

<!-- spine:anchor:begin -->
> **Spine anchor**: [dead-idiom batch census 要件定義](../requirements.md) REQ-419
>
> - parent: `dead-idiom-batch-census/requirements.md` REQ-419
> - role: `detailed`
> - status: `canonical_child`
<!-- spine:anchor:end -->

**要件**: [requirements.md](../requirements.md) REQ-419-001〜004
**種別**: TDD（sweep #10 測定 → 34 kind 追加 → 棄却 2 記録 → detector 初稿修正 3 件 → MW-083）　**推定工数**: 2h　**信頼性**: 🔵 *実測 36 candidate class（331 file・canonical `walkProductionSurface` 直接使用で計測・2 class は pin 前棄却）+ detector 初稿 bug 3 件は計測 2 件（clean surface の偽陽性）+ liveness fixture 1 件（正面例の検出漏れ）で再現確認 + ALLOWED 4 site は実コード読み（表示専用壁時計）+ MW-083 4 mutation とも offender list で kind 帰着を確認*

## 背景

steering 契約（REQ-410 確立）の 10 回目の適用。新 spec dir・新 family・新
phase 群を開かず、**同一 guard の kind 追加のみ**で 34 class の ratchet を
得る。118 kind 状態（REQ-418 sweep #9 merge 済み）から開始。

sweep #10 は 36 candidate を計測: 33 class exact-0 pin、1 class は ALLOWED
4 site 同梱（localedatestring-bare — dashboard 表示専用）、2 class
（inner-text-assign・array-literal-concat）は pin 前棄却。

## 実装

1. **`tests/guards/dead-idiom-batch-census.test.ts` に 34 kind 追加**
   （`IDIOM_KINDS` 118→152 entry・kind pin ratchet 更新）:
   doc-execcommand / window-clipboard-data / legacy-buffer-ctor /
   node-url-parse / node-querystring / nonstandard-setimmediate /
   node-process-nexttick / promise-deferred / object-observe /
   system-import / x-domain-request / activex-object /
   console-devtools-sink / getcomputedstyle-null-arg /
   prefixed-matches-selector / scroll-into-view-if-needed /
   react-force-update / react-prop-types / importscripts-global /
   error-capture-stack-trace / self-ternary-default /
   navigator-ms-prefixed / process-binding / window-external /
   getbox-object-for / mutation-event-name / window-status-assign /
   element-fire-event / children-map-direct / event-path-access /
   node-createcipher / localedatestring-bare / splice-indexof-remove /
   unstable-react-api — いずれも行 detector 1 entry（steering 契約
   どおり class 追加 = 1 entry）
2. **liveness fixture (ad1)〜(ad34)**: 各 kind の検出・非検出境界
   （`new Buffer(` と call 形は同一 regex で両検出・`console.time` は
   log/info 系 kind と不交差・`a.b ? a.b : c` の property-path 形も検出・
   `createDecipher` 正例が初稿 regex の検出漏れを RED で発見）
3. **detector 初稿修正 3 件（REQ-419-002）**: self-ternary-default は
   reserved-word consequent 排除（`null ? null :` 偽陽性・計測発見）・
   event-path-access は lookbehind 追加（`largestFile.path` 偽陽性・
   計測発見）・node-createcipher は `create(?:Decipher|Cipher)\(` へ
   （`De`+小文字 cipher 検出漏れ・liveness fixture 発見）
4. **ALLOWED 4 key 追加**: localedatestring-bare の src 実測 4 site
   （AdminAnalyticsDashboard:56/:606・FrameworkDashboard:494・
   Iteration43Interface:106 — 全て人間向け壁時計表示・DISPLAY-ONLY）
   + floor ≥ 4 + census-pin header 23→27 key
5. **MW-083 mutation 検証**: 4 独立 mutation（untrusted-json-core 末尾へ
   `document.execCommand('copy')`・`url.parse(rawUrl)`・
   `console.time('sweep')`・`children.map((c) => c)` 注入）で
   completeness RED を実測し mutation-witness ledger に追記
6. **spec 同梱更新**: requirements.md に REQ-419 節・本 TASK・
   overview Phase 227

## 受け入れ基準

- [x] kind 152 entry ratchet（118→152・追加分 34 kind の順序固定）
- [x] 33 kind とも surface 実測 0 site（exact-0 pin）
- [x] ALLOWED 27 key / ERADICATED 13 key の three-way 句一致
- [x] fixture (ad1)〜(ad34) で検出・非検出の両面検証
- [x] detector 初稿 bug 3 件の修正と境界固定（REQ-419-002）
- [x] 棄却 2 class の根拠が requirements に記録済み
- [x] MW-083 の 4 mutation で RED（各 1 failed・kind 帰着確認）・
      revert で 8/8 GREEN 復元・`git status --short src/` 空

## 成果物

- `tests/guards/dead-idiom-batch-census.test.ts`（34 kind + fixture (ad) + ALLOWED 4 key + floor）
- `specs/dead-idiom-batch-census/requirements.md`（REQ-419 節）
- `specs/dead-idiom-batch-census/tasks/TASK-0310.md`（本ファイル）
- `specs/dead-idiom-batch-census/tasks/overview.md`（Phase 227 節）
- `specs/speech-to-visuals/mutation-witness-ledger.md`（MW-083 行）
- `tests/guards/mutation-witness-ledger.test.ts`（PINNED_MIN_ENTRIES 76→77）
