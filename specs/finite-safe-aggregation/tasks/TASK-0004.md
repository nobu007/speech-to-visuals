# TASK-0004: diagram-detector score mean + pattern max の移行

**タスクID**: TASK-0004
**タスクタイプ**: TDD
**推定工数**: 3時間
**フェーズ**: Phase 2 - サイト移行（外部起因 T1）
**信頼性レベル**: 🔵 *REQ-003 一次リスト・D5 T1（`diagram-detector.ts:1344` と `:436`）*

## 関連文書

- **概要**: [📋 overview.md](overview.md)
- **要件定義**: [📋 requirements.md](../requirements.md) - REQ-003, REQ-004, REQ-102, EDGE-102
- **設計文書**: [📐 architecture.md](../architecture.md) - D5 ウェーブ 3

## タスク概要

`src/analysis/diagram-detector.ts:1344`（testResults score mean）と `:436`
（`Math.max(...)` spread — NaN 伝播）を `safeMean` / `safeMax` へ移行する（design ウェーブ 3）。
score は gate 判定（TEST_QUALITY_THRESHOLD 比較）の入力で、NaN 化すると gate が
silent-fail する sink。

## 依存タスク

- **前提タスク**: [TASK-0001](TASK-0001.md), [TASK-0002](TASK-0002.md)
- **後続タスク**: [TASK-0008](TASK-0008.md)

## 完了条件

- [ ] score mean サイトが `safeMean` へ移行済み（D4 形式 A/B は形状による）
- [ ] `Math.max(...)` spread サイトが `safeMax`（ループ実装）へ移行済み — spread 除去で EDGE-102 同時解消
- [ ] 数値デルタオラクル（有限入力で旧式と `Object.is` 等価）+ spread 破綻対比 assertion 追加
- [ ] commit message に `behavior change:` 明記（NaN 混入時のみ変化）
- [ ] diagram-detector 関連テスト + guards suite GREEN

---

## 主要実装項目

### spread 除去 🔵

**信頼性**: 🔵 *EDGE-102・D3 の例外項より*

**実装ファイル**: `src/analysis/diagram-detector.ts`

`Math.max(...arr)` → `safeMax(arr)`。等価オラクルは「通常サイズ配列で値等価」+
「巨大配列で helper が有限（旧は RangeError/NaN）」の 2 assertion（D3）。

---

## 基本的なテスト要件

### gate 下流の有限性 🔵

**信頼性**: 🔵 *dataflow.md 機能2・sink 2（品質 gate 判定）より*

- NaN 混入 score に対し mean が有限を返し、gate 比較が `false`/`true` のいずれかの
  確定値を取ること（NaN 比較の silent-fallthrough が無くなる）ことを確認

---

## 実装手順

### TDDタスクの場合

1. `/tsumiki:tdd-red` → 2. `/tsumiki:tdd-green` → 3. `/tsumiki:tdd-verify-complete`

---

## 注意事項

- 旧 spread の NaN 伝播挙動（`Math.max(NaN, 1)` = NaN）をテストに pin してから移行するとデルタが明確になる

---

## 信頼性レベルサマリー

- **総項目数**: 3項目
- 🔵 **青信号**: 3項目 (100%)
- 🟡 **黄信号**: 0項目 (0%)
- 🔴 **赤信号**: 0項目 (0%)

**品質評価**: 高品質
