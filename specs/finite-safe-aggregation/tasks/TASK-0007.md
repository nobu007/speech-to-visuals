# TASK-0007: T2/T3 確定分（export / performance / visualization）の移行

<!-- spine:anchor:begin -->
> **Spine anchor**: [speech-to-visuals アーキテクチャ設計](../../speech-to-visuals/architecture.md)
>
> - parent: `speech-to-visuals/architecture.md`
> - role: `detailed`
> - status: `canonical_child`
<!-- spine:anchor:end -->

**タスクID**: TASK-0007
**タスクタイプ**: TDD
**推定工数**: 4時間
**フェーズ**: Phase 2 - サイト移行（sweep 判定分）
**信頼性レベル**: 🟡 *移行対象の最終構成が TASK-0001 の sweep 結果依存（D5 の再分類保留サイトを含む）*

## 関連文書

- **概要**: [📋 overview.md](overview.md)
- **要件定義**: [📋 requirements.md](../requirements.md) - REQ-003, REQ-004, REQ-402, REQ-102
- **設計文書**: [📐 architecture.md](../architecture.md) - D5 T2/T2b/T3・ウェーブ 6

## タスク概要

TASK-0001 の sweep で移行と確定した T2/T3 サイトを移行する（design ウェーブ 6）。
対象候補と設計時の一次判定:

| ファイル | サイト | 一次判定 |
|---|---|---|
| `src/export/production-exporter.ts` | :687, :780 | 移行（時間差/frameCount 系） |
| `src/export/production-exporter.ts` | :437, :782 | sweep 判定（T3 端数） |
| `src/performance/intelligent-cache.ts` | :630, :653 | 対象外候補（`.length` 派生 / match count — REQ-402） |
| `src/performance/intelligent-cache.ts` | :693-695 | T2b（cosine similarity、`keywordVector` 生成元確認後に判定） |
| `src/visualization/complex-layout-engine.ts` | :968/969/988 | T2（`x \|\| 0` 済み箇所を含む、生成元確認） |

## 依存タスク

- **前提タスク**: [TASK-0001](TASK-0001.md), [TASK-0002](TASK-0002.md)
- **後続タスク**: [TASK-0008](TASK-0008.md)

## 完了条件

- [x] sweep で「移行」と確定した全サイトが helper 委譲済み — exporter 687（`safeSum/1000`。**SUM であって mean ではない** — 初回 safeMean 化はミスで即修正）・780（safeMean）。commit fe4af157
- [x] 「対象外」と確定したサイトに、有限構造保証の根拠（生成元コード位置）が記録されている — sweep-20260815.md（intelligent-cache 693-695 は createKeywordVector `count/totalWords`、exporter 437 は renderer 内部カウンタ、782 は `|| 0` ガード）
- [x] 各移行サイトに数値デルタオラクル。挙動変化があるサイトは `behavior change:` 明記 — tests/export/production-exporter-safe-aggregation-migration.test.ts（200 seeded ×2 + source anchor）。780 は behavior change、687 は全入力で同一
- [x] `.length` 系・個数集計が移行されていない（REQ-402 遵守）— keyphrase/match/frameCount 合計は全て旧形状のまま
- [x] 関連テスト + guards suite GREEN — 4 スイート / 88 テスト + frozen-literal 20/20

---

## 主要実装項目

### T2 の生成元確認 🔵

**信頼性**: 🔵 *D5 T2「有限構造保証が確認できたものは移行対象外」より*

内部生成値は生成元を遡って有限性を確認する。確認不能なものは移行する（安全側）。
`production-exporter.ts:780` の `endTime! - startTime!` は non-null assertion を含むため、
NaN 伝播経路の有無を確認する（09f: Date 系は Date.now 由来で有限、ただし assertion 別リスク）。

### 対象外宣言の記録 🔵

**信頼性**: 🔵 *REQ-402 より*

対象外サイトは TASK-0001 のリストに理由を付して残す。TASK-0008 の registry は
ファイルリスト scoped にすることで誤検出を避ける設計と整合させる。

---

## 基本的なテスト要件

### 数値デルタオラクル（移行分のみ）🔵

**信頼性**: 🔵 *REQ-004 より*

- 各移行サイト: 有限入力で旧式と `Object.is` 等価。形式 B（添字依存・内積系）も
  `safeSum(vec1.map((v, i) => v * vec2[i]))` の形で対応

---

## 実装手順

### TDDタスクの場合

1. `/tsumiki:tdd-red` → 2. `/tsumiki:tdd-green` → 3. `/tsumiki:tdd-verify-complete`

---

## 注意事項

- 本タスクの対象ファイル数は sweep 結果次第で増減する。コミットは `fix(export/performance/visualization)` の 1 コミットまたは層別分割のどちらでもよいが、デルタオラクルはサイト単位で残す

---

## 信頼性レベルサマリー

- **総項目数**: 3項目
- 🔵 **青信号**: 3項目 (100%)
- 🟡 **黄信号**: 0項目 (0%)
- 🔴 **赤信号**: 0項目 (0%)

**品質評価**: 高品質（タスク記述は 🔵 だが、対象範囲の確定度が 🟡 のためタスク全体は 🟡）
