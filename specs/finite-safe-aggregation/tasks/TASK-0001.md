# TASK-0001: 移行対象サイトの sweep 再確定

**タスクID**: TASK-0001
**タスクタイプ**: DIRECT
**推定工数**: 2時間
**フェーズ**: Phase 1 - 基盤（helper 前調査）
**信頼性レベル**: 🟡 *requirements.md REQ-003 の行番号が 2026-08-15 時点の実調査であり、行ずれ・増減の可能性があるため*

## 関連文書

- **概要**: [📋 overview.md](overview.md)
- **要件定義**: [📋 requirements.md](../requirements.md) - REQ-003
- **設計文書**: [📐 architecture.md](../architecture.md) - D5（Tier 分類）

## タスク概要

REQ-003 の一次リスト（`reduce((a,b)=>a+b,0)` 系 125 サイト + `Math.max(...)` spread 4 サイト）を
`grep -rn` で再 sweep し、Tier 分類（T1 外部起因 / T2 内部生成 / T3 端数）を行単位で再確定する。
移行タスク（TASK-0003〜0007）の入力となる正本リストを作る。

## 依存タスク

- **前提タスク**: なし
- **後続タスク**: [TASK-0002](TASK-0002.md)（helper 実装は sweep と並行可だが、移行は本リスト確定待ち）、[TASK-0003](TASK-0003.md)〜[TASK-0007](TASK-0007.md)

## 完了条件

- [x] `grep -rn "reduce((a\|reduce((sum\|reduce((total\|reduce((acc" src/` 系の sweep で 125 サイト台の現況を再取得し、要件一次リストとの差分（行ずれ・増減）を記録している — 2026-08-15 実施、reduce 134 実サイト（+9）/ spread 44（+40）。正本: [sweep-20260815.md](sweep-20260815.md)
- [x] T1/T2/T2b/T3 の Tier 判定が architecture.md D5 の表に従い行単位で確定している（特に再分類保留の `intelligent-cache.ts:630/653/693-695`、`production-exporter.ts:437/782`）— intelligent-cache 693-695 は keywordVector 生成元（`count/totalWords`、totalWords>0 ガード）で有限構造保証 → 対象外。exporter 437/782 もガード/内部生成で対象外。移行対象は T1 9 + T3 2 = 11 サイト
- [x] 確定リストを `specs/finite-safe-aggregation/tasks/` 配下の作業メモ（または本 overview の追記）に残している — [sweep-20260815.md](sweep-20260815.md)

---

## 主要実装項目

### sweep の実行 🔵

**信頼性**: 🔵 *architecture.md D5「実装フェーズ冒頭に sweep（`grep -rn`）でリストを再確定してから着手」より*

⚠️ RTK hook が `rg` を mangle するため `grep -rn` を使用すること。

検出パターン（frozen-literal-rules と同じ形状を採用）:

```sh
grep -rn "reduce((a, b) => a + b\|reduce((a,b)=>a+b\|reduce((sum,\|reduce((total,\|reduce((acc," src/ | grep -v test
grep -rn "Math\.max(\.\.\.\|Math\.min(\.\.\." src/ | grep -v test
```

**実装ファイル**: なし（調査のみ。成果物はメモ）

### Tier 判定と対象外宣言 🔵

**信頼性**: 🔵 *architecture.md D5 の Tier 表・REQ-402（`.length` 系・個数集計は対象外）より*

- T2 のうち生成元確認で有限構造保証が確認できたサイトは「対象外」として理由付きで記録する（REQ-402 の精神）
- `intelligent-cache.ts:693-695`（cosine similarity）は `keywordVector` 構築元を確認して T1/T2 を判定する

---

## 基本的なテスト要件

### sweep 結果の再現性 🔵

**信頼性**: 🔵 *REQ-003 より*

- 検証コマンドの出力（サイト数・行番号）を記録し、TASK-0008 の registry sweep pin の基準値とする

---

## 実装手順

### DIRECTタスクの場合

1. `/tsumiki:direct-setup` - sweep 実行
2. `/tsumiki:direct-verify` - Tier 分類表の完成確認

---

## 信頼性レベルサマリー

- **総項目数**: 3項目
- 🔵 **青信号**: 3項目 (100%)
- 🟡 **黄信号**: 0項目 (0%)
- 🔴 **赤信号**: 0項目 (0%)

**品質評価**: 高品質（タスク自体の信頼性は 🔵。ただし sweep 前提の一次リスト自体が 🟡）
