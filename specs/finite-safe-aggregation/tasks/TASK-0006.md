# TASK-0006: enhanced-error-recovery timestamp min/max spread の移行

**タスクID**: TASK-0006
**タスクタイプ**: TDD
**推定工数**: 3時間
**フェーズ**: Phase 2 - サイト移行（外部起因 T1）
**信頼性レベル**: 🔵 *REQ-003 一次リスト（`enhanced-error-recovery.ts:1452,1899-1900`）・EDGE-102*

## 関連文書

- **概要**: [📋 overview.md](overview.md)
- **要件定義**: [📋 requirements.md](../requirements.md) - REQ-003, REQ-004, REQ-102, EDGE-102
- **設計文書**: [📐 architecture.md](../architecture.md) - D5 ウェーブ 5（spread 除去を含む）

## タスク概要

`src/quality/enhanced-error-recovery.ts:1452,1899-1900` の `Math.max(...)` /
`Math.min(...)` spread 集計（timestamp 系）を `safeMax` / `safeMin` へ移行する
（design ウェーブ 5）。NaN 伝播に加え巨大配列での stack overflow を同時に解消する。

## 依存タスク

- **前提タスク**: [TASK-0001](TASK-0001.md), [TASK-0002](TASK-0002.md)
- **後続タスク**: [TASK-0008](TASK-0008.md)

## 完了条件

- [x] spread 集計サイト（sweep 確定分）が `safeMax`/`safeMin` のループ実装へ移行済み — 1452/1899-1900。commit 4a3c2da3
- [x] `safeMin` が有限要素ゼロで `+Infinity` を返さないこと（fallback 0）を assertion — wave-1 spec matrix（全要素非有限 → 0）+ wave-5 `safeMin([NaN], now) === now`。1899 は空分岐で `now` を明示 fallback 指定
- [x] 数値デルタオラクル（有限入力で `Object.is` 等価）追加 — tests/quality/error-recovery-safe-minmax-migration.test.ts（200 seeded ケース ×2 + source anchor）
- [x] commit message に `behavior change:` 明記（NaN 混入時のみ変化）— 4a3c2da3
- [x] error-recovery 関連テスト + guards suite GREEN — 24 スイート / 701 テスト

---

## 主要実装項目

### min/max の NaN 伝播 pin 🔵

**信頼性**: 🔵 *EDGE-102・D3 例外項より*

**実装ファイル**: `src/quality/enhanced-error-recovery.ts`

`Math.min(NaN, 5)` = NaN、`Math.max(...[])` = `-Infinity` の旧挙動をテストに pin してから
移行する。timestamp が 1 件でも欠損すると回復窓計算全体が NaN 化する現状をデルタで記録する。

---

## 基本的なテスト要件

### 数値デルタ + spread 破綻 🔵

**信頼性**: 🔵 *REQ-004・EDGE-102 より*

- 有限入力: 旧式と値等価
- 巨大配列（1e5+）: helper 有限・旧 spread RangeError の対比

---

## 実装手順

### TDDタスクの場合

1. `/tsumiki:tdd-red` → 2. `/tsumiki:tdd-green` → 3. `/tsumiki:tdd-verify-complete`

---

## 注意事項

- 09f 教訓（time-origin mismatch）に注意: timestamp 系の非有限は Date 系フィールドでは
  通常発生しないが、`performance.now()` 混在経路では発生し得る。sweep で発生経路を確認する

---

## 信頼性レベルサマリー

- **総項目数**: 3項目
- 🔵 **青信号**: 3項目 (100%)
- 🟡 **黄信号**: 0項目 (0%)
- 🔴 **赤信号**: 0項目 (0%)

**品質評価**: 高品質
