# TASK-0002: finite-safe helper 4 関数の実装と仕様・fuzz オラクル

<!-- spine:anchor:begin -->
> **Spine anchor**: [speech-to-visuals アーキテクチャ設計](../../speech-to-visuals/architecture.md)
>
> - parent: `speech-to-visuals/architecture.md`
> - role: `detailed`
> - status: `canonical_child`
<!-- spine:anchor:end -->

**タスクID**: TASK-0002
**タスクタイプ**: TDD
**推定工数**: 4時間
**フェーズ**: Phase 1 - 基盤（helper 実装）
**信頼性レベル**: 🔵 *architecture.md D1〜D3/D6 に API シグネチャ・bitwise 等価構造・テスト構成が全て確定済み*

## 関連文書

- **概要**: [📋 overview.md](overview.md)
- **要件定義**: [📋 requirements.md](../requirements.md) - REQ-001, REQ-002, REQ-004, REQ-005
- **設計文書**: [📐 architecture.md](../architecture.md) - D1（API 仕様の正本はここ）
- **データフロー**: [🔄 dataflow.md](../dataflow.md) - 機能1・エラーハンドリングフロー

## タスク概要

`src/lib/metrics-utils.ts` に `safeSum` / `safeMean` / `safeMax` / `safeMin` を追加する
（design ウェーブ 1。移行なし・挙動変化ゼロ）。仕様テスト（5 系 × 4 関数）と
seed 固定 fuzz オラクルを同時に作る。

## 依存タスク

- **前提タスク**: なし（TASK-0001 と並行可）
- **後続タスク**: [TASK-0003](TASK-0003.md)〜[TASK-0007](TASK-0007.md)（全移行タスクが本 helper に依存）

## 完了条件

- [x] 4 関数が architecture.md D1 のシグネチャ（`readonly number[]` 受け・`fallback=0`・selector 引数なし）どおり実装されている — commit 6fa7d591
- [x] 仕様テスト: 有限のみ / NaN 混入 / ±Infinity 混入 / 空配列 / 全要素非有限 の 5 系 × 4 関数で、戻り値が常に有限（空・全非有限は 0）
- [x] fuzz オラクル: `createLayoutRng`（mulberry32）seed 固定で数百ケース。有限入力は旧 inline 式（テスト内複製）と `Object.is` 等価（-0 含む）、非有限混入では helper のみ有限 — 300 ケース ×2 クラス ×4 関数。実装ノート: `Math.max(-0,+0)=+0` / `Math.min(+0,-0)=-0` のゼロ符号規約を比較ループで再現しないと bitwise 等価が崩れる（比較演算子は -0 を +0 に丸める）
- [x] spread 破綻回帰: 要素数 1e5+ で `safeMax` が有限値（旧 `Math.max(...arr)` は RangeError）— 2e5 要素で旧式 RangeError・helper 有限を対比 assertion
- [x] `NODE_OPTIONS='--experimental-vm-modules --max-old-space-size=4096' npx jest --config jest.config.cjs tests/guards/finite-safe-aggregation` が GREEN — 36/36。tsc --noEmit も clean

---

## 主要実装項目

### helper 実装（単一パス・ループ）🔵

**信頼性**: 🔵 *architecture.md D1/D3・NFR（`Math.max(...spread)` 禁止）より*

**実装ファイル**: `src/lib/metrics-utils.ts`

```ts
/** 非有限要素を除外した sum。有限要素ゼロなら fallback（既定 0）。単一パス O(n)。 */
export function safeSum(values: readonly number[], fallback: number = 0): number {
  let acc = 0;
  let any = false;
  for (const v of values) {
    if (Number.isFinite(v)) { acc += v; any = true; }
  }
  return any ? acc : fallback;
}
// safeMean: 分母は「有限要素数」（元配列長ではない）
// safeMax/safeMin: 比較ループ。有限要素ゼロなら fallback（±Infinity を返さない）
```

要点:
- 要素判定は `Number.isFinite`（`sanitizeFinite` を要素に適用しない — D2 の共存方針）
- 加算順序は先頭から（旧 reduce と同一演算列 → 有限入力で bitwise 等価、`-0` 保持）
- 例外送出ゼロ（dataflow エラーハンドリングフロー）

### fuzz オラクル 🔵

**信頼性**: 🔵 *REQ-005・architecture.md D6-3 より*

**実装ファイル**: `tests/guards/finite-safe-aggregation.test.ts`

`createLayoutRng`（`src/visualization/layout-rng.ts`）を seed 固定で使用。
**`Math.random` は不使用**（registry 禁止・REQ-005）。

### spread 破綻回帰 🔵

**信頼性**: 🔵 *EDGE-102 より*

旧 `Math.max(...arr)` はテスト内で `try { Math.max(...bigArr) } catch` として
RangeError になること自体を assertion し、helper は有限値を返すことを対比する。

---

## 基本的なテスト要件

### 仕様テスト 5 系 × 4 関数 🔵

**信頼性**: 🔵 *REQ-001/002/101・要件定義の受け入れ基準より*

- Given: helper がある / When: 4 系入力 / Then: 常に finite（空・全非有限は 0）

### fuzz 等価 🔵

**信頼性**: 🔵 *REQ-004/005 より*

- 有限入力: 旧式と bitwise 等価（`Object.is`、-0 含む）
- 非有限混入: helper のみ有限を返す

---

## 実装手順

### TDDタスクの場合

1. `/tsumiki:tdd-requirements TASK-0002` - 詳細要件定義
2. `/tsumiki:tdd-testcases` - テストケース作成
3. `/tsumiki:tdd-red` - テスト実装（失敗）
4. `/tsumiki:tdd-green` - 最小実装
5. `/tsumiki:tdd-refactor` - リファクタリング
6. `/tsumiki:tdd-verify-complete` - 品質確認

---

## 注意事項

- コミットメッセージ: `feat(lib): finite-safe aggregation helpers`（移行なし・挙動変化ゼロ → `behavior change:` 不要）
- worktree でテスト実行前に `ln -s /home/jinno/speech-to-visuals/node_modules node_modules` が必要な場合がある

---

## 信頼性レベルサマリー

- **総項目数**: 5項目
- 🔵 **青信号**: 5項目 (100%)
- 🟡 **黄信号**: 0項目 (0%)
- 🔴 **赤信号**: 0項目 (0%)

**品質評価**: 高品質
