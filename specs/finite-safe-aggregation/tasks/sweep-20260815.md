# TASK-0001 sweep 再確定メモ（2026-08-15、wave-1 コミット 6fa7d591 時点）

REQ-003 一次リスト（125 サイト + spread 4 サイト）を行単位で再確定した正本。
TASK-0003〜0007（ウェーブ 2-6）は本リストの行番号に従う。
TASK-0008 の registry sweep pin 基準値も本メモのカウント。

## sweep コマンドと現況

```sh
grep -rn "reduce((a, b) => a + b\|reduce((a,b)=>a+b\|reduce((sum,\|reduce((total,\|reduce((acc," src/ | grep -v test
grep -rn "Math\.max(\.\.\.\|Math\.min(\.\.\." src/ | grep -v test
```

- reduce 系: **136 ヒット**（要件時 125 → 増減 +11。うち 2 件は `src/lib/metrics-utils.ts`
  の doc コメント内コピーで実コード外 → **実サイト 134**）
- `Math.max(...)/Math.min(...)` spread 系: **47 ヒット**（同 3 件 doc コメント → **実サイト 44**）

増減の主因: 要件調査以降の stochastic-seeding ラウンド（round 17）で visualization 系の
reduce/spread サイトが増減。行番号のずれは下記正本リストで解消済み。

## Tier 正本リスト（移行対象のみ行番号付き）

### T1 外部起因・未ガード → 移行（ウェーブ 2-5）

| サイト | 現行 | 旧行番号 | 備考 |
|---|---|---|---|
| `src/analysis/llm-service.ts:724` | `reduce((a,b)=>a+b,0)/length` | 724 | flash 応答時間 mean（getStats）。時刻は内部計測（Date.now 差分）だが適応タイムアウト意思決定の上流 → 設計どおり T1 扱いで移行 |
| `src/analysis/llm-service.ts:728` | 同上 | 728 | pro 応答時間 mean |
| `src/analysis/llm-service.ts:732` | 同上 | 732 | 全応答時間 mean |
| `src/analysis/llm-service.ts:795-796` | 同上 ×2 | 795-796 | calculateTimeSavings の avgFlash/avgPro |
| `src/analysis/diagram-detector.ts:1344` | `reduce((sum,r)=>sum+r.score,0)/length` | 1344 | score は LLM statistical analysis 由来（外部起因） |
| `src/analysis/diagram-detector.ts:436` | `Math.max(...patternScores)` | 436 | spread 併用サイト（maxPossibleScore 正規化） |
| `src/analysis/scene-segmenter.ts:792` | `reduce((sum,seg)=>sum+(endMs-startMs),0)/length` | 792 | セグメント境界は transcription（Whisper）由来 |
| `src/quality/enhanced-error-recovery.ts:1452` | `Math.max(...similarErrors.map(e=>e.timestamp))` | 1452 | timestamp spread |
| `src/quality/enhanced-error-recovery.ts:1899-1900` | `Math.min/max(...allErrors.map(e=>e.timestamp))` | 1899-1900 | timestamp spread ×2 |

### T2b 再分類保留 → **確定: 対象外**（有限構造保証）

- `src/performance/intelligent-cache.ts:693-695`（cosine similarity）:
  `keywordVector` は `createKeywordVector`（`wordCounts.get(word) || 0` を
  `totalWords > 0` ガードで除算）で構築 — 全要素が `count / totalWords` の有限値。
  fingerprint 由来だが生成元で構造的に有限 → **対象外**（REQ-402 の精神）。

### T2 内部生成 → sweep 判定確定

| サイト | 判定 | 理由 |
|---|---|---|
| `intelligent-cache.ts:630` | 対象外 | factors = `content.length`・match count 派生の有限値 |
| `intelligent-cache.ts:653` | 対象外 | match count 合計（個数集計、REQ-402） |
| `src/quality/enhanced-error-recovery.ts:355-357,420,471,821,1971` | 移行（ウェーブ 5 傍流） | `averageResponseTime` 等は引数 metrics 由来（呼び側は内部計測だが外部境界を跨ぐ interface 値）。設計の enhanced-error-recovery ウェーブ内で随時 |
| `src/quality/enhanced-error-recovery.ts:1864,1885,1893-1894` | 対象外 | gaps/個数合計・`typeCounts` 系（内部生成の整数集計） |
| visualization 系 bbox spread（`EdgeAnimation.tsx:108-111`、`BaseLayoutEngine.ts:140-143`、`enhanced-zero-overlap-layout.ts:1361-1364`、`CulturalLayoutAdapter.ts:170-173`、`complex-layout-engine.ts:561-564,977-980`、`overlap-resolver.ts:143-144`、`cycle-strategy.ts:92-93,181-182`、`quality-monitor.ts:448-449`、`layout-worker.ts:110,144-145`、`TreeLayoutStrategy.ts:169-170`、`tree-strategy.ts:176`） | ウェーブ 6 で優先度低扱い | 座標は layout 出力（fitNodesToCanvas + finite oracle で有限検証済み、`x \|\| 0` ガード箇所あり）。NaN 伝播経路は dagre 系で構造的に閉済み。**全 44 spread サイトの禁止はしない**（registry は T1 移行済みモジュール scoped、D7） |

### T3 端数系 → sweep 判定確定

| サイト | 判定 | 理由 |
|---|---|---|
| `src/export/production-exporter.ts:780` | **移行**（ウェーブ 6） | `endTime! - startTime!` の Date 系差分。Date.now 由来で有限（09f 教訓）だが non-null assertion リスク同居 → mean 移行で統一 |
| `src/export/production-exporter.ts:687` | **移行**（ウェーブ 6） | 設計 D5 の指定。`Math.max(0, durationMs \|\| 0)` で既にガード済みだが mean 形状で safeMean 化 |
| `src/export/production-exporter.ts:437` | 対象外 | `scene.frameCount` は renderer 内部カウンタ生成（整数・有限保証） |
| `src/export/production-exporter.ts:782` | 対象外 | `estimatedSize \|\| 0` ガード済み |

### round 20（2026-08-16）: framework/api 大陸 — TASK-0011 で triage 完了

| サイト | 判定 | 理由 |
|---|---|---|
| `src/api/batch-processing-api.ts:411-412` | **移行**（round 20） | `SimplePipelineResult.qualityScore` は pipeline→REST 境界の interface フィールド（`?: number` optional）。`??` は null/undefined 置換のみで非有限は素通し → total/average 両方 NaN（REST では null 逐次化）。EER-355 と同判定 |
| `src/framework/recursive-custom-instructions.ts:295-299` | **移行**（round 20） | `filter(v => typeof v === 'number')` は誤述語 pre-filter（`typeof NaN === 'number'`）。NaN → `passed` gate silent fail、+Infinity → 常時 pass。T3「ガード付きだが形状誤り」 |
| `src/framework/continuous-learner.ts:497/502` | **移行**（round 20） | `userFeedback` は未検証 public `learnFromUserFeedback(rating)` 境界値（1-5 clamp 無し）。`|| 0` が 0 置換（D2 が拒否する代替）で mean を引き下げ偽 insight |
| `src/framework/continuous-learner.ts:381, 760-761` | 対象外 | learningDatabase の processingTime/qualityScore mean。単一内部 producer（simple-pipeline 8 call site 全て Date.now 差分・literal/clamp 済み）+ population guard。recovery-telemetry r18 除外と同型 → registry 除外登録 |
| `src/framework/continuous-learner.ts:700-704` | 対象外 | pearson sum fold。`Number.isFinite` pair pre-filter 済み（performance-dashboard r19 除外と同型） |
| `src/framework/continuous-learner.ts:942, 999-1001, 1144-1145, 1165, 1191` | 対象外 | 同上単一内部 producer。`Math.max(len,1)` / `length>0 ? : Infinity`（defect-9 fix 済み）/ `length===0 return` の guard あり |
| `src/framework/iteration-manager.ts:665, 765` | 対象外 | `duration` は `Date.now() - startTime` 内部生成。665 の空配列は 652 行 `length===0 return` で到達不能 |
| `src/api/batch-processing-api.ts:424` 附近 | 対象外 | averageProcessingTime。`totalProcessingTime` = Date.now 差分・`length>0` guard あり |
| `src/api/routes/errors.ts:98` | 対象外 | `Math.ceil(entries.length * 0.1)` は eviction 個数計算（集計でない・percentile pattern も不一致） |

## ウェーブ構成への反映（確定）

- ウェーブ 2: llm-service 5 サイト（724/728/732/795/796）→ safeMean
- ウェーブ 3: diagram-detector 2 サイト（436 spread→safeMax、1344→safeMean）
- ウェーブ 4: scene-segmenter 792 → safeMean（`sanitizeFinite` 0 置換既存サイト 609/671/702/808 は
  D2 の除外正規形への切替で mean 値が変わるため `behavior change:` 要 — ウェーブ 4 の判定事項）
- ウェーブ 5: enhanced-error-recovery 1452/1899/1900 spread→safeMax/safeMin（+355-357/420/471/821/1971 の safeMean 化を同ウェーブで可）
- ウェーブ 6: production-exporter 687/780（T3 確定分のみ。visualization bbox は対象外確定）

## 数値比較（要件 → 現況）

| 項目 | 要件時 | 今 sweep |
|---|---|---|
| reduce 系実サイト | 125 | 134 |
| spread 系実サイト | 4 | 44（うち移行対象 5: detector 436 + error-recovery 3 + exporter 判定対象外のため実移行 4） |
| 移行対象合計 | — | T1 9 サイト + T3 2 サイト = **11 サイト**（+ error-recovery T2 傍流 7 サイト任意） |
