# TASK-0008: frozen-literal registry family の追加

<!-- spine:anchor:begin -->
> **Spine anchor**: [speech-to-visuals アーキテクチャ設計](../../speech-to-visuals/architecture.md)
>
> - parent: `speech-to-visuals/architecture.md`
> - role: `detailed`
> - status: `canonical_child`
<!-- spine:anchor:end -->

**タスクID**: TASK-0008
**タスクタイプ**: TDD
**推定工数**: 3時間
**フェーズ**: Phase 3 - ガードと総合検証
**信頼性レベル**: 🟡 *family 形式は既存踏襲だが、検出パターンの実効性（誤検出ゼロ・漏れゼロ）は実装で検証するまで確定しない（D7 と同判定）*

## 関連文書

- **概要**: [📋 overview.md](overview.md)
- **要件定義**: [📋 requirements.md](../requirements.md) - REQ-401, REQ-402, REQ-403
- **設計文書**: [📐 architecture.md](../architecture.md) - D7・ウェーブ 7

## タスク概要

`tests/guards/frozen-literal-rules.ts` に未ガード集計形状の family エントリ
（約 15 行）を追加する（design ウェーブ 7）。125 サイト全件禁止ではなく、
移行済みモジュール（`src/analysis/`, `src/quality/`, `src/export/`）に scoped した
ファイルリスト検出 + sweep pin とする。

## 依存タスク

- **前提タスク**: [TASK-0003](TASK-0003.md)〜[TASK-0007](TASK-0007.md)（移行済みファイルが確定してから pin を作る）
- **後続タスク**: [TASK-0009](TASK-0009.md)

## 完了条件

- [x] family エントリが単一ファイル（frozen-literal-rules.ts）に追加され、registry 分割をしていない（REQ-401）— round-18 エントリ 2 件（files-pin + roots-sweep）、分割なし
- [x] 検出パターン: `reduce((a, b) => a + b` / `reduce((sum,` 集計形状 + `Math.max(...` / `Math.min(...` spread — files-pin 側はwaves 2-6の旧式そのもの（`(sum,` 系は `result.score` 形状に限定）、roots-sweep 側は `(a, b) => a + b, 0) /` の raw mean + spread
- [x] 対象が `src/analysis`, `src/quality`, `src/export` の移行済みファイルに scoped（REQ-402: `.length` 系・match-count 系の誤検出なし）— T2-deferred 6 ファイルを理由付き exclude、count 系 `(sum,` 全般は ban 対象外
- [x] sweep pin で新規ファイルを catch する形式 — roots ルール minSweptFiles=40（実測 55 ファイル）
- [x] `readFileSync` が `import.meta.url` 基準（REQ-403。cwd 相対で `--maxWorkers>1` flake を起こさない）— 共有 walker（freeze-guard.ts REPO_ROOT）をそのまま使用
- [x] RED 検証: family を一時的に violation するコードを置いて検出することを確認後、元に戻す — llm-cache.ts に raw mean・llm-service.ts に旧式そのものを挿入 → 両ルールが file:line 付きで検出、復元後 GREEN
- [x] `npx jest --config jest.config.cjs tests/guards/frozen-literal-registry` が GREEN — 20/20

---

## 主要実装項目

### family エントリの形式 🔵

**信頼性**: 🔵 *r4-r17 の既存 family 形式（frozen-literal-rules.ts 412 行）の踏襲より*

**実装ファイル**: `tests/guards/frozen-literal-rules.ts`

⚠️ registry の既知 GOTCHA: 検出 regex の callback 内に `)` を含む場合は
greedy `.*` ではなく `[^)]*` を使う（session 114 の教訓）。

### import.meta.url 基準の readFileSync 🔵

**信頼性**: 🔵 *REQ-403・TC-302/313 教訓より*

```ts
const rulesPath = new URL('./frozen-literal-rules.ts', import.meta.url);
```

cwd 相対パスは `--maxWorkers>1` で flake するため禁止。

---

## 基本的なテスト要件

### RED 検証 🔵

**信頼性**: 🔵 *「guard verified-only-behaviorally→source-anchored (10e)」の運用より*

- violation コードを一時挿入して family が検出すること（RED）を確認してから除去
- 既存 registry suite 全体が GREEN であること（分割していないことの実行証拠）

---

## 実装手順

### TDDタスクの場合

1. `/tsumiki:tdd-red`（violation 検出確認） → 2. `/tsumiki:tdd-green`（除去） → 3. `/tsumiki:tdd-verify-complete`

---

## 注意事項

- ウェーブ 2-6 の過程で増分を足す方が自然な場合は各ウェーブへの分散可（D5 ウェーブ 7 の注記）
- registry は 412 行 < 800 行上限のため分割禁止。追加後も単一ファイルを維持する

---

## 信頼性レベルサマリー

- **総項目数**: 3項目
- 🔵 **青信号**: 3項目 (100%)
- 🟡 **黄信号**: 0項目 (0%)
- 🔴 **赤信号**: 0項目 (0%)

**品質評価**: 高品質（検出パターン実効性のみ 🟡）
