# TASK-0319: transcribeStream routing 改修 — 環境別 3 分岐 + simulate chunk loop 廃止 + Node 構築可能化

<!-- spine:anchor:begin -->
> **Spine anchor**: [streaming-real-asr-inference アーキテクチャ設計](../architecture.md)
>
> - parent: `streaming-real-asr-inference/architecture.md`
> - role: `detailed`
> - status: `canonical_child`
<!-- spine:anchor:end -->

**タスクID**: TASK-0319
**タスクタイプ**: TDD
**推定工数**: 10時間
**フェーズ**: Phase 2 - 経路実質化（D-6 本体）
**信頼性レベル**: 🔵 *architecture.md「routing 改修」節・「constructor の環境 guard」節・REQ-424 (a)(b)(c)(e)・SD2/SD3/SD5/SD6 より*

## 関連文書

- **概要**: [overview.md](overview.md)
- **設計文書**: [architecture.md](../architecture.md)（routing 改修・constructor guard 節・SD2/SD3/SD5/SD6）
- **データフロー**: [dataflow.md](../dataflow.md)（3 経路の入出力形状・エラーフロー）
- **コンテキスト**: [note.md](../note.md)
- **要件**: [requirements.md](../../speech-to-visuals/requirements.md) REQ-424 (a)(b)(c)(e)
- **受け入れ基準**: [acceptance-criteria.md](../../speech-to-visuals/acceptance-criteria.md) TC-408-01（≡ AC-D6-1）+ TC-408-03 の result flag 半分（≡ AC-D6-3 前段）

## タスク概要

`StreamingTranscriber.transcribeStream`（streaming-transcriber.ts:147-254）のシミュレーション chunk loop（`createAudioChunks` + `processAudioChunk` + 10x realtime の `setTimeout` :224）を廃止し、環境判定による 3 分岐へ置き換える: (1) browser + Web Speech available → TASK-0318 の shared file engine 経由（final result ごとに実 `onSegment` / `onProgress`）(2) Node → `whisperTranscriber.transcribe` への丸ごと委譲（単発推論・完了時 1 回 emit）(3) いずれも不可 → `placeholder: true` 付き開示 placeholder。あわせて constructor（:111）と `validateStreamingSupport`（:625-）の bare `window` 参照を `typeof window !== 'undefined'` guard の内側へ置き、Node での構築を可能にする。

## 依存タスク

- **前提タスク**: [TASK-0318](TASK-0318.md)（browser 分岐の委譲先 engine）
- **後続タスク**: [TASK-0320](TASK-0320.md)（routing 後の filter/monitor semantics）・[TASK-0321](TASK-0321.md)（`placeholder` flag を消費する UI 開示）

## 完了条件

- [x] routing 契約 test（browser 分岐・Node 分岐・gate 閉フォールスルーの 3 leg）が RED→GREEN すること（TC-408-01 検証コマンド）
- [x] browser 分岐で engine の final result ごとに実 `onSegment` / `onProgress` が発火すること（合成 stagger なし — `setTimeout` による進捗演出の test が廃止済みであること）
- [x] Node 分岐で `whisperTranscriber.transcribe` が呼ばれ、`placeholder !== true` の結果の segments が採用され `onProgress` が完了時 1 回のみ emit されること。`placeholder: true` のときは分岐 3 へフォールスルーすること
- [x] ASR が走らなかった run の `TranscriptionResult` が `placeholder: true` を持つこと（`success: true` 単独の偽成功の消滅 — TC-408-03 result flag）
- [x] 開示 placeholder の固定文生成が `PLACEHOLDER_CHUNK_CONFIDENCE` 付きで残置すること（duration から chunk 数を算出する現行ロジックは開示のために保持）
- [x] Node 環境（`typeof window === 'undefined'`）で `new StreamingTranscriber({...})` と `validateStreamingSupport()` が throw しないこと（SD6）
- [x] `transcribeStream` signature・`StreamingProgress` 型・constructor 検証・`updateConfig` 検証・merge/dedup 境界・`startLiveTranscription` / `stopLiveTranscription` / `destroy`・REQ-091 API の外面契約が不変であること
- [x] 影響範囲の既存 test 群（下記テスト要件の網羅リスト）が全て GREEN または意図済み置換であること
- [x] `tsc -p tsconfig.app.json` / `-p tsconfig.test.json` が exit 0


**完了記録** (2026-09-05): routing 契約 test 6 leg（browser engine 委譲・Node whisper 委譲・Node gate 閉フォールスルー・browser 構築 API 不在フォールスルー・engine reject フォールスルー・Node 構築可能性 SD6）を RED→GREEN。`transcribeStream` は memoized duration probe（失敗は warn + 0 — reject しない）+ 3 分岐へ書き換え: 経路1 `transcribeFileWithWebSpeech` 委譲（final result ごとに onSegment/onProgress 1 回・合成 stagger 廃止 — chunk loop の `setTimeout(resolve, 100)` と processAudioChunk の 10x realtime sleep を削除）・経路2 `whisperTranscriber.transcribe` 委譲（`placeholder !== true` なら segments そのまま採用・完了時 onProgress 1 回のみ・merge しない。throw は gate 閉として経路3 へ）・経路3 開示 placeholder（固定文 + `PLACEHOLDER_CHUNK_CONFIDENCE` + `result.placeholder: true`・filter 行と merge は現行のまま残置 = score-ladder census ALLOWED 2 row の live site 維持）。constructor/`validateStreamingSupport` に typeof window/navigator guard（SD6）。意図済み置換 10 test: duration probe 失敗の reject→placeholder resolve 4 件（main 2・pipeline jsdom 1・ISS-A 1）・multi-chunk progress ≥3→完了 1 回 3 件（progress 形状は totalDuration/segmentCount pin で補強）・calc-throw / onProgress-throw の loop 継続→単一 event の包含へ 2 件。whisper-catch fallback の捏造 `language: 'en'` は frozen-literal guard が検出 → fallback object 廃止で純粋フォールスルー化。ESM mock: relative specifier の `unstable_mockModule` は module-scoped `jest` import が必須（global bridge だと setup ファイル基準の解決で Cannot find module）+ module-level mock fn は beforeEach で `mockReset()`（restoreAllMocks は plain jest.fn の call history を消さない）。検証: streaming-transcriber 117 + blast radius 13 suites 382 + guards 110 suites 3841 + 残 consumer 7 suites 155 全 GREEN・tsc app/test 両 config exit 0。

**Test stage 記録** (2026-09-05): dataflow.md error flow の未 cover 契約 6 leg を routing describe に追加 — engine `onError` 0 finals → 経路3・`onError` + partial finals は placeholder:false の実測 run 維持・Node whisper throw → 経路3 フォールスルー・duration probe 失敗は engine run を block しない（duration 0 開示）・onSegment callback throw は per-final で隔離・probe memoization（経路1→経路3 fallback でも Audio 構築は 1 回）。各 leg は単独 mutation RED で検証（`errored` swallow・whisper catch rethrow・memoize 条件削除の 3 mutation で対応 leg のみ RED）。検証: streaming-transcriber 123 + blast radius 25 suites 593 + guards 110 suites 3841 GREEN・tsc 両 config exit 0。

**Test stage 補強記録** (2026-09-05): 評価残 suggestion 2件を閉塞 — (1) test ローカル `EngineHooks` 型定義を engine module の `WebSpeechFileHooks` type import へ置換（hook 名単一ソース化）。⚠️ 実測発見: `src/**/__tests__/**` は tsc app/test 両 config の exclude 対象で ts-jest も isolatedModules transpile-only のため**compile-time drift 検出は本toolchainでは機能しない** — drift 検出は引き続き behavioral（engine が hook の runtime key を改名すると mock の旧名称呼び出しが no-op 化し 0-finals leg が RED・mutation で実証）。(2) whisper throw leg・engine onError 0-finals leg に TC-408-03 leg と同形式の開示内容 pin（固定文 `Processed segment` + `PLACEHOLDER_CHUNK_CONFIDENCE`）を追加 — describe 単独で開示内容を検証。mutation witness: 固定文を `Simulated segment` へ破壊すると新 pin 2 leg + 既存 TC-408-03 leg が RED。検証: streaming-transcriber 123 + transcription/streaming 707 + tsc 両 config exit 0・eslint clean。

**Test stage 補強記録 2** (2026-09-05): 評価 94点の残 suggestion 2件を閉塞 — (1) 開示内容 pin for-loop（3箇所重複）を routing describe 内 `assertPlaceholderDisclosure(result)` helper へ抽出 — 固定文 prefix と confidence 期待値の単一定義化。ついでに pin 未適用だった 2 leg（TC-408-01 c 構築 API 不在・engine rejection）にも helper 適用し routing describe 内の全 placeholder outcome leg（5 leg）が開示内容を検証。mutation witness 再実証: helper 化後も固定文 `Processed segment`→`Simulated segment` 破壊で該当 5 leg 全てが helper の `toContain` で RED（3→5 leg に増強）・戻し後 123 green。残置していた 1 箇所の inline hook 型（TC-408-01 a leg）も `WebSpeechFileHooks` へ統一し mock signature 5/5 単一ソース化・onend 系空配列と onError の境界不可区別性（hooks.onError 存在理由）を comment に追記。(2) `src/**/__tests__` 型検査ゼロ（両 config exclude + ts-jest transpile-only・純 type error は CI で落ちない）を AGENTS.md テスト規約へ横展開 — compile-error 検出前提の禁止と behavioral leg による drift 検出を規約化。

**Test stage 補強記録 3** (2026-09-05): 評価 96点の残 suggestion 2件を閉塞 — (1) 前段で AGENTS.md へ横展開した型検査ゼロ規約に構造 guard が無く前提変更で静かに stale 化する点を閉塞: `tests/guards/tsconfig-src-tests-exclude-pinned.test.ts`（4 leg）を新設し規約の前提（tsconfig.app.json exclude 3項目 exact + include・tsconfig.test.json の extends 継承と exclude override 不在・isolatedModules transpile-only の flag 継承と jest.config.cjs の ts-jest→tsconfig.test.json wiring・AGENTS.md 規約見出し verbatim）を設定ファイル実値から pin — AGENTS.md 規約 section に構造ガード行を追記し前提変更は同 commit 更新を強制。⚠️ tsconfig は JSONC かつ paths 文字列が `/*` glob を含むため comment-strip 正規表現は誤爆（exclude の `**/` まで喰う）— TypeScript 自身の readConfigFile（`import type` + createRequire・non-null-assertion-census と同 pattern）で読む。⚠️ block comment 内に `src/**/__tests__` は書けない（`*/` が comment を終端し TS1434）。(2) `assertPlaceholderDisclosure` helper comment に tests/ 側切り出し指引（型検査される include 配下）を追記し stale な "three legs" 表記を 5 leg へ修正。mutation witness: 4 mutation（exclude から test.tsx 削除・test config へ exclude override 追加・jest transform の tsconfig 参照破壊・AGENTS.md 見出し改題）がそれぞれ対応 leg のみ単独 RED（1 failed/3 passed ×4・M1 は leg1 の失敗名で確認）・復元後 guard 4/4 + 対象 4 suite 177 green。

---

## 主要実装項目

### 1. 環境別 3 分岐への routing 書き換え 🔵

**信頼性**: 🔵 *architecture.md「routing 改修」節・dataflow.md「データフロー全体」（分岐条件が明記）より*

`transcribeStream` 冒頭で環境判定し、3 経路のいずれかで `TranscriptionResult` を組み立てる:

1. **browser + Web Speech available**（`typeof window !== 'undefined'` かつ recognition 構築成功）: `transcribeFileWithWebSpeech(audioFile, { onFinalSegment })` に委譲。final result ごとに `onSegment(segment)` + `onProgress`（実イベントのみ）を転送
2. **Node**（`typeof window === 'undefined'`）: `whisperTranscriber.transcribe(audioFile)`（singleton・whisper-transcriber.ts:704）へ丸ごと委譲。`placeholder !== true` なら segments をそのまま流用し完了時 `onProgress` 1 回。`placeholder: true` なら 3 へフォールスルー（whisper 側固定文を二重持ち込みしない）
3. **開示 placeholder**: 現行 `processAudioChunk` 相当の固定文生成（:441）を残置し、結果に `placeholder: true` を付与。`PLACEHOLDER_CHUNK_CONFIDENCE`（:25）と `detectTranscriptionLanguage` 委譲（:242）は維持

いずれの経路も `placeholder` flag を必ず設定（実経路 `false` / 開示 `true`）。`getAudioDuration` 失敗（分岐 3 の chunk 数算出のみで使用）は `placeholder: true` で返す（dataflow エラーフロー）。

**実装ファイル**: `src/transcription/streaming-transcriber.ts`

### 2. constructor / validateStreamingSupport の window guard 🔵

**信頼性**: 🔵 *streaming-transcriber.ts:111（`'webkitSpeechRecognition' in window` — Node で ReferenceError）と :625-（`validateStreamingSupport` の bare 参照）の実在より*

- constructor の recognition 構築（:111-116）を `typeof window !== 'undefined' && (...)` の内側へ
- `validateStreamingSupport` の `'webkitSpeechRecognition' in window` 等 3 参照を同 guard の内側へ（Node では各 `false` 扱いで recommendation を返す — throw しない）

### 3. simulate 挙動 pin test 群の routing 契約 test への書き換え 🔵

**信頼性**: 🔵 *design-interview A10（テスト影響範囲の棚卸し）・grep による消費実測より*

`transcribeStream` / `validateStreamingSupport` は `src/transcription/index.ts` から barrel export され、広範な test が現行挙動を pin している。書き換え対象:

- `src/transcription/__tests__/streaming-transcriber.test.ts`（2713 行）: simulate 固定文（`Processed segment` / chunk 文言）と 0.75 経由の filter 挙動 pin を routing 契約 test へ置換。constructor 検証・`updateConfig` 検証・merge/dedup 境界 test は不変
- `tests/unit/pipeline/streaming-transcriber.test.ts`（370 行）: 固定文と language `'en'` pin（:111 comment）を置換
- `tests/transcription/streaming-transcriber.test.ts`（249 行・quality monitor 系）: TASK-0320 と協調して未測定 semantics test を追加・捏造 0-reject pin を改廃
- 影響確認必須の既存 test（現行挙動 pin の疑いがあるもの — 全件実行して GREEN を確認）: `src/components/__tests__/StreamingProcessor.test.tsx`・`src/__tests__/async-state-stale-closure-guard.test.ts`・`src/quality/__tests__/defect-9-silent-pass-consolidated-regression.test.ts`・`tests/unit/async-resource-cleanup.test.ts`・`tests/transcription/callback-error-logging.test.ts`・`tests/transcription/language-detection-migration.test.ts`・`tests/guards/timestamp-guard-mutation-pinning.test.ts`

### 4. guard census の追従 🔵

**信頼性**: 🔵 *design-interview A10「simulate 固定文の pin が census に現れる行は置換時に追従」より*

REQ-391 measurement-fixture census（`tests/guards/measurement-statement-literal-census.test.ts`）の ALLOWED 行のうち simulate 固定文に言及する行があれば同 commit で更新（`PLACEHOLDER_CHUNK_CONFIDENCE` 自体は維持されるため row は基本不変のはず — stale row が出た場合のみ更新）。

---

## 基本的なテスト要件

### テストケース1: browser 分岐 — engine 委譲と実イベント進捗 🔵

**信頼性**: 🔵 *TC-408-01 (a)・dataflow.md 経路1 より*

**Given**: jsdom + Web Speech available な環境（SpeechRecognition mock が final result を 2 つ発行）
**When**: `transcribeStream(file, onProgress, onSegment)` を呼ぶ
**Then**: `onSegment` が final result ごとに 2 回・`onProgress` が実イベント単位で発火し、結果の `placeholder` が `false` であること

### テストケース2: Node 分岐 — whisper 委譲と単発 emit 🔵

**信頼性**: 🔵 *TC-408-01 (b)・SD2/SD3 より*

**Given**: Node 環境（`typeof window === 'undefined'）。`whisperTranscriber.transcribe` を mock（`placeholder: false` の実測相当結果を返す）
**When**: `transcribeStream(path)` を呼ぶ
**Then**: 委譲結果の segments がそのまま採用され、`onProgress` が完了時 1 回のみ emit されること（chunk 分割された合成進捗が出ないこと）

### テストケース3: gate 閉フォールスルーと開示 placeholder 🔵

**信頼性**: 🔵 *TC-408-01 (c)・TC-408-03 result flag・SD5 より*

**Given**: Node 環境で `whisperTranscriber.transcribe` が `placeholder: true`（gate 閉）を返す / または Web Speech 不可能な browser 環境
**When**: `transcribeStream` を呼ぶ
**Then**: 結果が `placeholder: true` を持つこと。固定文 segment が `PLACEHOLDER_CHUNK_CONFIDENCE` 付きで残ること

### テストケース4: Node 構築可能性 🔵

**信頼性**: 🔵 *SD6（bare `window` の ReferenceError）より*

**Given**: Node 環境
**When**: `new StreamingTranscriber({})` と `validateStreamingSupport()` を呼ぶ
**Then**: いずれも throw しないこと

---

## 実装手順

### TDDタスクの場合

1. `/tsumiki:tdd-requirements TASK-0319` - 詳細要件定義
2. `/tsumiki:tdd-testcases` - テストケース作成
3. `/tsumiki:tdd-red` - テスト実装（失敗）
4. `/tsumiki:tdd-green` - 最小実装
5. `/tsumiki:tdd-refactor` - リファクタリング
6. `/tsumiki:tdd-verify-complete` - 品質確認

---

## 注意事項

- whisperTranscriber への依頼は `unstable_mockModule` + dynamic import で mock する（ESM で `jest.mock` は no-op）
- `chunkSizeMs` / `overlapMs` は API 互換で残すが、実推論に使う経路が v1 に存在しない旨を doc comment に開示する（SD2）
- per-chunk 分割推論・実結果の chunk 長分割（合成 stagger）は実装しない（偽 progress — SD3）
- live mic 経路（`startLiveTranscription` 系）は既に実 Web Speech のため触れない

---

## 信頼性レベルサマリー

- **総項目数**: 8項目
- 🔵 **青信号**: 8項目 (100%)
- 🟡 **黄信号**: 0項目 (0%)
- 🔴 **赤信号**: 0項目 (0%)

**品質評価**: 高品質（3 分岐の条件・委譲先・flag 付与・影響 test ファイル名がすべて設計と実測 grep に接地。唯一の実装時判断は 2713 行 test の置換粒度のみ）
