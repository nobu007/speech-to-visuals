# TASK-0321: UI 開示 — StreamingProcessor の placeholder notice と偽品質 stats 表示の抑制

<!-- spine:anchor:begin -->
> **Spine anchor**: [streaming-real-asr-inference アーキテクチャ設計](../architecture.md)
>
> - parent: `streaming-real-asr-inference/architecture.md`
> - role: `detailed`
> - status: `canonical_child`
<!-- spine:anchor:end -->

**タスクID**: TASK-0321
**タスクタイプ**: TDD
**推定工数**: 4時間
**フェーズ**: Phase 3 - UI 開示（偽成功のユーザー可視化）
**信頼性レベル**: 🔵 *architecture.md「UI 開示」節・REQ-424 (c)・dataflow.md「UI への開示データ」より（notice の具体形状のみ design-interview A9 で実装時確定と明記）*

## 関連文書

- **概要**: [overview.md](overview.md)
- **設計文書**: [architecture.md](../architecture.md)（UI 開示節・SD5）
- **データフロー**: [dataflow.md](../dataflow.md)（UI への開示データ）
- **コンテキスト**: [note.md](../note.md)
- **要件**: [requirements.md](../../speech-to-visuals/requirements.md) REQ-424 (c) の UI 半分
- **受け入れ基準**: [acceptance-criteria.md](../../speech-to-visuals/acceptance-criteria.md) TC-408-03 の UI 半分（≡ AC-D6-3 後段）

## タスク概要

`StreamingProcessor.tsx` は placeholder 経路の結果（TASK-0319 で `placeholder: true` が立つ）を「実測」と同一視して品質 stats 表示（`averageConfidence * 100` :574）に出している。result の `placeholder` flag を component state に取り込み、placeholder 経路では (1) 偽の品質 stats を表示しない (2) placeholder 経路である旨の notice を出す — server 経路の D-1 開示・README「音声認識の現状」と同一の開示方針を streaming 経路の UI にも適用する。

## 依存タスク

- **前提タスク**: [TASK-0319](TASK-0319.md)（`TranscriptionResult.placeholder` flag の供給）
- **後続タスク**: なし（本 feature の最終 task — 完了で TC-408-01〜04 全 Green）

## 完了条件

- [x] `transcribeStream` の結果 `placeholder: true` を StreamingProcessor が受け取り、placeholder 経路である旨の notice を render すること（要件は「placeholder 経路であることがユーザーに区別可能」なことのみ — notice の UI 形状は実装時に確定）🟡
- [x] placeholder 経路で `averageConfidence * 100` の実測風表示を出さないこと（0.75 は実測ではない — 偽 stats の抑制）🔵
- [x] 実経路（`placeholder: false`）では現行の stats 表示を維持すること（経路1 は実測 confidence なので表示が正当化される）🔵
- [x] placeholder flag + UI 開示 pin が RED→GREEN すること（TC-408-03 検証コマンド — TASK-0319 の result flag 半分と合わせて TC-408-03 完結）🔵
- [x] 既存 `src/components/__tests__/StreamingProcessor.test.tsx` が全て GREEN（または意図済み置換）であること 🔵
- [x] `tsc -p tsconfig.app.json` / `-p tsconfig.test.json` が exit 0 🔵

**完了記録** (2026-09-07): UI 開示 pin test 2 leg（placeholder 経路: notice render + `75%` 実測風表示不在 + `未測定` 開示・実経路: `92%` stats 維持 + notice 不在）を追加 — 両 leg が旧実装で RED（notice 欠落）→ GREEN。実装: (1) `isPlaceholderResult` state を新設し `transcribeStream` の result から `result.placeholder === true` を取り込み（`onProgress` の averageConfidence と異なり run 全体の経路判定は result で確定 — TASK-0319 供給の flag を消費）。開始時（file/live 両 path）と `resetProcessor` で reset。(2) placeholder 経路では Confidence stats 欄を `未測定` 表示へ切替（`averageConfidence * 100` の実測風 % 表示を抑制 — 0.75 は開示済み PLACEHOLDER_CHUNK_CONFIDENCE 定数で測定ではない）。(3) stats 欄直下に `role="note"` + `aria-label="transcription-placeholder-notice"` の開示 notice を render（形状は design-interview A9 の実装時確定事項として task 例示どおり採用・placeholder は error ではなく開示として `success: true` + complete status 維持）。mutation witness: stats 抑制 conditional を除去する mutation で placeholder leg のみ単独 RED を実測（notice 欠落は旧実装 RED で witness 済み）。検証: StreamingProcessor 1 suite 5 test + guard/regression 6 suites 275 test 全 GREEN・tsc app/test 両 config exit 0・eslint 変更 file clean。

**Chain 記録** (2026-09-07, test stage — implement run 20260906-150140-511469 / 評価 92点の残 suggestion 2件を閉塞): (1) **開示文言の単一ソース化**: notice 本文・aria-label・`未測定` ラベルを `StreamingProcessor.tsx` の named export 定数（`PLACEHOLDER_NOTICE_TEXT` / `PLACEHOLDER_NOTICE_ARIA_LABEL` / `UNMEASURED_CONFIDENCE_LABEL`）へ切り出し、JSX は定数を消費・test は import した定数と render DOM を突き合わせる（test 側に第2コピーを持たない — component と pin の文言 drift が test 失敗になる）。server D-1 経路との語彙整合は derived anchor leg で検出: 定数から `README「...」` の引用 title を抽出し README.md の実在見出し（h2-h4）を要求 — README 側 retitle で RED・同一 commit 更新を強制する（mutation 実測: 見出し rename で anchor leg のみ単独 RED・JSX の inline 別文言差し替えで textContent pin leg のみ単独 RED）。(2) **reset wiring 2 leg 新設**（implement の未検証経路）: file-start reset leg は run2 を in-flight 保持（deferred promise）し run 中 DOM で stale notice 不在を検証・live-start reset leg は recording 中の notice 不在を検証 — mutation 2種（file-start / live-start の `setIsPlaceholderResult(false)` 除去）で各 leg のみ単独 RED を実証。`resetProcessor` の reset は DOM 観測不能（idle では status card が非 render・次 run は必ず start reset を通るため isolated witness が存在しない）— defensive 重複として本記録で明示。検証コマンド verbatim（base = implement tip 598887a5）: `NODE_OPTIONS='--experimental-vm-modules --max-old-space-size=4096' npx jest --config jest.config.cjs --testPathPatterns "src/components/__tests__"` → 12 suites / 257 tests green・同 NODE_OPTIONS で `--testPathPatterns "transcription"` → 28 suites / 677 tests green・`--testPathPatterns "tests/guards"` → 111 suites / 3845 tests green・`node_modules/.bin/tsc -p tsconfig.app.json --noEmit` / `-p tsconfig.test.json --noEmit` → 両 config exit 0・`npx eslint src/components/StreamingProcessor.tsx src/components/__tests__/StreamingProcessor.test.tsx` → exit 0。

**Chain 記録 (baseline)** (2026-09-07, post-merge baseline — 前段 test stage run 20260906-152327-546253 は PR #113 として merge 済みも chain の stage evaluation が失敗〔eval 記録: LLM evaluation 300s timeout・llm_score null〕のため iter2 として再実行): merge 後 main tree（merge commit 0ef124cc・worktree は stale base 1d0a9357 から reset 回収）で検証コマンド verbatim: `NODE_OPTIONS='--experimental-vm-modules --max-old-space-size=4096' npx jest --config jest.config.cjs --testPathPatterns "src/components/__tests__"` → 12 suites / 257 tests green・同 NODE_OPTIONS で `--testPathPatterns "transcription"` → 28 suites / 677 tests green・`--testPathPatterns "tests/guards"` → 111 suites / 3845 tests green・`node_modules/.bin/tsc -p tsconfig.app.json --noEmit` / `-p tsconfig.test.json --noEmit` → 両 config exit 0 — merge 後 main の健全性を merge commit tree 自体で実証（各 stage の検証は stage base に対するものであり本記録が merge 後の追加証拠）。**D-6 feature（streaming 実ASR化・TASK-0318〜0321・TC-408-01〜04）完納宣言** — overview.md 全 Phase 完了済み・残課題（REQ-091 alert UI wiring）は design-interview A9 で別 task 化明示済み。以降の chain は次回採番 TASK-0322（kairo-design）へ移行。

**Chain 記録 (baseline iter3)** (2026-09-07, post-merge baseline — 本記録は post-merge baseline 検証であり新規 test 実装を含まない〔前回評価 93点の suggestion「baseline 系 run の宣言行定型化」を本記録で確立〕。前段 baseline iter2 run 20260906-155338-912051 は PR #114 として merge 済み: merge commit 1d2774a4・2026-09-06T16:07:55Z・checks 全 SUCCESS。前回評価の残 suggestion「検証証跡の機械可読紐付け」を閉塞): (1) **機械可読証跡の紐付け** — merged baseline tree の CI 証跡として PR #114 の actions run URL を Chain 記録と claims.ndjson 当該 claim の preconditions へ記録: https://github.com/nobu007/speech-to-visuals/actions/runs/34044355255 (test 4 shard・type-check・lint・build・code-size-audit・spine-validate 等 全 SUCCESS — prose 申告以外の再検証手段)。本 run の PR checks URL は checks 完了後に本記録へ追記。(2) 検証コマンド verbatim (base = main tip 1d2774a4 へ reset 回収した worktree・本 run の記録系 diff〔TASK-0321.md・claims.ndjson・run_state.json・ontology_metrics.json〕追記後の tree で実行): `NODE_OPTIONS='--experimental-vm-modules --max-old-space-size=4096' npx jest --config jest.config.cjs --testPathPatterns "src/components/__tests__"` → 12 suites / 257 tests green・同 NODE_OPTIONS で `--testPathPatterns "transcription"` → 28 suites / 677 tests green・`--testPathPatterns "tests/guards"` → 111 suites / 3845 tests green・`node_modules/.bin/tsc -p tsconfig.app.json --noEmit` / `-p tsconfig.test.json --noEmit` → 両 config exit 0。

---

## 主要実装項目

### 1. result placeholder flag の取り込み 🔵

**信頼性**: 🔵 *StreamingProcessor.tsx:216（`const result = await transcriber.current.transcribeStream(...)` の戻り値が未消費）より*

`transcribeStream` の戻り値から `result.placeholder` を state に保存（例: `isPlaceholderResult`）。`onProgress` 経由の `averageConfidence` と異なり、run 全体の経路判定は result で確定する。

**実装ファイル**: `src/components/StreamingProcessor.tsx`

### 2. 偽 stats 抑制と notice 表示 🔵 / 🟡

**信頼性**: 🔵 *dataflow.md「UI への開示データ」（抑制要件が明記）より。notice 形状は 🟡 *design-interview A9（実装時確定の明記）より*

- placeholder 経路: Confidence stats 欄（:574 `(stats.averageConfidence * 100).toFixed(0)%`）を実測表示として出さない（非表示 or 「未測定」表示 — 実装時に確定）
- placeholder 経路: 「ASR が実行されなかった経路（開示済み placeholder）」旨の notice を render
- 実経路: 現行表示を維持

```tsx
{isPlaceholderResult && (
  <div role="note" aria-label="transcription-placeholder-notice">
    この経路では音声認識（ASR）が実行されていません。表示されるセグメントは
    開示済みプレースホルダーです（README「音声認識の現状」参照）。
  </div>
)}
```

### 3. UI 開示 pin test 🔵

**信頼性**: 🔵 *TC-408-03 検証コマンド（placeholder flag + UI 開示 pin）より*

**テストファイル**: `src/components/__tests__/StreamingProcessor.test.tsx` へ追記

- placeholder result を返す mock `transcribeStream` で: notice が render されること・`%` 表示（`averageConfidence` 実測風）が出ないこと
- 実測 result（`placeholder: false`）で: 従来の stats 表示が維持されること

---

## UI/UX要件

### エラー表示 🔵

**信頼性**: 🔵 *現行 `setStatus('error')` 分岐（:225-）を置き換えない最小差分方針より*

- placeholder 経路は error ではなく「開示」として扱う（`success: true` + `placeholder: true` の結果を error 表示にしない — 開示と失敗の区別）

### アクセシビリティ 🟡

**信頼性**: 🟡 *notice 形状が実装時確定のため*

- notice は `role="note"` / `aria-label` 等で screen reader に読み上げ可能にする（形状確定時に既存 UI の慣行に合わせる）

---

## 実装手順

### TDDタスクの場合

1. `/tsumiki:tdd-requirements TASK-0321` - 詳細要件定義
2. `/tsumiki:tdd-testcases` - テストケース作成
3. `/tsumiki:tdd-red` - テスト実装（失敗）
4. `/tsumiki:tdd-green` - 最小実装
5. `/tsumiki:tdd-refactor` - リファクタリング
6. `/tsumiki:tdd-verify-complete` - 品質確認

---

## 注意事項

- REQ-091 alert UI wiring（`onQualityAlert` / `getQualitySummary` の消費）は本 task に含めない（design-interview A9 残課題 — 実測値が monitor に流れるようになった段階の別 task）
- Index.tsx（:202-210 の opt-in streaming mode render）は変更不要 — 開示は StreamingProcessor 内で完結する

---

## 信頼性レベルサマリー

- **総項目数**: 8項目
- 🔵 **青信号**: 6項目 (75%)
- 🟡 **黄信号**: 2項目 (25%)（notice の UI 形状・非表示 vs 未測定表示の選択 — いずれも design-interview A9 で実装時確定と明記済みの事項）
- 🔴 **赤信号**: 0項目 (0%)

**品質評価**: 高品質（抑制対象の行・維持条件・test 粒度が確定。🟡 は設計が意図的に実装時判断とした UI 形状のみで、要件（区別可能であること）は確定済み）
