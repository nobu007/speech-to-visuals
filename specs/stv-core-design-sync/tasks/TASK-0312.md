# TASK-0312: 設計正本の出典パス現勢性同期 — dead-path 53件正規化 + @stv/core 境界 section + design-doc-source-currency guard + MW-093 検証（REQ-420・設計 stage・第237回検証）

<!-- spine:anchor:begin -->
> **Spine anchor**: [stv-core design sync（設計正本の出典パス現勢性）要件定義](../requirements.md) REQ-420
>
> - parent: `stv-core-design-sync/requirements.md` REQ-420
> - role: `detailed`
> - status: `canonical_child`
<!-- spine:anchor:end -->

**要件**: [requirements.md](../requirements.md) REQ-420
**種別**: 設計 stage（kairo-design 差分分析→自動補完・src 変更ゼロ）　**推定工数**: 1.5h　**信頼性**: 🔵 *grep 実測（dead-path 52+1件・@stv/core mention 0→51・live src/ 18+1ディレクトリ）と guard 3 leg の GREEN/RED 実行に基づく*

## 背景

stv-core コア分割（PR #7・2026-08-18）で `src/{types,config,lib,utils}` が `@stv/core`（github:nobu007/stv-core#v1.0.7）へ移管・消滅した。第218回検証（REQ-310~312）は requirements.md・acceptance-criteria.md 側のみ同期し、**設計正本3文書は 2026-08-06 のまま取り残された**:

| 文書 | dead-path 出典 | @stv/core mention | 最終更新 |
| --- | --- | --- | --- |
| architecture.md | 22件（+木に消滅4行） | 0 | 2026-08-06（第209回） |
| dataflow.md | 7件 | 0 | （同世代） |
| interfaces.ts | 23件 | 0 | 2026-08-06（第208回） |

🔵 信頼性: 2026-08-27 実測（`grep -cE 'src/(types|utils|lib|config)/'`・`grep -c 'stv/core'`）

AI Hub steering（make-run feedback）の具体 symbol 群（`INV-STAT-006`・`impl/app/`・`grade_distribution.py`・V2.8→V2.9 rename の commit 文面指定）は grep で cross-repo phantom と判定（repo 内 0 hit・`scripts/code-size-audit.ts` は実在するが指示は既 commit の retroactive な文面変更で実行不可能）。META-intent（substantive で verifiable な diff の継続）のみ採用し、実態の drift（設計正本の未同期）を設計 stage の作業対象として選定した。

## 実施内容

1. **出典パス正規化（62置換）**: Node script（件数 assert 付き・abort 時無変更）で
   - `src/types/*` → `@stv/core/types/*`（diagram/api/workspace）
   - `src/utils/*` → `@stv/core/utils/*`（guards/sanitize/audio-duration/audio-validation/logger）
   - `src/config/*` → `@stv/core/config/*`（limits/validate/schema/code-size-audit）
   - `src/lib/*` → `@stv/core/lib/*`（unicode-script-ranges/quality-display-tiers）
   - `src/lib/actualVideoRenderer.ts` → `src/pipeline/actual-video-renderer.ts`（リポジトリ内移管・@stv/core 外の実在 path へ）
   - 歴史的 mutation 記述3件（round 41/42/43 の M4/M8）は「当時 src 配下 utils/guards・現 @stv/core/utils/guards」注記形へ（履歴の改変でなく path 形式の除去）
2. **architecture.md 構造同期**: ディレクトリ木から消滅4行除去・`├── src/` 行に19ディレクトリ実測注記・`node_modules/@stv/core/` 行追加・**「外部コアパッケージ（@stv/core）」section 新設**（移管対象・依存の方向361ファイル・単一ソース規則 REQ-310/312・バージョン運用 REQ-311）・最終更新 第237回検証 line・Spine children に本 feature 2件登録
3. **interfaces.ts**: header に共有型の正本が @stv/core/types/* である旨（REQ-310 単一ソース）と出典表記規約を明記
4. **guard 新設**: `tests/guards/design-doc-source-currency.test.ts`（3 leg — 詳細は requirements REQ-420-001〜003）
5. **MW-093 mutation 検証**: (a) dead-path 再注入 (b) 木に `types/` 行再注入 (c) 境界 section 見出し削除 → 各 RED・復元 GREEN

## 受け入れ基準（Given-When-Then）

- **G** 3文書のいずれかに `src/(types|utils|lib|config)/` が出典として再注入されたとき **W** guard leg 1 が走る **T** 当該 doc 名と行内容を列挙して RED（1 failed）
- **G** live src/ に新規ディレクトリが追加され木が未更新のとき **W** guard leg 2 が `fs.readdir` 導出と突合する **T** 不足 dir 名を列挙して RED
- **G** 木に消滅ディレクトリ行が再注入されたとき **T** RED（REQ-402 と同形式の事故再現）
- **G** 境界 section の見出しまたは pin 表記が削除/改変されたとき **T** RED（pin 表記は package.json 実値との完全一致比較）
- **G** 変更なしのとき **T** 全 leg GREEN（MW-093 復元後実測）

## 成果物

- specs/speech-to-visuals/{architecture.md, dataflow.md, interfaces.ts}（出典62箇所・木・境界 section・header）
- specs/speech-to-visuals/design-interview.md（A127）
- specs/stv-core-design-sync/{requirements.md, tasks/TASK-0312.md, tasks/overview.md, interview-record.md}
- tests/guards/design-doc-source-currency.test.ts（REQ-420）
- specs/speech-to-visuals/mutation-witness-ledger.md（MW-093 行）
