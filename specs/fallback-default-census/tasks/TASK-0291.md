# TASK-0291: fallback-default census guard 新設 + real inconsistency 3 site unify + atomic dogfood landing（REQ-405・ALLOWED/ERADICATED pin）

<!-- spine:anchor:begin -->
> **Spine anchor**: [fallback-default census 要件定義](../requirements.md) REQ-405
>
> - parent: `fallback-default-census/requirements.md` REQ-405
> - role: `detailed`
> - status: `canonical_child`
<!-- spine:anchor:end -->

**要件**: [requirements.md](../requirements.md) REQ-405-001〜007
**種別**: TDD（純関数 discovery primitive → census test → 3 site unify → ALLOWED 分類 → atomic dogfood landing）　**推定工数**: 4h　**信頼性**: 🔵 *REQ-404 が閉じた境界 class の第3面（欠損時代替値）+ 実測 mixed cluster 10 件・real inconsistency 3 site（file:line 付き）+ REQ-391〜404 census pattern の 14 例目*

## 背景

family 13（REQ-404）までで境界の入力側（比較）と出力側（丸め）を閉じた。本 TASK は同じ急所の**値が存在しないとき**の側 — 同一 chain への primitive literal fallback 混在 — を tree 全数で census する。機械的 discovery（chain-adjacency + standalone-RHS lookahead + canonical literal clustering）で site 327 / cluster 200 中に mixed cluster 10 件（32 site）を実測し、うち 3 site を real inconsistency として unify した。

## 実装

1. **`tests/guards/fallback-default-census.test.ts` 新設**:
   - discovery primitive を export（`canonicalFallbackLiteral` / `discoverFallbackSites` / `clusterKey` / `mixedLiteralClusters`）し合成 fixture で liveness 検証
   - real tree 未分類 site exact-0（`toEqual([])`）+ site/cluster floor pin（`>= 315` / `>= 192`・baseline 327/200）
   - ALLOWED roster 32 key（10 cluster・domain 理由付き）+ ERADICATED roster 3 key（chain 表記）+ negative anchor（正典 spelling 固定 + 旧 spelling not.toMatch ban）
2. **src 3 site unify（REQ-405-003）**:
   - `actual-video-renderer.ts` — `scene.durationMs || 10000` → `|| DEFAULT_SCENE_DURATION_MS`（空 scene 分岐は `* 2` で旧 10000ms を保存・render path を three-path agreement の violation から外す）
   - `pipeline-orchestrator.ts` — progress event の `gateResult.reason ?? 'Quality gate failed'` → `?? 'unknown'`（同一 function の throw と統一）
   - `websocket-handler.ts` — `decoded.role ?? ''` → `?? 'authenticated'`（HTTP auth middleware と統一）
   - `enhanced-error-recovery.ts` — maxRetries 分割 site に意图 comment 追記（ALLOWED 理由の現場化・第 4 file）
3. **three-way family 14 登録** — `census-artifact-three-way.test.ts` に REQ-405 行（requirementsPath = 本 spec・authority list 10 family）
4. **atomic dogfood landing（REQ-405-007）** — 本 spec 一式の anchor と parent 側登録（`speech-to-visuals/architecture.md` children 2 行・`speech-to-visuals/design-interview.md` children 1 行・`speech-to-visuals/note.md` references 1 行）を guard・MW・receipt と同一 commit に同梱（make-run steering の commit-splitting 指摘への直接回答）
5. **mutation 検証は TASK-0292**（REQ-405-008・MW-069）

## 検証

- fallback-default-census 全 test GREEN（real tree exact-0 + 合成 fixture 検出）
- census-artifact-three-way GREEN（REQ-405 行 phrase 一致・authority 10 family）
- spine-edge-census GREEN（atomic landing の dogfood）
- 影響 unit suite（actual-video-renderer・pipeline-orchestrator・websocket-handler・enhanced-error-recovery・scene-duration-limits）GREEN
- tsc 両 config（app / test）exit 0

## 設計決定

1. **chain-adjacency のみの LHS 抽出** — call-wrapped LHS / cast / Map-accumulator seed を除外（初期版の `)*` 許容が 7-site 偽 cluster を作った実測で排除）
2. **site key は `file:line:chain`** — 同一行の複数 field default（width/height 等）を分離。key 衝突は実測で発見
3. **ERADICATED key は chain 表記** — bare field 名は discovery key と一致せず eradicated-reappear が vacuous pass（MW-069 で発見・TASK-0292 に詳細）
4. **unify は「同一の量・claim を読む」cluster のみ** — same-token 別 domain（10 cluster）は domain 理由付き ALLOWED。REQ-403/404 が予言した shape の量産適用
