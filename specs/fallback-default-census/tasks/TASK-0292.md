# TASK-0292: MW-069 mutation 検証 + ledger 更新

<!-- spine:anchor:begin -->
> **Spine anchor**: [fallback-default census 要件定義](../requirements.md) REQ-405
>
> - parent: `fallback-default-census/requirements.md` REQ-405
> - role: `detailed`
> - status: `canonical_child`
<!-- spine:anchor:end -->

**要件**: [requirements.md](../requirements.md) REQ-405-008
**種別**: mutation 検証（RED 実測 → revert → GREEN 復元 → ledger 記載）　**推定工数**: 1.5h　**信頼性**: 🔵 *Phase 197 で確立した実装+MW+receipt 単一 commit 同梱規約の踏襲*

## 背景

census guard は GREEN だけでは「検出器として生きている」証明にならない。実事故 shape の mutation で RED を実測し、revert で GREEN 復元することで teeth を実証する（MW ledger 規約・PINNED_MIN_ENTRIES ratchet）。

## 実装

1. **mutation (a)**: `src/pipeline/actual-video-renderer.ts` の `scene.durationMs || DEFAULT_SCENE_DURATION_MS` を旧 `|| 10000` に revert → completeness（`scene.durationMs` cluster 再分裂）+ eradicated-reappear（`:226:scene.durationMs`）+ mode anchor の**三重** RED
2. **mutation (b)**: roster 外 `src/pipeline/smoke-orchestrator.ts` に `const stage = stage ?? 'render';` probe を注入（既存 bare-`stage` cluster への第 3 literal）→ completeness 単独 RED
3. **mutation (c)**: `src/visualization/strategies/TreeLayoutStrategy.ts` の `config.nodeSeparation || 80` を `|| 60` に flip → cluster 単一 literal 化により ALLOWED 2 行が stale-row RED
4. 各 mutation で Received の file:line を記録し revert（.bak 復元）で GREEN 復元
5. **ledger 記載** — `specs/speech-to-visuals/mutation-witness-ledger.md` に `MW-069` section（claim/target/mutation/command/observed ×3）+ appendix 5 列行を追記（cell 内に `|` 文字を含めない — shape guard が `split('|')` で列数検証するため）
6. **PINNED_MIN_ENTRIES 64→65**（`tests/guards/mutation-witness-ledger.test.ts`・phase history comment 更新）

## 検証

- 各 mutation で fallback-default-census が RED（三重捕捉 / completeness 単独 / stale-row）
- revert で全 guard GREEN 復元・`git diff --stat src/` が意图 4 file のみ
- mutation-witness-ledger guard（+ shape guard）が 65 entry で GREEN

## 設計決定

1. **同 commit 同梱** — Phase 197 (REQ-398) で確立した「実装 + MW + receipt 単一 commit」規約。MW だけ分離すると guard commit が teeth 未実証のまま land する
2. **mutation (a) は三重捕捉の実測** — completeness・eradicated-reappear・mode anchor の 3 検出経路が同一違反で重畳することの確認（ERADICATED key を chain 表記にしたことで reappear leg が生きた）
3. **mutation (b) は probe 綴りの注意** — `opts.stage ?? 'render'` は bare `stage` cluster に join しない（textual attribution ceiling・REQ-405-009 に明文化）
