# TASK-0290: MW-068 mutation 検証 + ledger 更新

<!-- spine:anchor:begin -->
> **Spine anchor**: [rounding-mode census 要件定義](../requirements.md) REQ-404
>
> - parent: `rounding-mode-census/requirements.md` REQ-404
> - role: `detailed`
> - status: `canonical_child`
<!-- spine:anchor:end -->

**要件**: [requirements.md](../requirements.md) REQ-404-007
**種別**: mutation 検証（RED 実測 → revert → GREEN 復元 → ledger 記載）　**推定工数**: 1.5h　**信頼性**: 🔵 *Phase 197 で確立した実装+MW+receipt 単一 commit 同梱規約の踏襲*

## 背景

census guard は GREEN だけでは「検出器として生きている」証明にならない。実事故 shape の mutation で RED を実測し、revert で GREEN 復元することで teeth を実証する（MW ledger 規約・PINNED_MIN_ENTRIES ratchet）。

## 実装

1. **mutation (a)**: roster 外の production file に `Math.floor(duration * options.fps)` probe を注入して新規 mixed cluster を作成 → completeness RED（未判定 cluster の全 site が RED）
2. **mutation (b)**: `src/export/enhanced-export-engine.ts` の `Math.ceil(duration * fps)` を `Math.round` に flip → negative anchor RED（worker の ceil が cluster を mixed 保持するため ALLOWED row は live のまま — anchor 単独捕捉）
3. **mutation (c)**: `src/export/animated-scene-renderer.ts` の `Math.round(duration * fps)` を `Math.ceil` に flip → cluster が単一 mode 化し ALLOWED 3 site 全てが stale-row RED + negative anchor RED
4. 各 mutation で Received の cluster/file:line を記録し revert で GREEN 復元
5. **ledger 記載** — `specs/speech-to-visuals/mutation-witness-ledger.md` に `MW-068` section（claim/target/mutation/command/observed ×3）+ appendix 5 列行を追記
6. **PINNED_MIN_ENTRIES 63→64**（`tests/guards/mutation-witness-ledger.test.ts`・phase history comment 更新）

## 検証

- 各 mutation で rounding-mode-census が RED（mutation 種別に応じ completeness / stale-row / negative anchor）
- revert で全 guard GREEN 復元
- mutation-witness-ledger guard が 64 entry で GREEN

## 設計決定

1. **同 commit 同梱** — Phase 197 (REQ-398) で確立した「実装 + MW + receipt 単一 commit」規約。MW だけ分離すると guard commit が teeth 未実証のまま land する
2. **mutation (b)/(c) は分類 site の mode flip** — ALLOWED roster の stale-row 検出（cluster から外れる方向の回帰）を実測。completeness 以外の検出経路があることが roster 運用の前提
3. **mutation (a) は新規注入** — roster にない file への新規違反が completeness pin で差し戻されることを実測
