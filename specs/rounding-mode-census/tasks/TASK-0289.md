# TASK-0289: rounding-mode census guard 新設 + 唯一 mixed cluster の ALLOWED 分類 + atomic dogfood landing（REQ-404・exact-0 固定）

<!-- spine:anchor:begin -->
> **Spine anchor**: [rounding-mode census 要件定義](../requirements.md) REQ-404
>
> - parent: `rounding-mode-census/requirements.md` REQ-404
> - role: `detailed`
> - status: `canonical_child`
<!-- spine:anchor:end -->

**要件**: [requirements.md](../requirements.md) REQ-404-001〜006
**種別**: TDD（純関数 discovery primitive → census test → ALLOWED 分類 → atomic dogfood landing）　**推定工数**: 3.5h　**信頼性**: 🔵 *REQ-403 が閉じた境界 class の出力側 + 実測 mixed cluster 1 件（file:line 付き）+ REQ-391〜403 census pattern の 13 例目*

## 背景

REQ-403（family 12）は比較の strict/inclusive 分裂を census で閉じた。本 TASK は同じ境界 class の出力側 — 同一式への `Math.round`/`floor`/`ceil` 混在 — を tree 全数で census する。機械的 discovery（空白正規化引数の clustering）で rounding site 242 / cluster 188 中に mixed cluster 1 件（`duration * fps`・3 site）を実測した。

## 実装

1. **`tests/guards/rounding-mode-census.test.ts` 新設**:
   - discovery primitive を export（`extractBalancedArg` / `normalizedInner` / `discoverRoundSites` / `clusterKey` / `mixedModeClusters`）し合成 fixture で liveness 検証
   - `CALL_RE` — `Math.(round|floor|ceil)(` の行内 paren-balance 引数抽出。comment 行 skip・複数行 wrap 対象外
   - real tree 未分類 site exact-0（`toEqual([])`）+ site/cluster floor pin（`>= 240` / `>= 185`・baseline 242/188）
   - ALLOWED roster 3 key（`duration * fps` の 2 domain 分割・domain 理由付き）+ ERADICATED confirmed-zero + negative anchor 3 件
2. **唯一の mixed cluster は ALLOWED 分類（unify しない）**:
   - renderer 側 `round` は per-scene Lottie keyframe timing の最近接 frame 構文（test suite が frame 9/141/150 を pin）
   - engine/worker 側 `ceil` は whole-export render loop の coverage 契約（末尾半 frame を落とさない）
   - src 変更なし — 現行 mode を negative anchor で固定する read-only census
3. **three-way family 13 登録** — `census-artifact-three-way.test.ts` に REQ-404 行（requirementsPath = 本 spec・authority list 9 family）
4. **atomic dogfood landing（REQ-404-006）** — 本 spec 一式の anchor と parent 側登録（`speech-to-visuals/architecture.md` children 2 行・`speech-to-visuals/design-interview.md` children 1 行・`speech-to-visuals/note.md` references 1 行）を同一 commit に同梱
5. **mutation 検証は TASK-0290**（REQ-404-007・MW-068）

## 検証

- rounding-mode-census 全 test GREEN（real tree exact-0 + 合成 fixture 検出）
- census-artifact-three-way GREEN（REQ-404 行 phrase 一致・authority 9 family）
- spine-edge-census GREEN（atomic landing の dogfood）
- tsc 両 config（app / test）exit 0

## 設計決定

1. **cluster key は空白正規化引数 text のみ** — `duration  *  fps` ≡ `duration * fps`。operand 順・変数名違いは join しない（誤マージ防止・同型綴りは single-source canon family の管轄）
2. **ALLOWED 非空の初適用（family 12 は 0 key だった）** — same-token 別 domain の偶然（`duration` が per-scene 量と whole-export 量）は domain 理由付きで分類する ratchet。REQ-399（ALLOWED 11 key）と同型の confirmed-roster 戦略
3. **ERADICATED confirmed-zero pin は複数行 + comment 形** — 単行 `= {};` は countRosterBlock が次 block に流血する（Phase 203 GOTCHA）
4. **実装は tests/ 配下のみ（src 変更ゼロ）** — 憲法の規模集計の予算外・guard 自身は production surface に含まれない
