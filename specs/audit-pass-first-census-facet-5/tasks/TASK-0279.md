# TASK-0279: type-narrow-as-any + any-annotate paired guard 新設 + family 6/7 登録（REQ-397・audit-driven 撲滅同梱）

<!-- spine:anchor:begin -->
> **Spine anchor**: [audit-pass-first-census-facet-5 要件定義](../requirements.md) REQ-397
>
> - parent: `audit-pass-first-census-facet-5/requirements.md` REQ-397
> - role: `detailed`
> - status: `canonical_child`
<!-- spine:anchor:end -->

**要件**: [requirements.md](../requirements.md) REQ-397-001〜007 / **受け入れ基準**: REQ-397-001 / REQ-397-003 テストケース + REQ-205 family 6/7 登録
**種別**: TDD (guard 新設 → audit-driven 撲滅同梱 → three-way family 6/7 登録 → mutation 検証)　**推定工数**: 6〜8h　**信頼性**: � *REQ-391/393 同型 audit-driven 撲滅同梱・47 site + 32 site 規模。audit 結果次第で confirmed-zero に切替可能な点を 🟡 として残す*

## 背景

REQ-393 score-ladder が `??`/`||` の silent-legit-zero 隠蔽を発見したのと**同型 class の type-system 側**として、`as any` cast（47 site）と `: any` 注釈（32 site）を census 化する。REQ-393 同型で **audit-driven 撲滅同梱**（REQ-391/393 precedent）または audit 結果次第で REQ-394 confirmed-zero に切替（REQ-397-006 🟡）。

検出対象は同根の type-system bypass だが、表現が異なる 2 形式（cast vs annotate）を **paired 新設** する。REQ-395 three-way guard precedent に従い、1 commit に複数 guard を batch。REQ-205 で family 6/7 として THREE_WAY table に登録。

## 実装

1. **type-narrow-as-any-census guard 新設** — `tests/guards/type-narrow-as-any-census.test.ts`: REQ-397-001 の 4 軸（`as any` / `as any as` / `// @ts-ignore` / `// @ts-expect-error`）で 1 行 discovery。ALLOWED 4 分類（test-mock / json-parse-narrowing / external-boundary / third-party-type-gap）と ERADICATED（generic cast）。reason 衛生 = REQ-391 5 分類命名規約踏襲（REQ-397-002）
2. **any-annotate-census guard 新設** — `tests/guards/any-annotate-census.test.ts`: REQ-397-003 の 3 軸（`: any` 注釈 / `<any>` 汎用型 / `any[]` bare array）で 1 行 discovery。ALLOWED 4 分類（external-input / third-party-sdk / migration-shim / dynamic-config-load）のみ・internal logic の parameter/return type は ALLOWED に上げず `unknown` 移行 canonical（REQ-397-004）
3. **test 除外規約** — REQ-397-007 で `src/` 配下の `.ts` / `.tsx` のみ対象・`**/__tests__/**` と `**/__mocks__/**` は ALLOWED 一律分類（ESM test mock の境界変数は production と同列に ERADICATED 化できない）
4. **walkProductionSurface 単一実装再利用** — REQ-302 で REQ-391 既存実装を `import` で再利用
5. **multiline discovery 上限の正直 doc 化** — REQ-203 で multiline cast（`as\n  any`）・multiline annotation は discovery 上限を header に明記
6. **negative anchor backup regex** — REQ-204 で cast 形（`as any` 単体）と annotation 形（`any[]` 単体）の backup を 1 個以上同梱
7. **family 6/7 paired 登録** — `tests/guards/census-artifact-three-way.test.ts` THREE_WAY table に REQ-397 = family 6 / REQ-397 = family 7 行追加。phrase は両 guard 実測 roster から構築（audit 完了後の数値）。`census-pin:F6:` / `census-pin:F7:` doc marker を各 guard header に付与（REQ-205, REQ-397-005）
8. **initial roster pin** — REQ-405 で audit 前に ship する場合は initial roster = 0 と明示・初回 run 発見 site を audit と一致確認の上で ERADICATED 登録（REQ-395 drift 教訓の proactive 反映）
9. **audit-driven 撲滅同梱** — REQ-397-006 で `as any` 47 site・`: any` 32 site の audit を実施し、generic cast / internal annotation を `unknown` 移行 / 型推論化 / 構造的 narrowing に置換。boundary external input の 4 分類のみ ALLOWED。残件は 0 に固定（confirmed-zero 化）
10. **MW-061 mutation 検証（3 独立 RED 実測）** — (a) ALLOWED row を 1 件 generic 分類で追加 → **1 failed**（reason-hygiene）(b) type-narrow-as-any ERADICATED を 1 件 generic cast で復活 → **1 failed**（negative anchor）(c) `census-pin:F7:` marker 破壊 → **1 failed**（family tie）

## 検証

- type-narrow-as-any-census 6/6 GREEN（4 軸 + counter liveness + multiline 限界）
- any-annotate-census 6/6 GREEN（3 軸 + counter liveness + boundary 4 分類整合）
- three-way guard 10/10 GREEN（family 6/7 追加で既存 5 family 不変）
- 撲滅後 audit: ERADICATED 候補が全 site `unknown` 移行済み・boundary 4 分類以外は 0 件
- tsc 両 config（app/test）exit 0
- specs 整合: `specs:anchor:check` / `specs:mirror:check` / `spine:validate` 全 GREEN
- 台帳 dogfood: MW-061 entry + appendix row・監査 pin **≥60 → ≥61**・PINNED_MIN_ENTRIES 更新

## 設計決定

1. **paired 新設 = 同一 commit** — REQ-395 three-way guard precedent に従い、type-narrow-as-any-census（F6）と any-annotate-census（F7）を 1 iteration で batch。検出 regex は異なるが対象 class は同根（type-system bypass の 2 表現）であり、ALLOWED 4 分類の共通化で cross-check 効率を上げる（REQ-397-005）
2. **boundary 4 分類厳守** — REQ-397-004 で ALLOWED 分類を boundary（external-input / third-party-sdk / migration-shim / dynamic-config-load）に限定。internal logic の parameter / return type の `: any` を ALLOWED に上げない設計判断（REQ-393 `?? 数字` → fail-closed 0 移行と同型）。`unknown` 移行可能な site は ERADICATED 候補
3. **audit-driven 撲滅同梱 vs confirmed-zero 切替** — REQ-397-006 🟡: 47 + 32 site 規模は REQ-393 score-ladder の 29→21 site 撲滅 precedent と類似のため audit-driven で初手から発見・修正同梱が妥当。ただし audit 結果が 0 件（既に撲滅済み）であれば REQ-394 confirmed-zero に切替
4. **test 除外規約** — REQ-397-007 で ESM test mock の境界変数（`as any`/`unknown` 注釈）を一律 ALLOWED 化。production code と同列に ERADICATED 化できない構造的判断

## 関連文書

- **要件定義**: [requirements.md](../requirements.md) REQ-397
- **コンテキストノート**: [note.md](../note.md) §1.2, §1.3
- **分析記録**: [interview-record.md](../interview-record.md) A1, A2, A4
- **直近 predecessor**: [TASK-0278](TASK-0278.md)（REQ-396 stale-comment-census）
- **直近 predecessor**: [TASK-0277](../../speech-to-visuals/tasks/TASK-0277.md)（REQ-395 three-way guard）

## 信頼性レベルサマリー

- **背景**: � *REQ-393 score-ladder 滴り発見 precedent 直接延長*
- **実装**: � *REQ-397-001〜005, REQ-205 を 10 ステップで全カバー*
- **検証**: 🔵 *MW 規約（3 独立 mutation）+ three-way guard family 6/7 paired 登録*
- **設計決定**: 🟡 *audit-driven 撲滅同梱 vs confirmed-zero 切替は audit 結果次第*
