# TASK-0293: title-sync violation 2 kind 新設 + confirmed-zero census + atomic dogfood landing（REQ-406・confirmed-zero pin）

<!-- spine:anchor:begin -->
> **Spine anchor**: [spine registry title-sync census 要件定義](../requirements.md) REQ-406
>
> - parent: `spine-registry-title-sync/requirements.md` REQ-406
> - role: `detailed`
> - status: `canonical_child`
<!-- spine:anchor:end -->

**要件**: [requirements.md](../requirements.md) REQ-406-001〜005・007
**種別**: TDD（純関数 helper → violation kind 拡張 → fixture 更新 → confirmed-zero census → atomic dogfood landing）　**推定工数**: 3h　**信頼性**: 🔵 *90c924db→47d71cd5 の事故一次資料 + 現 tree 112 entry 実測 drift 0 + REQ-402 guard 構造への直接拡張*

## 背景

REQ-402 は registry entry の**存在**を強制したが表題 text は未検証で、
90c924db（REQ-404 本体 commit）が子 spec 改題を親 index 同期なしで land し、
修復が `chore(make-run): commit 2 remaining change(s)`（47d71cd5）に分離した。
本 TASK は表題面の同一 commit 同期を構造的に強制する violation kind を
REQ-402 guard に追加する。撲滅対象なし（現 tree は drift 0 = confirmed-zero）。

## 実装

1. **`tests/guards/spine-edge-contract.ts` 拡張**:
   - violation kind 2 件追加 — `REGISTRY_TITLE_DRIFT`（entry 表題 ≠ 対象の
     最初の H1）・`REGISTRY_TARGET_H1_MISSING`（対象が H1 を持たない）
   - 純関数 `firstHeading(content)` を export（行頭 strict `^#\s+`・前後空白
     除去・空 H1 は null）
   - report に `titleChecked`（表題比較まで実施した entry 数）を追加 —
     検証 silent skip の検出器
   - 管轄外（anchor block 内表題・並び順・manifest 再生成・全文一致以外の
     緩い一致）を header doc comment に宣言（REQ-406-007）
2. **`tests/guards/spine-edge-census.test.ts` 拡張**:
   - `childrenBlock` / `referencesBlock` fixture helper を `[title, link]`
     tuple 化し、`canonicalTree` の表題を対象 H1 と一致させる（正規形が
     表題 sync を含む）+ 既存 6 kind test の呼び出し側更新
   - 新規 fixture test — (a) 子のみ改題の事故 shape（90c924db→47d71cd5 の
     再現・修復で GREEN 復帰）・(b) holder 側表題 typo・(c) H1 欠落・
     (d) `firstHeading` 境界 6 例
   - 実 tree test — 表題違反 exact-0 + `titleChecked >= 112` floor pin
3. **atomic dogfood landing（REQ-406-005）** — 本 spec 一式（6 file）の
   anchor と parent 側登録（`speech-to-visuals/architecture.md` children
   2 行・`speech-to-visuals/design-interview.md` children 1 行・
   `speech-to-visuals/note.md` references 1 行）を guard・MW・ledger と
   同一 commit に同梱。登録**前**に census が `PARENT_UNREGISTERED` ×4 で
   RED になることを実測してから登録して GREEN にする（sweep commit 残さず）
4. **mutation 検証は TASK-0294**（REQ-406-006・MW-070）

## 検証

- spine-edge-census 全 test GREEN（31 test — 実 tree exact-0 + 表題違反 0 +
  titleChecked 116（112 + 本 spec 4 登録））
- 登録前 RED（`PARENT_UNREGISTERED` ×4）→ 登録後 GREEN の dogfood 実測
- tsc 両 config（app / test）exit 0
- sibling guard（spine-anchor-role-census・mutation-witness-ledger）GREEN

## 設計決定

1. **根拠は doc 本文（H1）のみ** — manifest 等の複製を根拠にすると複製間
   一致で満たされ実 doc との drift を検出できない
2. **children / references 両方に適用** — entry は閲覧入口であり表題 stale
   の害は方向性に依存しない（REQ-402 の back-anchor 要求軸とは別軸）
3. **`titleChecked` 計数契約** — confirmed-zero guard の rot（検証の
   silent skip）は違反ではなく計数の落下として検出する
4. **並び順は契約外** — 47d71cd5 diff の順序正規化部分は人間規約として
   機械契約に含めない（REQ-406-007）
