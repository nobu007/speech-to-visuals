# TASK-0294: MW-070 mutation 検証 + ledger 更新

<!-- spine:anchor:begin -->
> **Spine anchor**: [spine registry title-sync census 要件定義](../requirements.md) REQ-406
>
> - parent: `spine-registry-title-sync/requirements.md` REQ-406
> - role: `detailed`
> - status: `canonical_child`
<!-- spine:anchor:end -->

**要件**: [requirements.md](../requirements.md) REQ-406-006
**種別**: mutation 検証（RED 実測 → revert → GREEN 復元 → ledger 記載）　**推定工数**: 1.5h　**信頼性**: 🔵 *Phase 197 で確立した実装+MW+receipt 単一 commit 同梱規約の踏襲*

## 背景

confirmed-zero pin の guard は GREEN だけでは「検出器として生きている」
証明にならない（検出条件が空振りしても GREEN のまま）。実事故 shape の
mutation で RED を実測し、revert で GREEN 復元することで teeth を実証する
（MW ledger 規約・PINNED_MIN_ENTRIES ratchet）。

## 実装

1. **mutation (a) 子 H1 改題のみ**: `specs/boundary-operator-census/requirements.md`
   の H1（先頭行）を一時変更し親 index を旧ままにする → 90c924db 事故の
   再現（`REGISTRY_TITLE_DRIFT`）
2. **mutation (b) 親 index 側の表題 typo**:
   `specs/speech-to-visuals/architecture.md` children の 1 行の表題 text を
   一時変更（対象 doc は不変）→ 同一違反の holder 側編集経路
3. **mutation (c) 対象 doc の H1 除去**: `specs/fallback-default-census/note.md`
   の H1 行を一時除去 → `REGISTRY_TARGET_H1_MISSING`
4. 各 mutation で spine-edge-census を実行し Received の file:line と表題を
   記録、revert（.bak 復元）で GREEN 復元
5. **ledger 記載** — `specs/speech-to-visuals/mutation-witness-ledger.md` に
   `MW-070` section（claim/target/mutation/command/observed ×3）+ appendix
   5 列行を追記（cell 内に `|` 文字を含めない — shape guard が `split('|')`
   で列数検証するため）
6. **PINNED_MIN_ENTRIES 65→66**（`tests/guards/mutation-witness-ledger.test.ts`・
   phase history comment 更新）

## 検証

- 各 mutation で spine-edge-census が RED（TITLE_DRIFT / TITLE_DRIFT /
  H1_MISSING）・exact-0 test が違反を列挙
- revert で全 guard GREEN 復元・`git status --short specs/` が意図 file のみ
- mutation-witness-ledger guard（+ shape guard）が 66 entry で GREEN

## 設計決定

1. **同 commit 同梱** — Phase 197 (REQ-398) で確立した「実装 + MW + receipt
   単一 commit」規約。MW だけ分離すると guard commit が teeth 未実証のまま
   land する
2. **mutation (a) は実事故の再現** — 90c924db が抱えた drift と同じ操作
   （子改題・index 不変）を当該 file で再実行し、検出経路を実測する
3. **mutation (c) は第 2 kind の独立検証** — H1 欠落は TITLE_DRIFT とは
   別 kind であり、片方の検出が片方を代理しないことを実証する
