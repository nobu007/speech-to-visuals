# TASK-0002: 2 family 移行と fingerprint 等価証明

**タスクID**: TASK-0002
**タスクタイプ**: TDD
**推定工数**: 5時間
**フェーズ**: Phase 1 - harness 抽出
**信頼性レベル**: 🔵 *REQ-004・architecture.md D4/D6/D10 より*

## 関連文書

- **概要**: [📋 overview.md](overview.md)
- **要件定義**: [📋 requirements.md](../requirements.md) - REQ-004 / REQ-401〜403
- **技術設計**: [📐 architecture.md](../architecture.md) - D4 / D6 / D10

## タスク概要

round 46〜50 の 5 family のうち最大（grid-packing 440 行）と最小
（default-node-extent 365 行）を harness 移行し、移行前後の assertion 集合が
fingerprint（family:rowId:expectations 列挙）で等価であることを証明する。
Layer 2 semantic pin は per-family test に残置（D1）。

## 依存タスク

- **前提タスク**: [TASK-0001](TASK-0001.md)
- **後続タスク**: なし（TASK-0003 とは独立）

## 完了条件

- [x] 両 family の Layer 1 + Layer 3 が `describeSingleSource()` の data row で宣言される
- [x] retired 式と corpus は per-family test 内に残置（`*-retired.ts` 分離しない・D4）
- [x] `harness-fingerprint.test.ts` が 2 family の `family:rowId:expectations` 列挙を pin
- [x] 移行前後の差分（it.each 折りたたみ等）が fingerprint test に理由付きで記録
- [x] mutation RED 5 種以上（ban 削除 / 回数 ±1 / corpus 縮小 / 正典変異 / witness 除去相当）を確認
- [x] 実行時間が移行前（131.2s 実測）の ±20% 以内（REQ-403）
- [x] production `src/` の diff 0（REQ-401）

---

## 主要実装項目

### grid-packing Layer 1/3 の row 化 🔵

**信頼性**: 🔵 *grid-packing-single-source.test.ts（440 行）の実読より*

Layer 1 = legacy 式 6 個（columns / rows / aspect / stamp A / stamp B /
matrix 2-step B）× corpus。stamp A は delta mode（witness 強制・整数系厳密は
別 object-is row）。Layer 3 = 13 file の delegation 形状 occurs + RAW ban
（計数は行ベースに統一 — D3）。

**実装ファイル**: `tests/guards/grid-packing-single-source.test.ts`

### default-node-extent Layer 1/3 の row 化 🔵

**信頼性**: 🔵 *default-node-extent-single-source.test.ts（365 行）の実読より*

Layer 1 = two-const pair ×2 / inline read ×2（249 case × 4 row）/ cycle maxima
×2 / scaledExtent ×2。`it.each` は corpus ループへ折りたたみ（D6 許容差分）。
Layer 3 = RAW_PAIR ban（`expect(src).not.toMatch` は scope `'source'` で保存）
+ delegation occurs。

**実装ファイル**: `tests/guards/default-node-extent-single-source.test.ts`

### fingerprint test 🔵

**信頼性**: 🔵 *round 35 before/after fingerprint 手法の再利用（D6）より*

`countExpectations()` 由来の期待数列挙を文字列 pin。corpus 縮小・row 削除・
ban 削除は列挙変化 = RED（TC-004-E01 の恒久 guard 化）。

**テストファイル**: `tests/guards/harness-fingerprint.test.ts`

---

## 基本的なテスト要件

### mutation RED（TC-004-E01） 🔵

**信頼性**: 🔵 *round 43〜50 の慣行（5 種以上）より*

row 変異（occurs 回数 ±1 / ban row 削除 / corpus 縮小）と正典変異
（layout-utils の式変更）で対応生成 test が RED になることを cp backup 付きで
実行確認する。

---

## 信頼性レベルサマリー

- **総項目数**: 4項目
- 🔵 **青信号**: 4項目 (100%)
- 🟡 **黄信号**: 0項目 (0%)
- 🔴 **赤信号**: 0項目 (0%)

**品質評価**: 高品質（移行対象の実形状が既に解剖済み）
