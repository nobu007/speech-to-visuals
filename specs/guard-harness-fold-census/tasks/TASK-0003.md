# TASK-0003: fold 収束 census の機械化

<!-- spine:anchor:begin -->
> **Spine anchor**: [speech-to-visuals アーキテクチャ設計](../../speech-to-visuals/architecture.md)
>
> - parent: `speech-to-visuals/architecture.md`
> - role: `detailed`
> - status: `canonical_child`
<!-- spine:anchor:end -->

**タスクID**: TASK-0003
**タスクタイプ**: TDD
**推定工数**: 4時間
**フェーズ**: Phase 2 - census 機械化
**信頼性レベル**: 🔵（計測ベースライン）+ 🟡（ratchet/doc-pin は新規機構）

## 関連文書

- **概要**: [📋 overview.md](overview.md)
- **要件定義**: [📋 requirements.md](../requirements.md) - REQ-005 / REQ-103 / REQ-201〜202 / REQ-404・census 表
- **技術設計**: [📐 architecture.md](../architecture.md) - D7〜D9

## タスク概要

残存 inline site を family × site 数 × 分類で数値化した census を
guard test として機械化する。`buildCensusSnapshot()`（src/ を 1 パス walk・
コメント行除外）で全 family の pattern を評価し、data pin と
requirements.md の doc-pin marker の 3 者一致を検証する。

## 依存タスク

- **前提タスク**: なし（TASK-0001/0002 と独立）
- **後続タスク**: なし

## 完了条件

- [x] `fold-census-families.ts` が C1〜C5 の data row（pattern・理由付き exclude・pin・分類）と `FOLD_SERIES_STATUS` を持つ
- [x] census 計測が code-line ベース・単一 engine・`import.meta.url` 起点（REQ-404 / NFR-101）
- [x] 要件 census 表の数値が engine 由来に再ベースラインされ、`<!-- census-pin:… -->` marker が埋め込まれる
- [x] guard test が marker 値 = data pin = engine 実測の 3 者一致を検証（増減どちらも RED・D8）
- [x] `FOLD_SERIES_STATUS.valueNeutralCandidates` が `[]` pin で収束を表明（REQ-201）
- [x] family id 列挙 pin により 0 化時の無声削除を防止（EDGE-102）
- [x] mutation（site 追加 / marker 改変）で RED を確認

---

## 主要実装項目

### census data（C1〜C5 + 収束 status） 🔵

**信頼性**: 🔵 *interview-record A2 の実測・architecture.md D7 の code-line 再実測（C1=30/20・C3=4/1・C4=1/1・C5=1/1）より。C2 は pattern 精密化後に再計測 🟡*

`patterns`（行ベース）・`exclude`（正典 file・理由必須）・`excludeLinePatterns`
（C2 の偽陽性源）・`pin`・`classification`。

**実装ファイル**: `tests/guards/fold-census-families.ts`

### census guard test 🔵

**信頼性**: 🔵 *frozen-literal registry の pin+sweep と同型・D8/D9 より*

`buildCensusSnapshot()` の 1 パス walk → family 別 sites/files を pin と
`toEqual` 比較（増加・減少どちらも RED + 指示メッセージ）+ requirements.md の
marker 突合 + family id 列挙 pin。

**テストファイル**: `tests/guards/fold-census-guard.test.ts`

---

## 基本的なテスト要件

### コード行フィルタ安定性（TC-005-02） 🔵

**信頼性**: 🔵 *codeLines() の既存実績より*

コメントに該当形状を含む行が計測対象外であることを合成行セットで検証
（EDGE-002）。

---

## 信頼性レベルサマリー

- **総項目数**: 3項目
- 🔵 **青信号**: 2項目 (67%)
- 🟡 **黄信号**: 1項目 (33%)（C2 pattern 精密化は実測確定待ち）
- 🔴 **赤信号**: 0項目 (0%)

**品質評価**: 高品質（ベースライン実測済み・ratchet は既存手法の適用）
