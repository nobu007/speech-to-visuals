# TASK-0288: MW-067 mutation 検証 + ledger 更新

<!-- spine:anchor:begin -->
> **Spine anchor**: [boundary strictness census 要件定義](../requirements.md) REQ-403
>
> - parent: `boundary-operator-census/requirements.md` REQ-403
> - role: `detailed`
> - status: `canonical_child`
<!-- spine:anchor:end -->

**要件**: [requirements.md](../requirements.md) REQ-403-007
**種別**: mutation 検証（RED 実測 → revert → GREEN 復元 → ledger 記載）　**推定工数**: 1.5h　**信頼性**: 🔵 *Phase 197 で確立した実装+MW+receipt 単一 commit 同梱規約の踏襲*

## 背景

census guard は GREEN だけでは「検出器として生きている」証明にならない。実事故 shape の mutation で RED を実測し、revert で GREEN 復元することで teeth を実証する（MW ledger 規約・PINNED_MIN_ENTRIES ratchet）。

## 実装

1. **mutation (a)**: `src/monitoring/health-check-service.ts` の `successRate >= 0.95` を `> 0.95` に revert → census completeness が RED（mixed cluster 再出現）
2. **mutation (b)**: 未分類 file（roster 外の production file）に新規 mixed cluster を注入（例: 既存 `>= 0.8` site を持つ metric への strict site 追加）→ completeness RED
3. **mutation (c)**: `src/analysis/diagram-detector.ts` の `metrics.confidence >= 0.5` を `> 0.5` に revert → completeness + negative anchor が RED
4. 各 mutation で Received の cluster/file:line を記録し revert で GREEN 復元
5. **ledger 記載** — `specs/speech-to-visuals/mutation-witness-ledger.md` に `MW-067` section（claim/target/mutation/command/observed ×3）+ appendix 5 列行を追記
6. **PINNED_MIN_ENTRIES 62→63**（`tests/guards/mutation-witness-ledger.test.ts`・phase history comment 更新）

## 検証

- 各 mutation で boundary-operator-census が RED（mutation 種別に応じ completeness / eradicated-reappear / negative anchor）
- revert で全 guard GREEN 復元
- mutation-witness-ledger guard が 63 entry で GREEN

## 設計決定

1. **同 commit 同梱** — Phase 197 (REQ-398) で確立した「実装 + MW + receipt 単一 commit」規約。MW だけ分離すると guard commit が teeth 未実証のまま land する
2. **mutation (a)/(c) は実撲滅 site の revert** — 正規形への回帰が検出器を通ることを実測（単なる合成注入ではない）
3. **mutation (b) は新規注入** — roster にない file への新規違反が ALLOWED 空 pin で差し戻されることを実測
