# TASK-0287: boundary strictness census guard 新設 + 3 cluster 撲滅 + atomic dogfood landing（REQ-403・exact-0 固定）

<!-- spine:anchor:begin -->
> **Spine anchor**: [boundary strictness census 要件定義](../requirements.md) REQ-403
>
> - parent: `boundary-operator-census/requirements.md` REQ-403
> - role: `detailed`
> - status: `canonical_child`
<!-- spine:anchor:end -->

**要件**: [requirements.md](../requirements.md) REQ-403-001〜006
**種別**: TDD（純関数 discovery primitive → census test → 3 site 撲滅 → atomic dogfood landing）　**推定工数**: 4h　**信頼性**: 🔵 *0.6 正典の実証済み事故 class + 実測 3 cluster（file:line 付き）+ REQ-391〜402 census pattern の 12 例目*

## 背景

`GOOD_DETECTION_CONFIDENCE_THRESHOLD = 0.6` 正典は「同じ値への strict/inclusive 演算子分歧が endpoint 間不同意を生む」事故を記録し、value と operator の一元化で解決した。本 TASK は同型 shape を tree 全数で census する。機械的 discovery（identifier × 正規化 decimal literal × 方向 の clustering）で comparison site 145 / cluster 126 中に mixed-strictness cluster 3 件（successRate @ 0.95 / successRate @ 0.8 / confidence @ 0.5）を実測した。

## 実装

1. **`tests/guards/boundary-operator-census.test.ts` 新設**:
   - discovery primitive を export（`strictness` / `direction` / `canonicalLiteral` / `discoverCmpSites` / `clusterKey` / `mixedStrictnessClusters`）し合成 fixture で liveness 検証
   - `CMP_RE` — identifier（末尾 attribution・閉じ括弧介在可）× 比較演算子 × decimal literal。comment 行 skip・整数 literal 対象外
   - real tree mixed cluster exact-0（`toEqual([])`）+ site/cluster floor pin（`>= 140` / `>= 120`・baseline 145/126）
   - ALLOWED roster 空（ALLOWED 0 key）+ ERADICATED 3 key + negative anchor 6 件
2. **3 site 撲滅（boundary-INCLUSIVE 統一）**:
   - `src/monitoring/health-check-service.ts:434` `> 0.95` → `>= 0.95`
   - `src/monitoring/health-check-service.ts:437` `> 0.80` → `>= 0.80`（spelling 保持・正規化で同一 cluster）
   - `src/analysis/diagram-detector.ts:1037` `> 0.5` → `>= 0.5`
   - 統一方向は 0.6 正典規約（EQUALS は threshold 合格）+ detector test の `toBeGreaterThanOrEqual` pin + `/health` route 現行 `>= 0.95` の 3 根拠
3. **three-way family 12 登録** — `census-artifact-three-way.test.ts` に REQ-403 行（requirementsPath = 本 spec・authority list 8 family）
4. **atomic dogfood landing（REQ-403-006）** — 本 spec 一式の anchor と parent 側登録（`speech-to-visuals/architecture.md` children 2 行・`speech-to-visuals/design-interview.md` children 1 行・`speech-to-visuals/note.md` references 1 行）を同一 commit に同梱
5. **mutation 検証は TASK-0288**（REQ-403-007・MW-067）

## 検証

- boundary-operator-census 全 test GREEN（real tree exact-0 + 合成 fixture 検出）
- census-artifact-three-way GREEN（REQ-403 行 phrase 一致）
- spine-edge-census GREEN（atomic landing の dogfood）
- health-check-service / diagram-detector 既存 test GREEN（境界 fixture は 0.98/0.85/0.70 で演算子変更の影響外）
- tsc 両 config（app / test）exit 0

## 設計決定

1. **cluster key は (identifier, `String(Number(literal))`, 方向)** — `0.80`/`0.8` spelling違いが別 cluster に隠れるのを正規化で防止（実 cluster で実証済み）。方向区別で相補 ladder（if `>=` / else `<`）は偽陽性にしない
2. **整数 literal 対象外** — 整数は float で正確に表現でき strict/inclusive 分歧は到達しない（REQ-403-001）。`avgProcessingTime < 60000` 系は既存 operator pin の管轄
3. **ALLOWED 空 confirmed-zero pin** — 誤マージ（same-token 別 domain）が実在した場合は正当理由付き ALLOWED 分類に昇格する ratchet
4. **実装は tests/ 配下 + src 3 行修正のみ** — 憲法の規模集計の予算外・guard 自身は production surface に含まれない
