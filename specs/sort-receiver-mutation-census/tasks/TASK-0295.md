# TASK-0295: sort-receiver-mutation census guard 新設 + ALLOWED 24 key 分類 + three-way family 16 登録 + atomic dogfood landing（REQ-407・confirmed-roster pin）

<!-- spine:anchor:begin -->
> **Spine anchor**: [sort-receiver-mutation census 要件定義](../requirements.md) REQ-407
>
> - parent: `sort-receiver-mutation-census/requirements.md` REQ-407
> - role: `detailed`
> - status: `canonical_child`
<!-- spine:anchor:end -->

**要件**: [requirements.md](../requirements.md) REQ-407-001〜009
**種別**: TDD（純関数 discovery primitive → census test → ALLOWED 分類 → three-way 登録 → atomic dogfood landing）　**推定工数**: 3.5h　**信頼性**: 🔵 *実測 `.sort(` 74 site / in-place 24 / `.reverse(` 2・comparator-less 0（file:line 付き）+ REQ-391〜406 census pattern の 16 例目*

## 背景

`.sort()` / `.reverse()` は receiver 自身を返す — `return items.sort(byX)` は sorted view ではなく caller 配列の破壊的並べ替え。family 16 として receiver 判別（fresh-producer 形 vs in-place dotted 形）を tree 全数で census する。実測は in-place 24 site 全て LOCAL-BUILT / OWN-FIELD で aliased-input 0 件（confirmed-clean）だが、guard 初回 RED が手動分類漏れ 3 site を捕捉し、roster exact both-ways の teeth は実効的。

## 実装

1. **`tests/guards/sort-receiver-mutation-census.test.ts` 新設**:
   - discovery primitive を export（`discoverDestructiveArraySites`）し合成 fixture 8 例で liveness 検証（incident shape・fresh 4 形・continuation 跨ぎ・optional-call・`this.field` chain・comparator-less 計上・comment skip・index access ceiling）
   - real tree in-place site の roster exact both-ways（completeness + stale-row）+ floor pin（sort >= 72 / in-place >= 24 / reverse >= 2・baseline 74/24/2）
   - axis 2: `.reverse()` in-place exact-0 + copy 形 negative anchor 2 件
   - axis 3: comparator-less `.sort()` production exact-0
   - ALLOWED roster 24 key（LOCAL-BUILT 22 + OWN-FIELD 2・理由付き）+ ERADICATED confirmed-zero block（複数行形式）+ canonical 形 negative anchor（fresh view sort 3 件 + OWN-FIELD 2 件）
2. **three-way family 16 登録** — `census-artifact-three-way.test.ts` に REQ-407 行（requirementsPath = 本 spec・authority list 11 family）
3. **atomic dogfood landing（REQ-407-007）** — 本 spec 一式の anchor と parent 側登録（`speech-to-visuals/architecture.md` children 2 行・`speech-to-visuals/design-interview.md` children 1 行・`speech-to-visuals/note.md` references 1 行）を guard・MW・ledger と同一 commit に同梱。登録前に spine-edge census が `PARENT_UNREGISTERED` ×4 で RED になることを実測してから登録して GREEN にする（index 表題は対象 doc の H1 と全文一致）
4. **mutation 検証は TASK-0296**（REQ-407-006・MW-071）

## 検証

- sort-receiver-mutation-census 全 test GREEN（roster exact both-ways + reverse/comparator-less exact-0 + 合成 fixture）
- census-artifact-three-way GREEN（REQ-407 行 phrase 一致・authority 11 family）
- spine-edge-census GREEN（atomic landing の dogfood・`PARENT_UNREGISTERED` ×4 RED の実測付き）
- mutation-witness-ledger GREEN（PINNED_MIN_ENTRIES 67）
- tsc 両 config（app / test）exit 0

## 設計決定

1. **receiver 判別は call 直前 text のみ** — bare dotted chain（末尾 `?` 含む）→ in-place、`)` / `]` 終端 → produced。call 結果に alias は生成されないため produced は安全側。index access receiver は produced 分類になる ceiling を doc comment に明示
2. **chain continuation は前行 tail から解決**（≤3 行 look-back）— `Array.from(...)\n.sort(fn)` を produced に、`data.times\n.sort(fn)` を in-place に落とす
3. **ERADICATED 0 の confirmed-clean pin** — aliased-input 0 件のため src 変更ゼロ。copy 形への統一強制は LOCAL-BUILT 確定 sort への無駄 copy churn（census honesty）
4. **roster key は `file:line:receiver`** — family 14 と同じ分離規約。receiver rename は stale-row + completeness の二重 RED（MW-071 mutation (b) が実証）
