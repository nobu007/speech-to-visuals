# TASK-0296: MW-071 mutation 検証 + ledger 更新（REQ-407-006）

<!-- spine:anchor:begin -->
> **Spine anchor**: [sort-receiver-mutation census 要件定義](../requirements.md) REQ-407-006
>
> - parent: `sort-receiver-mutation-census/requirements.md` REQ-407-006
> - role: `detailed`
> - status: `canonical_child`
<!-- spine:anchor:end -->

**要件**: [requirements.md](../requirements.md) REQ-407-006
**種別**: DIRECT（mutation 適用 → RED 実測 → revert → GREEN 復元 → ledger 記載）　**推定工数**: 1.5h　**信頼性**: 🔵 *MW-067〜070 と同じ同 commit 同梱規約（Phase 197 標準）*

## 背景

guard の teeth は宣言ではなく実測。3 独立 mutation で各 axis・各検出経路の RED を実事故 shape で確認する。

## 実装

1. **mutation (a) — canonical fresh 形の in-place 化（incident 再現）**:
   `src/monitoring/real-time-performance-monitor.ts` の
   `percentileCeil([...values].sort((a, b) => a - b), percentile)` を
   `percentileCeil(values.sort((a, b) => a - b), percentile)` に書き換え。
   percentile 計算が caller の samples 配列を破壊する実 incident shape —
   completeness（新規未分類 in-place site）+ negative anchor（copy 形 spelling）の二重 RED を予言
2. **mutation (b) — roster receiver rename（二重捕捉）**:
   `src/remotion/animation-strategies.ts:143` の `indexed.sort(` を
   `edgeIndexed.sort(` に rename（宣言行含む）— stale-row（旧 key `:143:indexed`）+ completeness（新 key `:143:edgeIndexed`）の同時 RED を予言
3. **mutation (c) — reverse copy 形の in-place 化（axis 2 単独発火）**:
   `src/export/export-job-queue.ts` の
   `return [...this.deadLetterQueue].reverse();` を
   `return this.deadLetterQueue.reverse();` に書き換え — axis 2（reverse in-place exact-0）+ negative anchor の RED を予言
4. 各 mutation を `.bak` 保存 → 適用 → guard run で RED 実測 → revert → GREEN 復元を確認
5. **ledger 記載** — `specs/speech-to-visuals/mutation-witness-ledger.md` に MW-071 section（claim / target / mutation / command / observed / date）+ appendix 5 列行を追記し、`tests/guards/mutation-witness-ledger.test.ts` の `PINNED_MIN_ENTRIES` を 66→67 に更新（comment 列にも追記）

## 検証

- 各 mutation 適用時: `sort-receiver-mutation-census` suite が予言した test で RED
- revert 後: 9/9 GREEN 復元・`git status --short src/` が意図 file のみ
- `mutation-witness-ledger` suite GREEN（PINNED 67・MW-071 の field 完備）
- `mutation-witness-ledger-shape` GREEN（appendix 行の shape）

## 注意事項

- appendix 行の cell に `|` 文字を使わない（shape guard が split('|') で列数検証）
- mutation (c) は persistent queue を破壊する実挙動変化を含むため、RED 実測後に必ず `.bak` から復元する
