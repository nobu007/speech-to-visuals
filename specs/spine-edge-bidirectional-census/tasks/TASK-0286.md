# TASK-0286: MW-066 mutation 検証 + 台帳 pin 更新（REQ-402-007・guard commit から分離実施）

<!-- spine:anchor:begin -->
> **Spine anchor**: [spine edge 双方向 census 要件定義](../requirements.md) REQ-402-007
>
> - parent: `spine-edge-bidirectional-census/requirements.md` REQ-402-007
> - role: `detailed`
> - status: `canonical_child`
<!-- spine:anchor:end -->

**要件**: [requirements.md](../requirements.md) REQ-402-007
**種別**: mutation 検証（実 tree 3 独立 mutation → RED 実測 → revert GREEN）　**推定工数**: 1.5h　**信頼性**: 🔵 *Phase 196（MW-060/061）で確立した分離実施 pattern の踏襲*

## 背景

REQ-402 guard（TASK-0285）は confirmed-zero 固定（real tree violations 0）。teeth の実証は guard を land させた commit から分離して実施する（Phase 196 precedent: guard commit に mutation を同梱すると RED 実測の妥当性検証が同じ commit の GREEN 主張に混ざる）。

## 実装（3 独立 mutation — いずれも実 file への一時適用）

1. **(a) 事故再現（c818286f~1 shape）**: `specs/audit-pass-first-census-facet-5/requirements.md` の anchor parent を `speech-to-visuals/requirements.md` に re-parent（同 file の children 登録は architecture.md 側にしか無いため forward 違反になる）→ **期待: 1 failed**（`PARENT_UNREGISTERED`）
2. **(b) 登録行削除**: `speech-to-visuals/architecture.md` children から facet-5 requirements 行を削除 → **期待: 1 failed**（forward + children back-anchor 双方向の違反）
3. **(c) phantom 登録**: `speech-to-visuals/architecture.md` children に存在しない対象（`../spine-edge-bidirectional-census/ghost.md`）を追加 → **期待: 1 failed**（`REGISTRY_TARGET_MISSING` + back-anchor）

各 mutation を `.bak` 保存 → 適用 → `npx jest spine-edge-census` で RED 実測 → `.bak` 復元で GREEN 復元。

## 検証

- 3 mutation 各 1+ failed 実測（Received に violation kind / detail を引用）
- revert で spine-edge-census 全 GREEN + role census（REQ-388）GREEN + `git diff` 上 mutation 残骸なし
- 台帳 dogfood: `mutation-witness-ledger.md` に MW-066 の 5 列 template 行追加・`tests/guards/mutation-witness-ledger.test.ts` の `PINNED_MIN_ENTRIES` 61 → 62・shape guard GREEN

## 設計決定

1. **実 tree mutation でなく合成 fixture にしない** — guard の検出能力ではなく「実在 file のこの編集が CI で落ちる」こと（= 事故の再発阻止）を実証するのが目的
2. **mutation (b) は forward/reverse 両方に掛かる** — 登録行 1 行の削除が children back-anchor と forward の両 violation を出すことは契約の二重捕捉（MW-060 precedent の "双方向捕捉" 記載と同じ記法で記録する）
