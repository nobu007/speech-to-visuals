# TASK-0285: spine edge 双方向 census guard 新設 + atomic dogfood landing（REQ-402・exact-0 固定）

<!-- spine:anchor:begin -->
> **Spine anchor**: [spine edge 双方向 census 要件定義](../requirements.md) REQ-402
>
> - parent: `spine-edge-bidirectional-census/requirements.md` REQ-402
> - role: `detailed`
> - status: `canonical_child`
<!-- spine:anchor:end -->

**要件**: [requirements.md](../requirements.md) REQ-402-001〜006
**種別**: TDD（module 純関数 → census test → atomic dogfood landing）　**推定工数**: 4h　**信頼性**: 🔵 *c818286f 事故の実証済み盲点 + REQ-388 構成の直接延長・実 tree 全数観測で exempt 規則が確定済み*

## 背景

656a0d58/bb844a0f が land した facet-5 spec 2 件の anchor は `parent: speech-to-visuals/requirements.md` を宣言したが、宣言先に `spine:children` block が存在しない片方向 dangling anchor で、REQ-388 guard（anchor block 単体の shape 検査）は GREEN のまま通過した。修復は c818286f の後付け sweep commit に分離した（make-run steering: SPEC_LANDING_ATOMICITY）。本 TASK は事故 class を構造的に RED 化する census guard を新設する。

## 実装

1. **REQ-388 側の `isTaskFile` export 化** — TASK file shape 判定の単一実装化（invariant-split 回避・`parseAnchorBlocks` と同じく本契約が委譲利用）
2. **`tests/guards/spine-edge-contract.ts` 新設**（純関数・書き込みなし）:
   - `resolveSpecsLink(holderRel, link)` — markdown 相対 link を specs-relative に解決（http/絶対/specs 外は null）
   - `parseSpineRegistries(holderRel, content)` — children/references block 内の全 `- [title](link)` entry を解析
   - `scanSpineRegistryMarkerDefects` — begin/end marker 件数不一致（`REGISTRY_BLOCK_UNCLOSED`）
   - `auditSpineEdges(files)` — 6 violation kind の census:
     `PARENT_DOC_MISSING` / `PARENT_UNREGISTERED`（TASK file・bare parent は exempt）/
     `REGISTRY_TARGET_MISSING` / `REGISTRY_LINK_UNSUPPORTED` / `CHILD_BACK_ANCHOR_MISSING`（children のみ双方向）/ `REGISTRY_BLOCK_UNCLOSED`
3. **`tests/guards/spine-edge-census.test.ts` 新設**:
   - real tree exact-0（violations `toEqual([])`・ceiling ではない）+ 在庫 floor pin（filesChecked / anchorEdges / registryEntries）
   - bare parent 観測集合 `['SYSTEM_CONSTITUTION.md']` exact pin
   - 合成 fixture で 6 kind 各検出 + c818286f~1 事故 shape 再現（`PARENT_UNREGISTERED`）+ 修復後 shape GREEN
   - 境界 test: TASK exempt・root exempt・references one-way 受容（note→note wiring）
4. **atomic dogfood landing（REQ-402-006）** — 本 spec 一式（requirements / note / interview-record / tasks/overview / TASK-0285・0286）の anchor と parent 側登録（`speech-to-visuals/architecture.md` children 2 行・`speech-to-visuals/design-interview.md` children 1 行・`speech-to-visuals/note.md` references 1 行）を**同一 commit** に同梱。登録なし状態で guard が RED（自 spec 4 file が `PARENT_UNREGISTERED`）→ 登録追記で GREEN になることを実測してから land する（sweep commit 残さない運用の構造的実証）
5. **mutation 検証は TASK-0286 に分離**（REQ-402-007・Phase 196 precedent）

## 検証

- spine-edge-census 全 test GREEN（real tree exact-0 + 合成 fixture 検出）
- spine-anchor-role-census（REQ-388）が export 化で壊れず GREEN
- `specs:anchor:check` / `specs:mirror:check` / `spine:validate` GREEN
- tsc 両 config（app / test）exit 0
- dogfood: 未登録状態 RED 実測 → 同 commit 登録で GREEN

## 設計決定

1. **登録受容は children または references** — c818286f 修復後の正規形（note→note references wiring）を偽陽性にしない。双方向要求は children entry 側に限定
2. **TASK file exempt は schema 準拠** — 実在 288 TASK anchor が全て個別登録なし。全件要求は 288 行の schema 変更で bounded でない
3. **marker 件数不一致の違反化** — 未閉鎖 block は parser から entries が silent drop され視野外になるため、件数で網羅性を担保
4. **実装は tests/ 配下のみ** — 憲法の規模集計（実装 90K 行 / 320 file）の予算外・実装 src/ を触らない
