# TASK-0001: 環境設定・依存パッケージ整備

**タスクID**: TASK-0001
**タスクタイプ**: DIRECT
**推定工数**: 4時間
**フェーズ**: Phase 1 - 基盤・データ層
**信頼性レベル**: 🔵 *architecture.md, note.mdより*

## 関連文書

- **概要**: [📋 overview.md](overview.md)
- **要件定義**: [📋 requirements.md](../../spec/speech-to-visuals/requirements.md) - REQ-401
- **設計文書**: [📐 architecture.md](../../design/speech-to-visuals/architecture.md)
- **API仕様**: [🔌 api-endpoints.md](../../design/speech-to-visuals/api-endpoints.md)

## タスク概要

プロジェクトの開発環境構築。Node.js 18+, TypeScript 5.8 strict, ESM設定, パスエイリアス(@→./src), ESLint 9, Jest 30, tsx の設定を行い、全後続タスクの基盤を整備する。

## 依存タスク

- **前提タスク**: なし
- **後続タスク**: TASK-0002, TASK-0006

## 完了条件

- [ ] `npm install` がエラーなく成功する
- [ ] `npm run type-check` が成功する（TypeScript strict モード）
- [ ] `npm run test` が実行可能である
- [ ] `npm run dev` で開発サーバーが起動する
- [ ] パスエイリアス `@` → `./src` が正しく解決される
- [ ] ESLint 9 が設定され `npm run lint` が実行可能である

---

## 実装詳細

### 1. package.json 依存関係確認・整備 🔵

**信頼性**: 🔵 *note.md テクノロジースタック・architecture.md より*

package.json に以下の依存関係が正しく含まれていることを確認・整備する。

**本番依存**:
- react@18.3, react-dom@18.3
- express@5.1
- @google/generative-ai@0.24
- @remotion/player@4.0, remotion@4.0
- @supabase/supabase-js@2.58
- socket.io@4.8, socket.io-client@4.8
- @dagrejs/dagre@1.1
- kuromoji@0.1

**開発依存**:
- typescript@5.8
- vite@5.4
- eslint@9, @eslint/js
- jest@30, ts-jest@29, @types/jest
- tsx@4.20
- tailwindcss@3.4
- @types/react@18.3, @types/react-dom@18.3, @types/express@5

**実装ファイル**: `package.json`

### 2. TypeScript 設定 (tsconfig.json) 🔵

**信頼性**: 🔵 *note.md 開発ルール・REQ-401より*

```json
{
  "compilerOptions": {
    "target": "ES2022",
    "module": "ESNext",
    "moduleResolution": "bundler",
    "strict": true,
    "esModuleInterop": true,
    "skipLibCheck": true,
    "forceConsistentCasingInFileNames": true,
    "resolveJsonModule": true,
    "isolatedModules": true,
    "noEmit": true,
    "jsx": "react-jsx",
    "paths": {
      "@/*": ["./src/*"]
    },
    "types": ["jest", "node"]
  },
  "include": ["src/**/*"],
  "exclude": ["node_modules", "dist"]
}
```

**実装ファイル**: `tsconfig.json`

### 3. Vite 設定 (vite.config.ts) 🔵

**信頼性**: 🔵 *note.md・architecture.md フロントエンド構成より*

- React プラグイン設定
- パスエイリアス `@` → `./src` の解決設定
- 開発サーバーポート: 8080
- プロキシ設定（API サーバー `localhost:3001`）

**実装ファイル**: `vite.config.ts`

### 4. ESLint 設定 🔵

**信頼性**: 🔵 *note.md 開発ツールより*

ESLint 9 フラットコンフィグ形式での設定。

- TypeScript パーサー: @typescript-eslint/parser
- 推奨ルール: eslint:recommended, @typescript-eslint/recommended
- React プラグイン: eslint-plugin-react, eslint-plugin-react-hooks

**実装ファイル**: `eslint.config.js`

### 5. Jest 設定 (jest.config.ts) 🔵

**信頼性**: 🔵 *note.md 開発ツールより*

- ts-jest による TypeScript サポート
- ESM モジュール対応
- パスエイリアス `@` のマッピング
- テストディレクトリ: `src/`
- テストファイルパターン: `**/*.test.ts`, `**/*.test.tsx`

**実装ファイル**: `jest.config.ts`

### 6. npm scripts 整備 🔵

**信頼性**: 🔵 *note.md・architecture.md より*

以下のスクリプトを package.json に定義する。

- `dev`: Vite 開発サーバー起動
- `build`: Vite プロダクションビルド
- `api:dev`: Express API サーバー起動
- `type-check`: TypeScript 型チェック (`tsc --noEmit`)
- `test`: Jest テスト実行
- `lint`: ESLint 実行
- `remotion:studio`: Remotion Studio 起動

**実装ファイル**: `package.json`

---

## 単体テスト要件

### テストケース1: TypeScript strict モードのコンパイル確認 🔵

**Given**: TypeScript 5.8 strict モードが有効な tsconfig.json が存在する
**When**: `npm run type-check` を実行する
**Then**: エラーなくコンパイルが完了する

**テストファイル**: （型チェックコマンドで確認）

### テストケース2: Jest テストランナーの動作確認 🔵

**Given**: Jest 30 + ts-jest 29 が設定済み
**When**: ダミーテストファイルを作成し `npm run test` を実行する
**Then**: テストが正常に実行され結果が表示される

**テストファイル**: `src/test/setup.test.ts`

### テストケース3: パスエイリアス解決確認 🔵

**Given**: `@/types/diagram` にモジュールが存在する
**When**: `import { DiagramType } from '@/types/diagram'` を実行する
**Then**: インポートが正しく解決される

**テストファイル**: `src/test/alias-resolve.test.ts`

---

## 統合テスト要件

### 統合テスト1: 開発サーバー起動確認 🔵

**テスト内容**: `npm run dev` で Vite 開発サーバーがポート8080で起動する
**期待結果**: HTTP GET `/` が 200 を返す

### 統合テスト2: API サーバー起動確認 🔵

**テスト内容**: `npm run api:dev` で Express サーバーがポート3001で起動する
**期待結果**: HTTP GET `/api/v1/health` が 200 を返す

---

## UI/UX要件

### ローディング状態 🔵
N/A（環境設定タスクのためUIなし）

### エラー表示 🟡
N/A（環境設定タスクのためUIなし）

### モバイル対応 🔵
N/A（環境設定タスクのためUIなし）

### アクセシビリティ 🔵
N/A（環境設定タスクのためUIなし）

---

## 実装手順

### DIRECTタスクの場合

1. `/tsumiki:direct-setup` - package.json の依存関係確認・整備
2. tsconfig.json の strict + ESM 設定確認
3. vite.config.ts のパスエイリアス・プロキシ設定
4. eslint.config.js の ESLint 9 フラットコンフィグ設定
5. jest.config.ts の ts-jest + ESM 設定
6. npm scripts の定義確認
7. `/tsumiki:direct-verify` - 全コマンドの動作確認・品質確認

---

## 注意事項

- TypeScript 5.8 strict モードは必須。`any` 型の使用を避けること
- ESM（`"type": "module"`）設定を package.json に含めること
- パスエイリアスは Vite と Jest の両方で一貫して設定すること
- 既存の依存関係がある場合はバージョン互換性を確認すること
- Node.js 18+ が前提環境であることをドキュメントに明記すること

---

## 信頼性レベルサマリー

### 項目別信頼性

| カテゴリ | 🔵 青 | 🟡 黄 | 🔴 赤 | 合計 |
|---------|-------|-------|-------|------|
| 実装詳細 | 6 | 0 | 0 | 6 |
| 単体テスト | 3 | 0 | 0 | 3 |
| 統合テスト | 2 | 0 | 0 | 2 |
| UI/UX要件 | 0 | 1 | 0 | 1 |

### 全体評価

- **総項目数**: 12項目
- 🔵 **青信号**: 11項目 (92%)
- 🟡 **黄信号**: 1項目 (8%)
- 🔴 **赤信号**: 0項目 (0%)

**品質評価**: 高品質
