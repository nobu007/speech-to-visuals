ambiguities:
  - id: "AMB-QUAL-001"
    term: "QualityMonitor"
    topic: "品質メトリクスのスケール統一（0-1 と 0-100 の二重運用）"
    severity: "medium"
    uncertainty: 0.55
    candidates:
      - "単一スケール（0-1）へ統合する"
      - "二重スケール維持 + 交叉配線 guard で検出する"
    missing_info:
      - "統合時の下位互換（既存メトリクス consumer の移行範囲）"
    linked_conflicts: []
    auto_decision:
      status: "applied"
      decision_key: "AUTO:QualityMonitor.scale:dual-with-guard"
      chosen: "二重スケール維持 + REQ-382/TC-366-01 の4-leg guard で交叉配線検出"
      policy: "reversibility + blast_radius_min（統合は design-heavy）"
      confidence_auto: 0.7
      decided_at_run: "2026-08-26T10:49:16Z"
      expires_after_runs: 20
      revert_triggers:
        - "primary_evidence_contradiction"
        - "major_test_failure"
    last_touched_run: "2026-08-26T10:49:16Z"
    evidence:
      - source: "tests/guards/quality-monitor-dual-scale.test.ts#L95"
        quote: "describe('dual QualityMonitor scale contract (REQ-382 / TC-366-01)', () => {"
  - id: "AMB-QUAL-002"
    term: "QualityMonitor"
    topic: "transcriptionAccuracy / avgSceneQuality の 0.85 既定値とゲート閾値の結合"
    severity: "medium"
    uncertainty: 0.6
    candidates:
      - "0.85 を正解ラベルに基づく実測精度へ置換する"
      - "fail-closed（実測不能な指標は既定値で扱わない）を維持する"
    missing_info:
      - "文字起こし精度の正解ラベルデータセット（scope 外）"
    linked_conflicts: []
    auto_decision:
      status: "applied"
      decision_key: "AUTO:QualityMonitor.metrics:fail-closed-defaults"
      chosen: "正解ラベルが入手可能になるまで実測化せず fail-closed 継続"
      policy: "safety（捏造 metric の禁止）+ reversibility"
      confidence_auto: 0.65
      decided_at_run: "2026-08-26T10:49:16Z"
      expires_after_runs: 20
      revert_triggers:
        - "primary_evidence_contradiction"
    last_touched_run: "2026-08-26T10:49:16Z"
    evidence:
      - source: "src/pipeline/quality-estimators.ts#L16"
        quote: "silently coerced to the `sanitizeFinite` fallbacks 0.85 / 0.75 / 0."
  - id: "AMB-VIS-001"
    term: "LayoutEngine"
    topic: "ezo overlapDetectionMode 設定の生成時未読（latent config）"
    severity: "low"
    uncertainty: 0.5
    candidates:
      - "overlapDetectionMode をレイアウト生成時に読むように実装する"
      - "未読のまま文書化して維持する"
    missing_info:
      - "3モード（strict/balanced/performance）の生成時分岐仕様"
    linked_conflicts: []
    auto_decision:
      status: "applied"
      decision_key: "AUTO:LayoutEngine.overlapDetectionMode:latent-documented"
      chosen: "生成時は未読のまま維持（config-boundary テストが未読事実を固定）"
      policy: "blast_radius_min（実装は design-heavy・現状破壊のリスク）"
      confidence_auto: 0.6
      decided_at_run: "2026-08-26T10:49:16Z"
      expires_after_runs: 20
      revert_triggers:
        - "primary_evidence_contradiction"
    last_touched_run: "2026-08-26T10:49:16Z"
    evidence:
      - source: "src/pipeline/__tests__/config-boundary-reaches-generation.test.ts#L68"
        quote: ".overlapDetectionMode is never read at generation;"
      - source: "src/visualization/enhanced-zero-overlap-layout.ts#L38"
        quote: "overlapDetectionMode: 'strict' | 'balanced' | 'performance';"
  - id: "AMB-PROC-001"
    term: "GuardHarness"
    topic: "Q3（invariant 抽出率）の分母定義: テスト全ファイル比 50% は 386 invariants を要求し Rule 7（統合優先）と構造的に衝突する"
    severity: "low"
    uncertainty: 0.5
    candidates:
      - "テスト全ファイル比 50% を維持する（772 ファイル → 386 invariants が必要）"
      - "bug-class 網羅基準（census / mutation-pinning guard が表す再発クラス）へ分母を再定義する"
    missing_info:
      - "抽出可能な制約の総数の機械導出方法（抽出率の真の分母）"
    linked_conflicts: []
    auto_decision:
      status: "applied"
      decision_key: "AUTO:ConceptSync.Q3-denominator:bug-class-coverage"
      chosen: "Q3 は bug-class 網羅基準で運用し、テスト全ファイル比は参考値として記録する（16→26 invariants・増産は続行）"
      policy: "integration-first（Rule 7 統合優先）+ blast_radius_min"
      confidence_auto: 0.65
      decided_at_run: "2026-08-26T12:48:37Z"
      expires_after_runs: 20
      revert_triggers:
        - "primary_evidence_contradiction"
    last_touched_run: "2026-08-26T12:48:37Z"
    evidence:
      - source: "tests/guards/concept-sync-consistency.test.ts#L5"
        quote: "The bootstrap generated 41 ontology terms / 16 invariants / 41 mappings /"
  - id: "AMB-EXP-001"
    term: "ExportEngine"
    topic: "backgroundColor 契約外長度 (#RGBA 等 3/6 桁以外) 入力の方針: fail-fast・無視・fail-open のいずれ也未指定"
    severity: "low"
    uncertainty: 0.45
    candidates:
      - "現行どおり per-channel fail-open（NaN → 1.000、'#3C88' → 0.235 0.533 1.000 rg）"
      - "fail-fast（ExportError で拒否）"
      - "CSS #RGBA (#RRGGBBAA) 対応として正式解釈する"
    missing_info:
      - "UI/API 側から 3/6 桁以外の backgroundColor が流入する経路の有無"
    linked_conflicts: []
    auto_decision:
      status: "applied"
      decision_key: "AUTO:ExportEngine.pdfColorFill.invalid-length:fail-open"
      chosen: "契約外長度は現行の per-channel fail-open を維持し test leg で文書化（fail-fast 化・#RGBA 対応は意図した契約変更として扱う）"
      policy: "reversibility + blast_radius_min（既存 NaN→1 fallback と同一・UI は 6 桁 hex のみ供給）"
      confidence_auto: 0.6
      decided_at_run: "2026-08-26T21:12:22Z"
      expires_after_runs: 20
      revert_triggers:
        - "primary_evidence_contradiction"
        - "security_risk"
    last_touched_run: "2026-08-26T22:15:36Z"
    evidence:
      - source: "tests/unit/export/multi-format-exporter.test.ts#L401-L406"
        quote: "pdfColorFill's contract is #RGB / #RRGGBB. … current policy is fail-open, not fail-fast: '#3C88' is not doubled … Pinning this makes the … policy explicit, so extending to #RGBA support or switching to … fail-fast later is a conscious contract change, not a silent drift."
      - source: "tests/unit/export/multi-format-exporter.test.ts#L421"
        quote: "expect(await (svg.data as Blob).text()).toContain('<rect width=\"800\" height=\"600\" fill=\"#3C88\"/>');"
      - source: "tests/unit/export/multi-format-exporter.test.ts#L415-L418"
        quote: "The expect is rect-anchored (the … background rect's attribute list is exactly width/height/fill), so a … '#3C88' that leaked into a node fill cannot mask a broken background … hand-off the way a bare `fill=\"#3C88\"` containment would."
