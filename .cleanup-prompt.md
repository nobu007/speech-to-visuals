You are a file cleanup assistant. Analyze the following list of files and identify which ones are junk files that should be deleted.

Repository: speech-to-visuals

These files were found during automated AI development:



JUNK files (delete):
- Log files: *.log, *-log-*, debug logs
- Report files: *report*, *-report-*
- Trace files: *trace*, *-trace-*
- Cache files: *.cache, *-cache-*
- Temporary files: *.tmp, *.temp, *.bak, *.swp
- Output dumps: *output*, *-output-*
- AI agent execution artifacts
- Test execution artifacts

KEEP files (do not delete):
- Source code: *.py, *.js, *.ts, *.java, *.rb, *.go, *.rs, *.c, *.cpp, *.h
- Configuration files: *.json, *.yaml, *.yml, *.toml, *.env.*, *.ini, *.cfg
- Documentation: README*.md, *.md (unless it's a generated report), *.txt, *.rst
- Test files: test_*.py, *.test.ts, *.spec.ts, *_test.rb
- Build artifacts: package.json, requirements.txt, Gemfile, Cargo.toml, go.mod
- CI/CD files: .github/**/*.yml, .gitlab-ci.yml, Dockerfile

CRITICAL: Respond ONLY with a valid JSON array. Do NOT include any explanation, markdown formatting, or additional text.

Example correct response:
["path/to/file1.log", "path/to/file2.tmp", "path/to/report.txt"]

If no junk files, respond with:
[]

Now respond with the JSON array of files to delete: